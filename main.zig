const std = @import("std");
const builtin = @import("builtin");

const version = "0.4.0";

const OutputFormat = enum { json, table, plain };

// --- view: embedded browser assets ---
// View asset pins
// marked@15.0.12 sha256:3e7e7d7feb3e5d58cb6c804f68ab5c24cc7e5eb6270fd6e5cbb9124739217d0c
// DOMPurify@3.2.6 sha256:89e1fa7647cb495370d3a997ace4387f5d15d9f4c5af12352c53daa400956287
// Licenses: marked BSD-3-Clause / MIT banner; DOMPurify Apache-2.0 (+ MPL-2.0 dual upstream)
const marked_js: []const u8 =
    "/**\n * marked v15.0.12 - a markdown parser\n * Copyright (c) 2011-2025, Christopher Jeffrey. (MIT Licensed)\n * https://github.com/markedjs/marked\n */\n\n/**\n * DO NOT EDIT THIS FILE\n * The code in this file is generated from files in ./src/\n */\n(function(g,f){if(typeof exports==\"object\"&&typeof module<\"u\"){module.exports=f()}else if(\"function\"==typeof define && define.amd){define(\"marked\",f)}else {g[\"marked\"]=f()}}(typeof globalThis < \"u\" ? globalThis : typeof self < \"u\" ? self : this,function(){var exports={}" ++ ";var __exports=exports;var module={exports};\n\"use strict\";var H=Object.defineProperty;var be=Object.getOwnPropertyDescriptor;var Te=Object.getOwnPropertyNames;var we=Object.prototype.hasOwnProperty;var ye=(l,e)=>{for(var t in e)H(l,t,{get:e[t],enumerable:!0})},Re=(l,e,t,n)=>{if(e&&typeof e==\"object\"||typeof e==\"function\")for(let s of Te(e))!we.call(l,s)&&s!==t&&H(l,s,{get:()=>e[s],enumerable:!(n=be(e,s))||n.enumerable});return l};var Se=l=>Re(H({},\"__esModule\",{value:!0}),l);var kt={};ye(kt,{Hooks:()=>L,Lex" ++ "er:()=>x,Marked:()=>E,Parser:()=>b,Renderer:()=>$,TextRenderer:()=>_,Tokenizer:()=>S,defaults:()=>w,getDefaults:()=>z,lexer:()=>ht,marked:()=>k,options:()=>it,parse:()=>pt,parseInline:()=>ct,parser:()=>ut,setOptions:()=>ot,use:()=>lt,walkTokens:()=>at});module.exports=Se(kt);function z(){return{async:!1,breaks:!1,extensions:null,gfm:!0,hooks:null,pedantic:!1,renderer:null,silent:!1,tokenizer:null,walkTokens:null}}var w=z();function N(l){w=l}var I={exec:()=>null};function h(l,e=\"\"){let t=typeof l==\"string\"?l" ++ ":l.source,n={replace:(s,i)=>{let r=typeof i==\"string\"?i:i.source;return r=r.replace(m.caret,\"$1\"),t=t.replace(s,r),n},getRegex:()=>new RegExp(t,e)};return n}var m={codeRemoveIndent:/^(?: {1,4}| {0,3}\\t)/gm,outputLinkReplace:/\\\\([\\[\\]])/g,indentCodeCompensation:/^(\\s+)(?:```)/,beginningSpace:/^\\s+/,endingHash:/#$/,startingSpaceChar:/^ /,endingSpaceChar:/ $/,nonSpaceChar:/[^ ]/,newLineCharGlobal:/\\n/g,tabCharGlobal:/\\t/g,multipleSpaceGlobal:/\\s+/g,blankLine:/^[ \\t]*$/,doubleBlankLine:/\\n[ \\t]*\\n[ \\t]*$/,block" ++ "quoteStart:/^ {0,3}>/,blockquoteSetextReplace:/\\n {0,3}((?:=+|-+) *)(?=\\n|$)/g,blockquoteSetextReplace2:/^ {0,3}>[ \\t]?/gm,listReplaceTabs:/^\\t+/,listReplaceNesting:/^ {1,4}(?=( {4})*[^ ])/g,listIsTask:/^\\[[ xX]\\] /,listReplaceTask:/^\\[[ xX]\\] +/,anyLine:/\\n.*\\n/,hrefBrackets:/^<(.*)>$/,tableDelimiter:/[:|]/,tableAlignChars:/^\\||\\| *$/g,tableRowBlankLine:/\\n[ \\t]*$/,tableAlignRight:/^ *-+: *$/,tableAlignCenter:/^ *:-+: *$/,tableAlignLeft:/^ *:-+ *$/,startATag:/^<a /i,endATag:/^<\\/a>/i,startPreScriptTag:/^<(" ++ "pre|code|kbd|script)(\\s|>)/i,endPreScriptTag:/^<\\/(pre|code|kbd|script)(\\s|>)/i,startAngleBracket:/^</,endAngleBracket:/>$/,pedanticHrefTitle:/^([^'\"]*[^\\s])\\s+(['\"])(.*)\\2/,unicodeAlphaNumeric:/[\\p{L}\\p{N}]/u,escapeTest:/[&<>\"']/,escapeReplace:/[&<>\"']/g,escapeTestNoEncode:/[<>\"']|&(?!(#\\d{1,7}|#[Xx][a-fA-F0-9]{1,6}|\\w+);)/,escapeReplaceNoEncode:/[<>\"']|&(?!(#\\d{1,7}|#[Xx][a-fA-F0-9]{1,6}|\\w+);)/g,unescapeTest:/&(#(?:\\d+)|(?:#x[0-9A-Fa-f]+)|(?:\\w+));?/ig,caret:/(^|[^\\[])\\^/g,percentDecode:/%25/g,findPipe:/" ++ "\\|/g,splitPipe:/ \\|/,slashPipe:/\\\\\\|/g,carriageReturn:/\\r\\n|\\r/g,spaceLine:/^ +$/gm,notSpaceStart:/^\\S*/,endingNewline:/\\n$/,listItemRegex:l=>new RegExp(`^( {0,3}${l})((?:[\t ][^\\\\n]*)?(?:\\\\n|$))`),nextBulletRegex:l=>new RegExp(`^ {0,${Math.min(3,l-1)}}(?:[*+-]|\\\\d{1,9}[.)])((?:[ \t][^\\\\n]*)?(?:\\\\n|$))`),hrRegex:l=>new RegExp(`^ {0,${Math.min(3,l-1)}}((?:- *){3,}|(?:_ *){3,}|(?:\\\\* *){3,})(?:\\\\n+|$)`),fencesBeginRegex:l=>new RegExp(`^ {0,${Math.min(3,l-1)}}(?:\\`\\`\\`|~~~)`),headingBeginRegex:l=>new RegExp(`^ {" ++ "0,${Math.min(3,l-1)}}#`),htmlBeginRegex:l=>new RegExp(`^ {0,${Math.min(3,l-1)}}<(?:[a-z].*>|!--)`,\"i\")},$e=/^(?:[ \\t]*(?:\\n|$))+/,_e=/^((?: {4}| {0,3}\\t)[^\\n]+(?:\\n(?:[ \\t]*(?:\\n|$))*)?)+/,Le=/^ {0,3}(`{3,}(?=[^`\\n]*(?:\\n|$))|~{3,})([^\\n]*)(?:\\n|$)(?:|([\\s\\S]*?)(?:\\n|$))(?: {0,3}\\1[~`]* *(?=\\n|$)|$)/,O=/^ {0,3}((?:-[\\t ]*){3,}|(?:_[ \\t]*){3,}|(?:\\*[ \\t]*){3,})(?:\\n+|$)/,ze=/^ {0,3}(#{1,6})(?=\\s|$)(.*)(?:\\n+|$)/,F=/(?:[*+-]|\\d{1,9}[.)])/,ie=/^(?!bull |blockCode|fences|blockquote|heading|html|table)((?:.|\\n(?" ++ "!\\s*?\\n|bull |blockCode|fences|blockquote|heading|html|table))+?)\\n {0,3}(=+|-+) *(?:\\n+|$)/,oe=h(ie).replace(/bull/g,F).replace(/blockCode/g,/(?: {4}| {0,3}\\t)/).replace(/fences/g,/ {0,3}(?:`{3,}|~{3,})/).replace(/blockquote/g,/ {0,3}>/).replace(/heading/g,/ {0,3}#{1,6}/).replace(/html/g,/ {0,3}<[^\\n>]+>\\n/).replace(/\\|table/g,\"\").getRegex(),Me=h(ie).replace(/bull/g,F).replace(/blockCode/g,/(?: {4}| {0,3}\\t)/).replace(/fences/g,/ {0,3}(?:`{3,}|~{3,})/).replace(/blockquote/g,/ {0,3}>/).replace(/heading/g,/ " ++ "{0,3}#{1,6}/).replace(/html/g,/ {0,3}<[^\\n>]+>\\n/).replace(/table/g,/ {0,3}\\|?(?:[:\\- ]*\\|)+[\\:\\- ]*\\n/).getRegex(),Q=/^([^\\n]+(?:\\n(?!hr|heading|lheading|blockquote|fences|list|html|table| +\\n)[^\\n]+)*)/,Pe=/^[^\\n]+/,U=/(?!\\s*\\])(?:\\\\.|[^\\[\\]\\\\])+/,Ae=h(/^ {0,3}\\[(label)\\]: *(?:\\n[ \\t]*)?([^<\\s][^\\s]*|<.*?>)(?:(?: +(?:\\n[ \\t]*)?| *\\n[ \\t]*)(title))? *(?:\\n+|$)/).replace(\"label\",U).replace(\"title\",/(?:\"(?:\\\\\"?|[^\"\\\\])*\"|'[^'\\n]*(?:\\n[^'\\n]+)*\\n?'|\\([^()]*\\))/).getRegex(),Ee=h(/^( {0,3}bull)([ \\t][^\\n]+?)?(?" ++ ":\\n|$)/).replace(/bull/g,F).getRegex(),v=\"address|article|aside|base|basefont|blockquote|body|caption|center|col|colgroup|dd|details|dialog|dir|div|dl|dt|fieldset|figcaption|figure|footer|form|frame|frameset|h[1-6]|head|header|hr|html|iframe|legend|li|link|main|menu|menuitem|meta|nav|noframes|ol|optgroup|option|p|param|search|section|summary|table|tbody|td|tfoot|th|thead|title|tr|track|ul\",K=/<!--(?:-?>|[\\s\\S]*?(?:-->|$))/,Ce=h(\"^ {0,3}(?:<(script|pre|style|textarea)[\\\\s>][\\\\s\\\\S]*?(?:</\\\\1>[^\\\\n]*\\\\n+|$)|c" ++ "omment[^\\\\n]*(\\\\n+|$)|<\\\\?[\\\\s\\\\S]*?(?:\\\\?>\\\\n*|$)|<![A-Z][\\\\s\\\\S]*?(?:>\\\\n*|$)|<!\\\\[CDATA\\\\[[\\\\s\\\\S]*?(?:\\\\]\\\\]>\\\\n*|$)|</?(tag)(?: +|\\\\n|/?>)[\\\\s\\\\S]*?(?:(?:\\\\n[ \t]*)+\\\\n|$)|<(?!script|pre|style|textarea)([a-z][\\\\w-]*)(?:attribute)*? */?>(?=[ \\\\t]*(?:\\\\n|$))[\\\\s\\\\S]*?(?:(?:\\\\n[ \t]*)+\\\\n|$)|</(?!script|pre|style|textarea)[a-z][\\\\w-]*\\\\s*>(?=[ \\\\t]*(?:\\\\n|$))[\\\\s\\\\S]*?(?:(?:\\\\n[ \t]*)+\\\\n|$))\",\"i\").replace(\"comment\",K).replace(\"tag\",v).replace(\"attribute\",/ +[a-zA-Z:_][\\w.:-]*(?: *= *\"[^\"\\n]*\"| *= *'[^'\\n]*'" ++ "| *= *[^\\s\"'=<>`]+)?/).getRegex(),le=h(Q).replace(\"hr\",O).replace(\"heading\",\" {0,3}#{1,6}(?:\\\\s|$)\").replace(\"|lheading\",\"\").replace(\"|table\",\"\").replace(\"blockquote\",\" {0,3}>\").replace(\"fences\",\" {0,3}(?:`{3,}(?=[^`\\\\n]*\\\\n)|~{3,})[^\\\\n]*\\\\n\").replace(\"list\",\" {0,3}(?:[*+-]|1[.)]) \").replace(\"html\",\"</?(?:tag)(?: +|\\\\n|/?>)|<(?:script|pre|style|textarea|!--)\").replace(\"tag\",v).getRegex(),Ie=h(/^( {0,3}> ?(paragraph|[^\\n]*)(?:\\n|$))+/).replace(\"paragraph\",le).getRegex(),X={blockquote:Ie,code:_e,def:Ae,fence" ++ "s:Le,heading:ze,hr:O,html:Ce,lheading:oe,list:Ee,newline:$e,paragraph:le,table:I,text:Pe},re=h(\"^ *([^\\\\n ].*)\\\\n {0,3}((?:\\\\| *)?:?-+:? *(?:\\\\| *:?-+:? *)*(?:\\\\| *)?)(?:\\\\n((?:(?! *\\\\n|hr|heading|blockquote|code|fences|list|html).*(?:\\\\n|$))*)\\\\n*|$)\").replace(\"hr\",O).replace(\"heading\",\" {0,3}#{1,6}(?:\\\\s|$)\").replace(\"blockquote\",\" {0,3}>\").replace(\"code\",\"(?: {4}| {0,3}\t)[^\\\\n]\").replace(\"fences\",\" {0,3}(?:`{3,}(?=[^`\\\\n]*\\\\n)|~{3,})[^\\\\n]*\\\\n\").replace(\"list\",\" {0,3}(?:[*+-]|1[.)]) \").replace(\"html\",\"</" ++ "?(?:tag)(?: +|\\\\n|/?>)|<(?:script|pre|style|textarea|!--)\").replace(\"tag\",v).getRegex(),Oe={...X,lheading:Me,table:re,paragraph:h(Q).replace(\"hr\",O).replace(\"heading\",\" {0,3}#{1,6}(?:\\\\s|$)\").replace(\"|lheading\",\"\").replace(\"table\",re).replace(\"blockquote\",\" {0,3}>\").replace(\"fences\",\" {0,3}(?:`{3,}(?=[^`\\\\n]*\\\\n)|~{3,})[^\\\\n]*\\\\n\").replace(\"list\",\" {0,3}(?:[*+-]|1[.)]) \").replace(\"html\",\"</?(?:tag)(?: +|\\\\n|/?>)|<(?:script|pre|style|textarea|!--)\").replace(\"tag\",v).getRegex()},Be={...X,html:h(`^ *(?:commen" ++ "t *(?:\\\\n|\\\\s*$)|<(tag)[\\\\s\\\\S]+?</\\\\1> *(?:\\\\n{2,}|\\\\s*$)|<tag(?:\"[^\"]*\"|'[^']*'|\\\\s[^'\"/>\\\\s]*)*?/?> *(?:\\\\n{2,}|\\\\s*$))`).replace(\"comment\",K).replace(/tag/g,\"(?!(?:a|em|strong|small|s|cite|q|dfn|abbr|data|time|code|var|samp|kbd|sub|sup|i|b|u|mark|ruby|rt|rp|bdi|bdo|span|br|wbr|ins|del|img)\\\\b)\\\\w+(?!:|[^\\\\w\\\\s@]*@)\\\\b\").getRegex(),def:/^ *\\[([^\\]]+)\\]: *<?([^\\s>]+)>?(?: +([\"(][^\\n]+[\")]))? *(?:\\n+|$)/,heading:/^(#{1,6})(.*)(?:\\n+|$)/,fences:I,lheading:/^(.+?)\\n {0,3}(=+|-+) *(?:\\n+|$)/,paragraph:h(Q).re" ++ "place(\"hr\",O).replace(\"heading\",` *#{1,6} *[^\n]`).replace(\"lheading\",oe).replace(\"|table\",\"\").replace(\"blockquote\",\" {0,3}>\").replace(\"|fences\",\"\").replace(\"|list\",\"\").replace(\"|html\",\"\").replace(\"|tag\",\"\").getRegex()},qe=/^\\\\([!\"#$%&'()*+,\\-./:;<=>?@\\[\\]\\\\^_`{|}~])/,ve=/^(`+)([^`]|[^`][\\s\\S]*?[^`])\\1(?!`)/,ae=/^( {2,}|\\\\)\\n(?!\\s*$)/,De=/^(`+|[^`])(?:(?= {2,}\\n)|[\\s\\S]*?(?:(?=[\\\\<!\\[`*_]|\\b_|$)|[^ ](?= {2,}\\n)))/,D=/[\\p{P}\\p{S}]/u,W=/[\\s\\p{P}\\p{S}]/u,ce=/[^\\s\\p{P}\\p{S}]/u,Ze=h(/^((?![*_])punctSpace)/,\"u\").r" ++ "eplace(/punctSpace/g,W).getRegex(),pe=/(?!~)[\\p{P}\\p{S}]/u,Ge=/(?!~)[\\s\\p{P}\\p{S}]/u,He=/(?:[^\\s\\p{P}\\p{S}]|~)/u,Ne=/\\[[^[\\]]*?\\]\\((?:\\\\.|[^\\\\\\(\\)]|\\((?:\\\\.|[^\\\\\\(\\)])*\\))*\\)|`[^`]*?`|<[^<>]*?>/g,ue=/^(?:\\*+(?:((?!\\*)punct)|[^\\s*]))|^_+(?:((?!_)punct)|([^\\s_]))/,je=h(ue,\"u\").replace(/punct/g,D).getRegex(),Fe=h(ue,\"u\").replace(/punct/g,pe).getRegex(),he=\"^[^_*]*?__[^_*]*?\\\\*[^_*]*?(?=__)|[^*]+(?=[^*])|(?!\\\\*)punct(\\\\*+)(?=[\\\\s]|$)|notPunctSpace(\\\\*+)(?!\\\\*)(?=punctSpace|$)|(?!\\\\*)punctSpace(\\\\*+)(?=notPunctS" ++ "pace)|[\\\\s](\\\\*+)(?!\\\\*)(?=punct)|(?!\\\\*)punct(\\\\*+)(?!\\\\*)(?=punct)|notPunctSpace(\\\\*+)(?=notPunctSpace)\",Qe=h(he,\"gu\").replace(/notPunctSpace/g,ce).replace(/punctSpace/g,W).replace(/punct/g,D).getRegex(),Ue=h(he,\"gu\").replace(/notPunctSpace/g,He).replace(/punctSpace/g,Ge).replace(/punct/g,pe).getRegex(),Ke=h(\"^[^_*]*?\\\\*\\\\*[^_*]*?_[^_*]*?(?=\\\\*\\\\*)|[^_]+(?=[^_])|(?!_)punct(_+)(?=[\\\\s]|$)|notPunctSpace(_+)(?!_)(?=punctSpace|$)|(?!_)punctSpace(_+)(?=notPunctSpace)|[\\\\s](_+)(?!_)(?=punct)|(?!_)punct(_+)(?!_)" ++ "(?=punct)\",\"gu\").replace(/notPunctSpace/g,ce).replace(/punctSpace/g,W).replace(/punct/g,D).getRegex(),Xe=h(/\\\\(punct)/,\"gu\").replace(/punct/g,D).getRegex(),We=h(/^<(scheme:[^\\s\\x00-\\x1f<>]*|email)>/).replace(\"scheme\",/[a-zA-Z][a-zA-Z0-9+.-]{1,31}/).replace(\"email\",/[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+(@)[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)+(?![-_])/).getRegex(),Je=h(K).replace(\"(?:-->|$)\",\"-->\").getRegex(),Ve=h(\"^comment|^</[a-zA-Z][\\\\w:-]*\\\\s*>|^<[a-z" ++ "A-Z][\\\\w-]*(?:attribute)*?\\\\s*/?>|^<\\\\?[\\\\s\\\\S]*?\\\\?>|^<![a-zA-Z]+\\\\s[\\\\s\\\\S]*?>|^<!\\\\[CDATA\\\\[[\\\\s\\\\S]*?\\\\]\\\\]>\").replace(\"comment\",Je).replace(\"attribute\",/\\s+[a-zA-Z:_][\\w.:-]*(?:\\s*=\\s*\"[^\"]*\"|\\s*=\\s*'[^']*'|\\s*=\\s*[^\\s\"'=<>`]+)?/).getRegex(),q=/(?:\\[(?:\\\\.|[^\\[\\]\\\\])*\\]|\\\\.|`[^`]*`|[^\\[\\]\\\\`])*?/,Ye=h(/^!?\\[(label)\\]\\(\\s*(href)(?:(?:[ \\t]*(?:\\n[ \\t]*)?)(title))?\\s*\\)/).replace(\"label\",q).replace(\"href\",/<(?:\\\\.|[^\\n<>\\\\])+>|[^ \\t\\n\\x00-\\x1f]*/).replace(\"title\",/\"(?:\\\\\"?|[^\"\\\\])*\"|'(?:\\\\'?|[^'\\\\])*'|\\((" ++ "?:\\\\\\)?|[^)\\\\])*\\)/).getRegex(),ke=h(/^!?\\[(label)\\]\\[(ref)\\]/).replace(\"label\",q).replace(\"ref\",U).getRegex(),ge=h(/^!?\\[(ref)\\](?:\\[\\])?/).replace(\"ref\",U).getRegex(),et=h(\"reflink|nolink(?!\\\\()\",\"g\").replace(\"reflink\",ke).replace(\"nolink\",ge).getRegex(),J={_backpedal:I,anyPunctuation:Xe,autolink:We,blockSkip:Ne,br:ae,code:ve,del:I,emStrongLDelim:je,emStrongRDelimAst:Qe,emStrongRDelimUnd:Ke,escape:qe,link:Ye,nolink:ge,punctuation:Ze,reflink:ke,reflinkSearch:et,tag:Ve,text:De,url:I},tt={...J,link:h(/^!?\\[(" ++ "label)\\]\\((.*?)\\)/).replace(\"label\",q).getRegex(),reflink:h(/^!?\\[(label)\\]\\s*\\[([^\\]]*)\\]/).replace(\"label\",q).getRegex()},j={...J,emStrongRDelimAst:Ue,emStrongLDelim:Fe,url:h(/^((?:ftp|https?):\\/\\/|www\\.)(?:[a-zA-Z0-9\\-]+\\.?)+[^\\s<]*|^email/,\"i\").replace(\"email\",/[A-Za-z0-9._+-]+(@)[a-zA-Z0-9-_]+(?:\\.[a-zA-Z0-9-_]*[a-zA-Z0-9])+(?![-_])/).getRegex(),_backpedal:/(?:[^?!.,:;*_'\"~()&]+|\\([^)]*\\)|&(?![a-zA-Z0-9]+;$)|[?!.,:;*_'\"~)]+(?!$))+/,del:/^(~~?)(?=[^\\s~])((?:\\\\.|[^\\\\])*?(?:\\\\.|[^\\s~\\\\]))\\1(?=[^~]|$)/,tex" ++ "t:/^([`~]+|[^`~])(?:(?= {2,}\\n)|(?=[a-zA-Z0-9.!#$%&'*+\\/=?_`{\\|}~-]+@)|[\\s\\S]*?(?:(?=[\\\\<!\\[`*~_]|\\b_|https?:\\/\\/|ftp:\\/\\/|www\\.|$)|[^ ](?= {2,}\\n)|[^a-zA-Z0-9.!#$%&'*+\\/=?_`{\\|}~-](?=[a-zA-Z0-9.!#$%&'*+\\/=?_`{\\|}~-]+@)))/},nt={...j,br:h(ae).replace(\"{2,}\",\"*\").getRegex(),text:h(j.text).replace(\"\\\\b_\",\"\\\\b_| {2,}\\\\n\").replace(/\\{2,\\}/g,\"*\").getRegex()},B={normal:X,gfm:Oe,pedantic:Be},P={normal:J,gfm:j,breaks:nt,pedantic:tt};var st={\"&\":\"&amp;\",\"<\":\"&lt;\",\">\":\"&gt;\",'\"':\"&quot;\",\"'\":\"&#39;\"},fe=l=>st[l];func" ++ "tion R(l,e){if(e){if(m.escapeTest.test(l))return l.replace(m.escapeReplace,fe)}else if(m.escapeTestNoEncode.test(l))return l.replace(m.escapeReplaceNoEncode,fe);return l}function V(l){try{l=encodeURI(l).replace(m.percentDecode,\"%\")}catch{return null}return l}function Y(l,e){let t=l.replace(m.findPipe,(i,r,o)=>{let a=!1,c=r;for(;--c>=0&&o[c]===\"\\\\\";)a=!a;return a?\"|\":\" |\"}),n=t.split(m.splitPipe),s=0;if(n[0].trim()||n.shift(),n.length>0&&!n.at(-1)?.trim()&&n.pop(),e)if(n.length>e)n.splice(e);else for(;n.leng" ++ "th<e;)n.push(\"\");for(;s<n.length;s++)n[s]=n[s].trim().replace(m.slashPipe,\"|\");return n}function A(l,e,t){let n=l.length;if(n===0)return\"\";let s=0;for(;s<n;){let i=l.charAt(n-s-1);if(i===e&&!t)s++;else if(i!==e&&t)s++;else break}return l.slice(0,n-s)}function de(l,e){if(l.indexOf(e[1])===-1)return-1;let t=0;for(let n=0;n<l.length;n++)if(l[n]===\"\\\\\")n++;else if(l[n]===e[0])t++;else if(l[n]===e[1]&&(t--,t<0))return n;return t>0?-2:-1}function me(l,e,t,n,s){let i=e.href,r=e.title||null,o=l[1].replace(s.other.o" ++ "utputLinkReplace,\"$1\");n.state.inLink=!0;let a={type:l[0].charAt(0)===\"!\"?\"image\":\"link\",raw:t,href:i,title:r,text:o,tokens:n.inlineTokens(o)};return n.state.inLink=!1,a}function rt(l,e,t){let n=l.match(t.other.indentCodeCompensation);if(n===null)return e;let s=n[1];return e.split(`\n`).map(i=>{let r=i.match(t.other.beginningSpace);if(r===null)return i;let[o]=r;return o.length>=s.length?i.slice(s.length):i}).join(`\n`)}var S=class{options;rules;lexer;constructor(e){this.options=e||w}space(e){let t=this.rules." ++ "block.newline.exec(e);if(t&&t[0].length>0)return{type:\"space\",raw:t[0]}}code(e){let t=this.rules.block.code.exec(e);if(t){let n=t[0].replace(this.rules.other.codeRemoveIndent,\"\");return{type:\"code\",raw:t[0],codeBlockStyle:\"indented\",text:this.options.pedantic?n:A(n,`\n`)}}}fences(e){let t=this.rules.block.fences.exec(e);if(t){let n=t[0],s=rt(n,t[3]||\"\",this.rules);return{type:\"code\",raw:n,lang:t[2]?t[2].trim().replace(this.rules.inline.anyPunctuation,\"$1\"):t[2],text:s}}}heading(e){let t=this.rules.block.head" ++ "ing.exec(e);if(t){let n=t[2].trim();if(this.rules.other.endingHash.test(n)){let s=A(n,\"#\");(this.options.pedantic||!s||this.rules.other.endingSpaceChar.test(s))&&(n=s.trim())}return{type:\"heading\",raw:t[0],depth:t[1].length,text:n,tokens:this.lexer.inline(n)}}}hr(e){let t=this.rules.block.hr.exec(e);if(t)return{type:\"hr\",raw:A(t[0],`\n`)}}blockquote(e){let t=this.rules.block.blockquote.exec(e);if(t){let n=A(t[0],`\n`).split(`\n`),s=\"\",i=\"\",r=[];for(;n.length>0;){let o=!1,a=[],c;for(c=0;c<n.length;c++)if(this.r" ++ "ules.other.blockquoteStart.test(n[c]))a.push(n[c]),o=!0;else if(!o)a.push(n[c]);else break;n=n.slice(c);let p=a.join(`\n`),u=p.replace(this.rules.other.blockquoteSetextReplace,`\n    $1`).replace(this.rules.other.blockquoteSetextReplace2,\"\");s=s?`${s}\n${p}`:p,i=i?`${i}\n${u}`:u;let d=this.lexer.state.top;if(this.lexer.state.top=!0,this.lexer.blockTokens(u,r,!0),this.lexer.state.top=d,n.length===0)break;let g=r.at(-1);if(g?.type===\"code\")break;if(g?.type===\"blockquote\"){let T=g,f=T.raw+`\n`+n.join(`\n`),y=this.bl" ++ "ockquote(f);r[r.length-1]=y,s=s.substring(0,s.length-T.raw.length)+y.raw,i=i.substring(0,i.length-T.text.length)+y.text;break}else if(g?.type===\"list\"){let T=g,f=T.raw+`\n`+n.join(`\n`),y=this.list(f);r[r.length-1]=y,s=s.substring(0,s.length-g.raw.length)+y.raw,i=i.substring(0,i.length-T.raw.length)+y.raw,n=f.substring(r.at(-1).raw.length).split(`\n`);continue}}return{type:\"blockquote\",raw:s,tokens:r,text:i}}}list(e){let t=this.rules.block.list.exec(e);if(t){let n=t[1].trim(),s=n.length>1,i={type:\"list\",raw:\"\"" ++ ",ordered:s,start:s?+n.slice(0,-1):\"\",loose:!1,items:[]};n=s?`\\\\d{1,9}\\\\${n.slice(-1)}`:`\\\\${n}`,this.options.pedantic&&(n=s?n:\"[*+-]\");let r=this.rules.other.listItemRegex(n),o=!1;for(;e;){let c=!1,p=\"\",u=\"\";if(!(t=r.exec(e))||this.rules.block.hr.test(e))break;p=t[0],e=e.substring(p.length);let d=t[2].split(`\n`,1)[0].replace(this.rules.other.listReplaceTabs,Z=>\" \".repeat(3*Z.length)),g=e.split(`\n`,1)[0],T=!d.trim(),f=0;if(this.options.pedantic?(f=2,u=d.trimStart()):T?f=t[1].length+1:(f=t[2].search(this.rule" ++ "s.other.nonSpaceChar),f=f>4?1:f,u=d.slice(f),f+=t[1].length),T&&this.rules.other.blankLine.test(g)&&(p+=g+`\n`,e=e.substring(g.length+1),c=!0),!c){let Z=this.rules.other.nextBulletRegex(f),te=this.rules.other.hrRegex(f),ne=this.rules.other.fencesBeginRegex(f),se=this.rules.other.headingBeginRegex(f),xe=this.rules.other.htmlBeginRegex(f);for(;e;){let G=e.split(`\n`,1)[0],C;if(g=G,this.options.pedantic?(g=g.replace(this.rules.other.listReplaceNesting,\"  \"),C=g):C=g.replace(this.rules.other.tabCharGlobal,\"    \")" ++ ",ne.test(g)||se.test(g)||xe.test(g)||Z.test(g)||te.test(g))break;if(C.search(this.rules.other.nonSpaceChar)>=f||!g.trim())u+=`\n`+C.slice(f);else{if(T||d.replace(this.rules.other.tabCharGlobal,\"    \").search(this.rules.other.nonSpaceChar)>=4||ne.test(d)||se.test(d)||te.test(d))break;u+=`\n`+g}!T&&!g.trim()&&(T=!0),p+=G+`\n`,e=e.substring(G.length+1),d=C.slice(f)}}i.loose||(o?i.loose=!0:this.rules.other.doubleBlankLine.test(p)&&(o=!0));let y=null,ee;this.options.gfm&&(y=this.rules.other.listIsTask.exec(u),y&&(e" ++ "e=y[0]!==\"[ ] \",u=u.replace(this.rules.other.listReplaceTask,\"\"))),i.items.push({type:\"list_item\",raw:p,task:!!y,checked:ee,loose:!1,text:u,tokens:[]}),i.raw+=p}let a=i.items.at(-1);if(a)a.raw=a.raw.trimEnd(),a.text=a.text.trimEnd();else return;i.raw=i.raw.trimEnd();for(let c=0;c<i.items.length;c++)if(this.lexer.state.top=!1,i.items[c].tokens=this.lexer.blockTokens(i.items[c].text,[]),!i.loose){let p=i.items[c].tokens.filter(d=>d.type===\"space\"),u=p.length>0&&p.some(d=>this.rules.other.anyLine.test(d.raw));" ++ "i.loose=u}if(i.loose)for(let c=0;c<i.items.length;c++)i.items[c].loose=!0;return i}}html(e){let t=this.rules.block.html.exec(e);if(t)return{type:\"html\",block:!0,raw:t[0],pre:t[1]===\"pre\"||t[1]===\"script\"||t[1]===\"style\",text:t[0]}}def(e){let t=this.rules.block.def.exec(e);if(t){let n=t[1].toLowerCase().replace(this.rules.other.multipleSpaceGlobal,\" \"),s=t[2]?t[2].replace(this.rules.other.hrefBrackets,\"$1\").replace(this.rules.inline.anyPunctuation,\"$1\"):\"\",i=t[3]?t[3].substring(1,t[3].length-1).replace(this." ++ "rules.inline.anyPunctuation,\"$1\"):t[3];return{type:\"def\",tag:n,raw:t[0],href:s,title:i}}}table(e){let t=this.rules.block.table.exec(e);if(!t||!this.rules.other.tableDelimiter.test(t[2]))return;let n=Y(t[1]),s=t[2].replace(this.rules.other.tableAlignChars,\"\").split(\"|\"),i=t[3]?.trim()?t[3].replace(this.rules.other.tableRowBlankLine,\"\").split(`\n`):[],r={type:\"table\",raw:t[0],header:[],align:[],rows:[]};if(n.length===s.length){for(let o of s)this.rules.other.tableAlignRight.test(o)?r.align.push(\"right\"):this.r" ++ "ules.other.tableAlignCenter.test(o)?r.align.push(\"center\"):this.rules.other.tableAlignLeft.test(o)?r.align.push(\"left\"):r.align.push(null);for(let o=0;o<n.length;o++)r.header.push({text:n[o],tokens:this.lexer.inline(n[o]),header:!0,align:r.align[o]});for(let o of i)r.rows.push(Y(o,r.header.length).map((a,c)=>({text:a,tokens:this.lexer.inline(a),header:!1,align:r.align[c]})));return r}}lheading(e){let t=this.rules.block.lheading.exec(e);if(t)return{type:\"heading\",raw:t[0],depth:t[2].charAt(0)===\"=\"?1:2,text:" ++ "t[1],tokens:this.lexer.inline(t[1])}}paragraph(e){let t=this.rules.block.paragraph.exec(e);if(t){let n=t[1].charAt(t[1].length-1)===`\n`?t[1].slice(0,-1):t[1];return{type:\"paragraph\",raw:t[0],text:n,tokens:this.lexer.inline(n)}}}text(e){let t=this.rules.block.text.exec(e);if(t)return{type:\"text\",raw:t[0],text:t[0],tokens:this.lexer.inline(t[0])}}escape(e){let t=this.rules.inline.escape.exec(e);if(t)return{type:\"escape\",raw:t[0],text:t[1]}}tag(e){let t=this.rules.inline.tag.exec(e);if(t)return!this.lexer.stat" ++ "e.inLink&&this.rules.other.startATag.test(t[0])?this.lexer.state.inLink=!0:this.lexer.state.inLink&&this.rules.other.endATag.test(t[0])&&(this.lexer.state.inLink=!1),!this.lexer.state.inRawBlock&&this.rules.other.startPreScriptTag.test(t[0])?this.lexer.state.inRawBlock=!0:this.lexer.state.inRawBlock&&this.rules.other.endPreScriptTag.test(t[0])&&(this.lexer.state.inRawBlock=!1),{type:\"html\",raw:t[0],inLink:this.lexer.state.inLink,inRawBlock:this.lexer.state.inRawBlock,block:!1,text:t[0]}}link(e){let t=this.r" ++ "ules.inline.link.exec(e);if(t){let n=t[2].trim();if(!this.options.pedantic&&this.rules.other.startAngleBracket.test(n)){if(!this.rules.other.endAngleBracket.test(n))return;let r=A(n.slice(0,-1),\"\\\\\");if((n.length-r.length)%2===0)return}else{let r=de(t[2],\"()\");if(r===-2)return;if(r>-1){let a=(t[0].indexOf(\"!\")===0?5:4)+t[1].length+r;t[2]=t[2].substring(0,r),t[0]=t[0].substring(0,a).trim(),t[3]=\"\"}}let s=t[2],i=\"\";if(this.options.pedantic){let r=this.rules.other.pedanticHrefTitle.exec(s);r&&(s=r[1],i=r[3])}e" ++ "lse i=t[3]?t[3].slice(1,-1):\"\";return s=s.trim(),this.rules.other.startAngleBracket.test(s)&&(this.options.pedantic&&!this.rules.other.endAngleBracket.test(n)?s=s.slice(1):s=s.slice(1,-1)),me(t,{href:s&&s.replace(this.rules.inline.anyPunctuation,\"$1\"),title:i&&i.replace(this.rules.inline.anyPunctuation,\"$1\")},t[0],this.lexer,this.rules)}}reflink(e,t){let n;if((n=this.rules.inline.reflink.exec(e))||(n=this.rules.inline.nolink.exec(e))){let s=(n[2]||n[1]).replace(this.rules.other.multipleSpaceGlobal,\" \"),i=t[" ++ "s.toLowerCase()];if(!i){let r=n[0].charAt(0);return{type:\"text\",raw:r,text:r}}return me(n,i,n[0],this.lexer,this.rules)}}emStrong(e,t,n=\"\"){let s=this.rules.inline.emStrongLDelim.exec(e);if(!s||s[3]&&n.match(this.rules.other.unicodeAlphaNumeric))return;if(!(s[1]||s[2]||\"\")||!n||this.rules.inline.punctuation.exec(n)){let r=[...s[0]].length-1,o,a,c=r,p=0,u=s[0][0]===\"*\"?this.rules.inline.emStrongRDelimAst:this.rules.inline.emStrongRDelimUnd;for(u.lastIndex=0,t=t.slice(-1*e.length+r);(s=u.exec(t))!=null;){if(o" ++ "=s[1]||s[2]||s[3]||s[4]||s[5]||s[6],!o)continue;if(a=[...o].length,s[3]||s[4]){c+=a;continue}else if((s[5]||s[6])&&r%3&&!((r+a)%3)){p+=a;continue}if(c-=a,c>0)continue;a=Math.min(a,a+c+p);let d=[...s[0]][0].length,g=e.slice(0,r+s.index+d+a);if(Math.min(r,a)%2){let f=g.slice(1,-1);return{type:\"em\",raw:g,text:f,tokens:this.lexer.inlineTokens(f)}}let T=g.slice(2,-2);return{type:\"strong\",raw:g,text:T,tokens:this.lexer.inlineTokens(T)}}}}codespan(e){let t=this.rules.inline.code.exec(e);if(t){let n=t[2].replace(th" ++ "is.rules.other.newLineCharGlobal,\" \"),s=this.rules.other.nonSpaceChar.test(n),i=this.rules.other.startingSpaceChar.test(n)&&this.rules.other.endingSpaceChar.test(n);return s&&i&&(n=n.substring(1,n.length-1)),{type:\"codespan\",raw:t[0],text:n}}}br(e){let t=this.rules.inline.br.exec(e);if(t)return{type:\"br\",raw:t[0]}}del(e){let t=this.rules.inline.del.exec(e);if(t)return{type:\"del\",raw:t[0],text:t[2],tokens:this.lexer.inlineTokens(t[2])}}autolink(e){let t=this.rules.inline.autolink.exec(e);if(t){let n,s;return" ++ " t[2]===\"@\"?(n=t[1],s=\"mailto:\"+n):(n=t[1],s=n),{type:\"link\",raw:t[0],text:n,href:s,tokens:[{type:\"text\",raw:n,text:n}]}}}url(e){let t;if(t=this.rules.inline.url.exec(e)){let n,s;if(t[2]===\"@\")n=t[0],s=\"mailto:\"+n;else{let i;do i=t[0],t[0]=this.rules.inline._backpedal.exec(t[0])?.[0]??\"\";while(i!==t[0]);n=t[0],t[1]===\"www.\"?s=\"http://\"+t[0]:s=t[0]}return{type:\"link\",raw:t[0],text:n,href:s,tokens:[{type:\"text\",raw:n,text:n}]}}}inlineText(e){let t=this.rules.inline.text.exec(e);if(t){let n=this.lexer.state.in" ++ "RawBlock;return{type:\"text\",raw:t[0],text:t[0],escaped:n}}}};var x=class l{tokens;options;state;tokenizer;inlineQueue;constructor(e){this.tokens=[],this.tokens.links=Object.create(null),this.options=e||w,this.options.tokenizer=this.options.tokenizer||new S,this.tokenizer=this.options.tokenizer,this.tokenizer.options=this.options,this.tokenizer.lexer=this,this.inlineQueue=[],this.state={inLink:!1,inRawBlock:!1,top:!0};let t={other:m,block:B.normal,inline:P.normal};this.options.pedantic?(t.block=B.pedantic,t." ++ "inline=P.pedantic):this.options.gfm&&(t.block=B.gfm,this.options.breaks?t.inline=P.breaks:t.inline=P.gfm),this.tokenizer.rules=t}static get rules(){return{block:B,inline:P}}static lex(e,t){return new l(t).lex(e)}static lexInline(e,t){return new l(t).inlineTokens(e)}lex(e){e=e.replace(m.carriageReturn,`\n`),this.blockTokens(e,this.tokens);for(let t=0;t<this.inlineQueue.length;t++){let n=this.inlineQueue[t];this.inlineTokens(n.src,n.tokens)}return this.inlineQueue=[],this.tokens}blockTokens(e,t=[],n=!1){for(th" ++ "is.options.pedantic&&(e=e.replace(m.tabCharGlobal,\"    \").replace(m.spaceLine,\"\"));e;){let s;if(this.options.extensions?.block?.some(r=>(s=r.call({lexer:this},e,t))?(e=e.substring(s.raw.length),t.push(s),!0):!1))continue;if(s=this.tokenizer.space(e)){e=e.substring(s.raw.length);let r=t.at(-1);s.raw.length===1&&r!==void 0?r.raw+=`\n`:t.push(s);continue}if(s=this.tokenizer.code(e)){e=e.substring(s.raw.length);let r=t.at(-1);r?.type===\"paragraph\"||r?.type===\"text\"?(r.raw+=`\n`+s.raw,r.text+=`\n`+s.text,this.inlin" ++ "eQueue.at(-1).src=r.text):t.push(s);continue}if(s=this.tokenizer.fences(e)){e=e.substring(s.raw.length),t.push(s);continue}if(s=this.tokenizer.heading(e)){e=e.substring(s.raw.length),t.push(s);continue}if(s=this.tokenizer.hr(e)){e=e.substring(s.raw.length),t.push(s);continue}if(s=this.tokenizer.blockquote(e)){e=e.substring(s.raw.length),t.push(s);continue}if(s=this.tokenizer.list(e)){e=e.substring(s.raw.length),t.push(s);continue}if(s=this.tokenizer.html(e)){e=e.substring(s.raw.length),t.push(s);continue}if" ++ "(s=this.tokenizer.def(e)){e=e.substring(s.raw.length);let r=t.at(-1);r?.type===\"paragraph\"||r?.type===\"text\"?(r.raw+=`\n`+s.raw,r.text+=`\n`+s.raw,this.inlineQueue.at(-1).src=r.text):this.tokens.links[s.tag]||(this.tokens.links[s.tag]={href:s.href,title:s.title});continue}if(s=this.tokenizer.table(e)){e=e.substring(s.raw.length),t.push(s);continue}if(s=this.tokenizer.lheading(e)){e=e.substring(s.raw.length),t.push(s);continue}let i=e;if(this.options.extensions?.startBlock){let r=1/0,o=e.slice(1),a;this.option" ++ "s.extensions.startBlock.forEach(c=>{a=c.call({lexer:this},o),typeof a==\"number\"&&a>=0&&(r=Math.min(r,a))}),r<1/0&&r>=0&&(i=e.substring(0,r+1))}if(this.state.top&&(s=this.tokenizer.paragraph(i))){let r=t.at(-1);n&&r?.type===\"paragraph\"?(r.raw+=`\n`+s.raw,r.text+=`\n`+s.text,this.inlineQueue.pop(),this.inlineQueue.at(-1).src=r.text):t.push(s),n=i.length!==e.length,e=e.substring(s.raw.length);continue}if(s=this.tokenizer.text(e)){e=e.substring(s.raw.length);let r=t.at(-1);r?.type===\"text\"?(r.raw+=`\n`+s.raw,r.tex" ++ "t+=`\n`+s.text,this.inlineQueue.pop(),this.inlineQueue.at(-1).src=r.text):t.push(s);continue}if(e){let r=\"Infinite loop on byte: \"+e.charCodeAt(0);if(this.options.silent){console.error(r);break}else throw new Error(r)}}return this.state.top=!0,t}inline(e,t=[]){return this.inlineQueue.push({src:e,tokens:t}),t}inlineTokens(e,t=[]){let n=e,s=null;if(this.tokens.links){let o=Object.keys(this.tokens.links);if(o.length>0)for(;(s=this.tokenizer.rules.inline.reflinkSearch.exec(n))!=null;)o.includes(s[0].slice(s[0].l" ++ "astIndexOf(\"[\")+1,-1))&&(n=n.slice(0,s.index)+\"[\"+\"a\".repeat(s[0].length-2)+\"]\"+n.slice(this.tokenizer.rules.inline.reflinkSearch.lastIndex))}for(;(s=this.tokenizer.rules.inline.anyPunctuation.exec(n))!=null;)n=n.slice(0,s.index)+\"++\"+n.slice(this.tokenizer.rules.inline.anyPunctuation.lastIndex);for(;(s=this.tokenizer.rules.inline.blockSkip.exec(n))!=null;)n=n.slice(0,s.index)+\"[\"+\"a\".repeat(s[0].length-2)+\"]\"+n.slice(this.tokenizer.rules.inline.blockSkip.lastIndex);let i=!1,r=\"\";for(;e;){i||(r=\"\"),i=!1;let" ++ " o;if(this.options.extensions?.inline?.some(c=>(o=c.call({lexer:this},e,t))?(e=e.substring(o.raw.length),t.push(o),!0):!1))continue;if(o=this.tokenizer.escape(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.tag(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.link(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.reflink(e,this.tokens.links)){e=e.substring(o.raw.length);let c=t.at(-1);o.type===\"text\"&&c?.type===\"text\"?(c.raw+=o.raw,c.text+=o." ++ "text):t.push(o);continue}if(o=this.tokenizer.emStrong(e,n,r)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.codespan(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.br(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.del(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.autolink(e)){e=e.substring(o.raw.length),t.push(o);continue}if(!this.state.inLink&&(o=this.tokenizer.url(e))){e=e.substring(o.raw.length),t.push(o);contin" ++ "ue}let a=e;if(this.options.extensions?.startInline){let c=1/0,p=e.slice(1),u;this.options.extensions.startInline.forEach(d=>{u=d.call({lexer:this},p),typeof u==\"number\"&&u>=0&&(c=Math.min(c,u))}),c<1/0&&c>=0&&(a=e.substring(0,c+1))}if(o=this.tokenizer.inlineText(a)){e=e.substring(o.raw.length),o.raw.slice(-1)!==\"_\"&&(r=o.raw.slice(-1)),i=!0;let c=t.at(-1);c?.type===\"text\"?(c.raw+=o.raw,c.text+=o.text):t.push(o);continue}if(e){let c=\"Infinite loop on byte: \"+e.charCodeAt(0);if(this.options.silent){console.er" ++ "ror(c);break}else throw new Error(c)}}return t}};var $=class{options;parser;constructor(e){this.options=e||w}space(e){return\"\"}code({text:e,lang:t,escaped:n}){let s=(t||\"\").match(m.notSpaceStart)?.[0],i=e.replace(m.endingNewline,\"\")+`\n`;return s?'<pre><code class=\"language-'+R(s)+'\">'+(n?i:R(i,!0))+`</code></pre>\n`:\"<pre><code>\"+(n?i:R(i,!0))+`</code></pre>\n`}blockquote({tokens:e}){return`<blockquote>\n${this.parser.parse(e)}</blockquote>\n`}html({text:e}){return e}heading({tokens:e,depth:t}){return`<h${t}>${" ++ "this.parser.parseInline(e)}</h${t}>\n`}hr(e){return`<hr>\n`}list(e){let t=e.ordered,n=e.start,s=\"\";for(let o=0;o<e.items.length;o++){let a=e.items[o];s+=this.listitem(a)}let i=t?\"ol\":\"ul\",r=t&&n!==1?' start=\"'+n+'\"':\"\";return\"<\"+i+r+`>\n`+s+\"</\"+i+`>\n`}listitem(e){let t=\"\";if(e.task){let n=this.checkbox({checked:!!e.checked});e.loose?e.tokens[0]?.type===\"paragraph\"?(e.tokens[0].text=n+\" \"+e.tokens[0].text,e.tokens[0].tokens&&e.tokens[0].tokens.length>0&&e.tokens[0].tokens[0].type===\"text\"&&(e.tokens[0].tokens[" ++ "0].text=n+\" \"+R(e.tokens[0].tokens[0].text),e.tokens[0].tokens[0].escaped=!0)):e.tokens.unshift({type:\"text\",raw:n+\" \",text:n+\" \",escaped:!0}):t+=n+\" \"}return t+=this.parser.parse(e.tokens,!!e.loose),`<li>${t}</li>\n`}checkbox({checked:e}){return\"<input \"+(e?'checked=\"\" ':\"\")+'disabled=\"\" type=\"checkbox\">'}paragraph({tokens:e}){return`<p>${this.parser.parseInline(e)}</p>\n`}table(e){let t=\"\",n=\"\";for(let i=0;i<e.header.length;i++)n+=this.tablecell(e.header[i]);t+=this.tablerow({text:n});let s=\"\";for(let i=0;i" ++ "<e.rows.length;i++){let r=e.rows[i];n=\"\";for(let o=0;o<r.length;o++)n+=this.tablecell(r[o]);s+=this.tablerow({text:n})}return s&&(s=`<tbody>${s}</tbody>`),`<table>\n<thead>\n`+t+`</thead>\n`+s+`</table>\n`}tablerow({text:e}){return`<tr>\n${e}</tr>\n`}tablecell(e){let t=this.parser.parseInline(e.tokens),n=e.header?\"th\":\"td\";return(e.align?`<${n} align=\"${e.align}\">`:`<${n}>`)+t+`</${n}>\n`}strong({tokens:e}){return`<strong>${this.parser.parseInline(e)}</strong>`}em({tokens:e}){return`<em>${this.parser.parseInline(e" ++ ")}</em>`}codespan({text:e}){return`<code>${R(e,!0)}</code>`}br(e){return\"<br>\"}del({tokens:e}){return`<del>${this.parser.parseInline(e)}</del>`}link({href:e,title:t,tokens:n}){let s=this.parser.parseInline(n),i=V(e);if(i===null)return s;e=i;let r='<a href=\"'+e+'\"';return t&&(r+=' title=\"'+R(t)+'\"'),r+=\">\"+s+\"</a>\",r}image({href:e,title:t,text:n,tokens:s}){s&&(n=this.parser.parseInline(s,this.parser.textRenderer));let i=V(e);if(i===null)return R(n);e=i;let r=`<img src=\"${e}\" alt=\"${n}\"`;return t&&(r+=` title" ++ "=\"${R(t)}\"`),r+=\">\",r}text(e){return\"tokens\"in e&&e.tokens?this.parser.parseInline(e.tokens):\"escaped\"in e&&e.escaped?e.text:R(e.text)}};var _=class{strong({text:e}){return e}em({text:e}){return e}codespan({text:e}){return e}del({text:e}){return e}html({text:e}){return e}text({text:e}){return e}link({text:e}){return\"\"+e}image({text:e}){return\"\"+e}br(){return\"\"}};var b=class l{options;renderer;textRenderer;constructor(e){this.options=e||w,this.options.renderer=this.options.renderer||new $,this.renderer=this." ++ "options.renderer,this.renderer.options=this.options,this.renderer.parser=this,this.textRenderer=new _}static parse(e,t){return new l(t).parse(e)}static parseInline(e,t){return new l(t).parseInline(e)}parse(e,t=!0){let n=\"\";for(let s=0;s<e.length;s++){let i=e[s];if(this.options.extensions?.renderers?.[i.type]){let o=i,a=this.options.extensions.renderers[o.type].call({parser:this},o);if(a!==!1||![\"space\",\"hr\",\"heading\",\"code\",\"table\",\"blockquote\",\"list\",\"html\",\"paragraph\",\"text\"].includes(o.type)){n+=a||\"\";co" ++ "ntinue}}let r=i;switch(r.type){case\"space\":{n+=this.renderer.space(r);continue}case\"hr\":{n+=this.renderer.hr(r);continue}case\"heading\":{n+=this.renderer.heading(r);continue}case\"code\":{n+=this.renderer.code(r);continue}case\"table\":{n+=this.renderer.table(r);continue}case\"blockquote\":{n+=this.renderer.blockquote(r);continue}case\"list\":{n+=this.renderer.list(r);continue}case\"html\":{n+=this.renderer.html(r);continue}case\"paragraph\":{n+=this.renderer.paragraph(r);continue}case\"text\":{let o=r,a=this.renderer.tex" ++ "t(o);for(;s+1<e.length&&e[s+1].type===\"text\";)o=e[++s],a+=`\n`+this.renderer.text(o);t?n+=this.renderer.paragraph({type:\"paragraph\",raw:a,text:a,tokens:[{type:\"text\",raw:a,text:a,escaped:!0}]}):n+=a;continue}default:{let o='Token with \"'+r.type+'\" type was not found.';if(this.options.silent)return console.error(o),\"\";throw new Error(o)}}}return n}parseInline(e,t=this.renderer){let n=\"\";for(let s=0;s<e.length;s++){let i=e[s];if(this.options.extensions?.renderers?.[i.type]){let o=this.options.extensions.render" ++ "ers[i.type].call({parser:this},i);if(o!==!1||![\"escape\",\"html\",\"link\",\"image\",\"strong\",\"em\",\"codespan\",\"br\",\"del\",\"text\"].includes(i.type)){n+=o||\"\";continue}}let r=i;switch(r.type){case\"escape\":{n+=t.text(r);break}case\"html\":{n+=t.html(r);break}case\"link\":{n+=t.link(r);break}case\"image\":{n+=t.image(r);break}case\"strong\":{n+=t.strong(r);break}case\"em\":{n+=t.em(r);break}case\"codespan\":{n+=t.codespan(r);break}case\"br\":{n+=t.br(r);break}case\"del\":{n+=t.del(r);break}case\"text\":{n+=t.text(r);break}default:{let o" ++ "='Token with \"'+r.type+'\" type was not found.';if(this.options.silent)return console.error(o),\"\";throw new Error(o)}}}return n}};var L=class{options;block;constructor(e){this.options=e||w}static passThroughHooks=new Set([\"preprocess\",\"postprocess\",\"processAllTokens\"]);preprocess(e){return e}postprocess(e){return e}processAllTokens(e){return e}provideLexer(){return this.block?x.lex:x.lexInline}provideParser(){return this.block?b.parse:b.parseInline}};var E=class{defaults=z();options=this.setOptions;parse=thi" ++ "s.parseMarkdown(!0);parseInline=this.parseMarkdown(!1);Parser=b;Renderer=$;TextRenderer=_;Lexer=x;Tokenizer=S;Hooks=L;constructor(...e){this.use(...e)}walkTokens(e,t){let n=[];for(let s of e)switch(n=n.concat(t.call(this,s)),s.type){case\"table\":{let i=s;for(let r of i.header)n=n.concat(this.walkTokens(r.tokens,t));for(let r of i.rows)for(let o of r)n=n.concat(this.walkTokens(o.tokens,t));break}case\"list\":{let i=s;n=n.concat(this.walkTokens(i.items,t));break}default:{let i=s;this.defaults.extensions?.childTo" ++ "kens?.[i.type]?this.defaults.extensions.childTokens[i.type].forEach(r=>{let o=i[r].flat(1/0);n=n.concat(this.walkTokens(o,t))}):i.tokens&&(n=n.concat(this.walkTokens(i.tokens,t)))}}return n}use(...e){let t=this.defaults.extensions||{renderers:{},childTokens:{}};return e.forEach(n=>{let s={...n};if(s.async=this.defaults.async||s.async||!1,n.extensions&&(n.extensions.forEach(i=>{if(!i.name)throw new Error(\"extension name required\");if(\"renderer\"in i){let r=t.renderers[i.name];r?t.renderers[i.name]=function(.." ++ ".o){let a=i.renderer.apply(this,o);return a===!1&&(a=r.apply(this,o)),a}:t.renderers[i.name]=i.renderer}if(\"tokenizer\"in i){if(!i.level||i.level!==\"block\"&&i.level!==\"inline\")throw new Error(\"extension level must be 'block' or 'inline'\");let r=t[i.level];r?r.unshift(i.tokenizer):t[i.level]=[i.tokenizer],i.start&&(i.level===\"block\"?t.startBlock?t.startBlock.push(i.start):t.startBlock=[i.start]:i.level===\"inline\"&&(t.startInline?t.startInline.push(i.start):t.startInline=[i.start]))}\"childTokens\"in i&&i.childT" ++ "okens&&(t.childTokens[i.name]=i.childTokens)}),s.extensions=t),n.renderer){let i=this.defaults.renderer||new $(this.defaults);for(let r in n.renderer){if(!(r in i))throw new Error(`renderer '${r}' does not exist`);if([\"options\",\"parser\"].includes(r))continue;let o=r,a=n.renderer[o],c=i[o];i[o]=(...p)=>{let u=a.apply(i,p);return u===!1&&(u=c.apply(i,p)),u||\"\"}}s.renderer=i}if(n.tokenizer){let i=this.defaults.tokenizer||new S(this.defaults);for(let r in n.tokenizer){if(!(r in i))throw new Error(`tokenizer '${" ++ "r}' does not exist`);if([\"options\",\"rules\",\"lexer\"].includes(r))continue;let o=r,a=n.tokenizer[o],c=i[o];i[o]=(...p)=>{let u=a.apply(i,p);return u===!1&&(u=c.apply(i,p)),u}}s.tokenizer=i}if(n.hooks){let i=this.defaults.hooks||new L;for(let r in n.hooks){if(!(r in i))throw new Error(`hook '${r}' does not exist`);if([\"options\",\"block\"].includes(r))continue;let o=r,a=n.hooks[o],c=i[o];L.passThroughHooks.has(r)?i[o]=p=>{if(this.defaults.async)return Promise.resolve(a.call(i,p)).then(d=>c.call(i,d));let u=a.call" ++ "(i,p);return c.call(i,u)}:i[o]=(...p)=>{let u=a.apply(i,p);return u===!1&&(u=c.apply(i,p)),u}}s.hooks=i}if(n.walkTokens){let i=this.defaults.walkTokens,r=n.walkTokens;s.walkTokens=function(o){let a=[];return a.push(r.call(this,o)),i&&(a=a.concat(i.call(this,o))),a}}this.defaults={...this.defaults,...s}}),this}setOptions(e){return this.defaults={...this.defaults,...e},this}lexer(e,t){return x.lex(e,t??this.defaults)}parser(e,t){return b.parse(e,t??this.defaults)}parseMarkdown(e){return(n,s)=>{let i={...s},r=" ++ "{...this.defaults,...i},o=this.onError(!!r.silent,!!r.async);if(this.defaults.async===!0&&i.async===!1)return o(new Error(\"marked(): The async option was set to true by an extension. Remove async: false from the parse options object to return a Promise.\"));if(typeof n>\"u\"||n===null)return o(new Error(\"marked(): input parameter is undefined or null\"));if(typeof n!=\"string\")return o(new Error(\"marked(): input parameter is of type \"+Object.prototype.toString.call(n)+\", string expected\"));r.hooks&&(r.hooks.opti" ++ "ons=r,r.hooks.block=e);let a=r.hooks?r.hooks.provideLexer():e?x.lex:x.lexInline,c=r.hooks?r.hooks.provideParser():e?b.parse:b.parseInline;if(r.async)return Promise.resolve(r.hooks?r.hooks.preprocess(n):n).then(p=>a(p,r)).then(p=>r.hooks?r.hooks.processAllTokens(p):p).then(p=>r.walkTokens?Promise.all(this.walkTokens(p,r.walkTokens)).then(()=>p):p).then(p=>c(p,r)).then(p=>r.hooks?r.hooks.postprocess(p):p).catch(o);try{r.hooks&&(n=r.hooks.preprocess(n));let p=a(n,r);r.hooks&&(p=r.hooks.processAllTokens(p)),r.w" ++ "alkTokens&&this.walkTokens(p,r.walkTokens);let u=c(p,r);return r.hooks&&(u=r.hooks.postprocess(u)),u}catch(p){return o(p)}}}onError(e,t){return n=>{if(n.message+=`\nPlease report this to https://github.com/markedjs/marked.`,e){let s=\"<p>An error occurred:</p><pre>\"+R(n.message+\"\",!0)+\"</pre>\";return t?Promise.resolve(s):s}if(t)return Promise.reject(n);throw n}}};var M=new E;function k(l,e){return M.parse(l,e)}k.options=k.setOptions=function(l){return M.setOptions(l),k.defaults=M.defaults,N(k.defaults),k};k.g" ++ "etDefaults=z;k.defaults=w;k.use=function(...l){return M.use(...l),k.defaults=M.defaults,N(k.defaults),k};k.walkTokens=function(l,e){return M.walkTokens(l,e)};k.parseInline=M.parseInline;k.Parser=b;k.parser=b.parse;k.Renderer=$;k.TextRenderer=_;k.Lexer=x;k.lexer=x.lex;k.Tokenizer=S;k.Hooks=L;k.parse=k;var it=k.options,ot=k.setOptions,lt=k.use,at=k.walkTokens,ct=k.parseInline,pt=k,ut=b.parse,ht=x.lex;\n\nif(__exports != exports)module.exports = exports;return module.exports}));\n";
const dompurify_js: []const u8 =
    "/*! @license DOMPurify 3.2.6 | (c) Cure53 and other contributors | Released under the Apache license 2.0 and Mozilla Public License 2.0 | github.com/cure53/DOMPurify/blob/3.2.6/LICENSE */\n!function(e,t){\"object\"==typeof exports&&\"undefined\"!=typeof module?module.exports=t():\"function\"==typeof define&&define.amd?define(t):(e=\"undefined\"!=typeof globalThis?globalThis:e||self).DOMPurify=t()}(this,(function(){\"use strict\";const{entries:e,setPrototypeOf:t,isFrozen:n,getPrototypeOf:o,getOwnPropertyDescriptor:r}=O" ++ "bject;let{freeze:i,seal:a,create:l}=Object,{apply:c,construct:s}=\"undefined\"!=typeof Reflect&&Reflect;i||(i=function(e){return e}),a||(a=function(e){return e}),c||(c=function(e,t,n){return e.apply(t,n)}),s||(s=function(e,t){return new e(...t)});const u=R(Array.prototype.forEach),m=R(Array.prototype.lastIndexOf),p=R(Array.prototype.pop),f=R(Array.prototype.push),d=R(Array.prototype.splice),h=R(String.prototype.toLowerCase),g=R(String.prototype.toString),T=R(String.prototype.match),y=R(String.prototype.replac" ++ "e),E=R(String.prototype.indexOf),A=R(String.prototype.trim),_=R(Object.prototype.hasOwnProperty),S=R(RegExp.prototype.test),b=(N=TypeError,function(){for(var e=arguments.length,t=new Array(e),n=0;n<e;n++)t[n]=arguments[n];return s(N,t)});var N;function R(e){return function(t){t instanceof RegExp&&(t.lastIndex=0);for(var n=arguments.length,o=new Array(n>1?n-1:0),r=1;r<n;r++)o[r-1]=arguments[r];return c(e,t,o)}}function w(e,o){let r=arguments.length>2&&void 0!==arguments[2]?arguments[2]:h;t&&t(e,null);let i=o" ++ ".length;for(;i--;){let t=o[i];if(\"string\"==typeof t){const e=r(t);e!==t&&(n(o)||(o[i]=e),t=e)}e[t]=!0}return e}function O(e){for(let t=0;t<e.length;t++){_(e,t)||(e[t]=null)}return e}function D(t){const n=l(null);for(const[o,r]of e(t)){_(t,o)&&(Array.isArray(r)?n[o]=O(r):r&&\"object\"==typeof r&&r.constructor===Object?n[o]=D(r):n[o]=r)}return n}function v(e,t){for(;null!==e;){const n=r(e,t);if(n){if(n.get)return R(n.get);if(\"function\"==typeof n.value)return R(n.value)}e=o(e)}return function(){return null}}cons" ++ "t L=i([\"a\",\"abbr\",\"acronym\",\"address\",\"area\",\"article\",\"aside\",\"audio\",\"b\",\"bdi\",\"bdo\",\"big\",\"blink\",\"blockquote\",\"body\",\"br\",\"button\",\"canvas\",\"caption\",\"center\",\"cite\",\"code\",\"col\",\"colgroup\",\"content\",\"data\",\"datalist\",\"dd\",\"decorator\",\"del\",\"details\",\"dfn\",\"dialog\",\"dir\",\"div\",\"dl\",\"dt\",\"element\",\"em\",\"fieldset\",\"figcaption\",\"figure\",\"font\",\"footer\",\"form\",\"h1\",\"h2\",\"h3\",\"h4\",\"h5\",\"h6\",\"head\",\"header\",\"hgroup\",\"hr\",\"html\",\"i\",\"img\",\"input\",\"ins\",\"kbd\",\"label\",\"legend\",\"li\",\"main\",\"map\",\"mark\",\"marquee\"," ++ "\"menu\",\"menuitem\",\"meter\",\"nav\",\"nobr\",\"ol\",\"optgroup\",\"option\",\"output\",\"p\",\"picture\",\"pre\",\"progress\",\"q\",\"rp\",\"rt\",\"ruby\",\"s\",\"samp\",\"section\",\"select\",\"shadow\",\"small\",\"source\",\"spacer\",\"span\",\"strike\",\"strong\",\"style\",\"sub\",\"summary\",\"sup\",\"table\",\"tbody\",\"td\",\"template\",\"textarea\",\"tfoot\",\"th\",\"thead\",\"time\",\"tr\",\"track\",\"tt\",\"u\",\"ul\",\"var\",\"video\",\"wbr\"]),C=i([\"svg\",\"a\",\"altglyph\",\"altglyphdef\",\"altglyphitem\",\"animatecolor\",\"animatemotion\",\"animatetransform\",\"circle\",\"clippath\",\"defs\",\"desc\",\"ellipse" ++ "\",\"filter\",\"font\",\"g\",\"glyph\",\"glyphref\",\"hkern\",\"image\",\"line\",\"lineargradient\",\"marker\",\"mask\",\"metadata\",\"mpath\",\"path\",\"pattern\",\"polygon\",\"polyline\",\"radialgradient\",\"rect\",\"stop\",\"style\",\"switch\",\"symbol\",\"text\",\"textpath\",\"title\",\"tref\",\"tspan\",\"view\",\"vkern\"]),x=i([\"feBlend\",\"feColorMatrix\",\"feComponentTransfer\",\"feComposite\",\"feConvolveMatrix\",\"feDiffuseLighting\",\"feDisplacementMap\",\"feDistantLight\",\"feDropShadow\",\"feFlood\",\"feFuncA\",\"feFuncB\",\"feFuncG\",\"feFuncR\",\"feGaussianBlur\",\"feImage\",\"feMerge" ++ "\",\"feMergeNode\",\"feMorphology\",\"feOffset\",\"fePointLight\",\"feSpecularLighting\",\"feSpotLight\",\"feTile\",\"feTurbulence\"]),I=i([\"animate\",\"color-profile\",\"cursor\",\"discard\",\"font-face\",\"font-face-format\",\"font-face-name\",\"font-face-src\",\"font-face-uri\",\"foreignobject\",\"hatch\",\"hatchpath\",\"mesh\",\"meshgradient\",\"meshpatch\",\"meshrow\",\"missing-glyph\",\"script\",\"set\",\"solidcolor\",\"unknown\",\"use\"]),M=i([\"math\",\"menclose\",\"merror\",\"mfenced\",\"mfrac\",\"mglyph\",\"mi\",\"mlabeledtr\",\"mmultiscripts\",\"mn\",\"mo\",\"mover\",\"mpadded\",\"" ++ "mphantom\",\"mroot\",\"mrow\",\"ms\",\"mspace\",\"msqrt\",\"mstyle\",\"msub\",\"msup\",\"msubsup\",\"mtable\",\"mtd\",\"mtext\",\"mtr\",\"munder\",\"munderover\",\"mprescripts\"]),k=i([\"maction\",\"maligngroup\",\"malignmark\",\"mlongdiv\",\"mscarries\",\"mscarry\",\"msgroup\",\"mstack\",\"msline\",\"msrow\",\"semantics\",\"annotation\",\"annotation-xml\",\"mprescripts\",\"none\"]),U=i([\"#text\"]),z=i([\"accept\",\"action\",\"align\",\"alt\",\"autocapitalize\",\"autocomplete\",\"autopictureinpicture\",\"autoplay\",\"background\",\"bgcolor\",\"border\",\"capture\",\"cellpadding\",\"cellspacing\",\"" ++ "checked\",\"cite\",\"class\",\"clear\",\"color\",\"cols\",\"colspan\",\"controls\",\"controlslist\",\"coords\",\"crossorigin\",\"datetime\",\"decoding\",\"default\",\"dir\",\"disabled\",\"disablepictureinpicture\",\"disableremoteplayback\",\"download\",\"draggable\",\"enctype\",\"enterkeyhint\",\"face\",\"for\",\"headers\",\"height\",\"hidden\",\"high\",\"href\",\"hreflang\",\"id\",\"inputmode\",\"integrity\",\"ismap\",\"kind\",\"label\",\"lang\",\"list\",\"loading\",\"loop\",\"low\",\"max\",\"maxlength\",\"media\",\"method\",\"min\",\"minlength\",\"multiple\",\"muted\",\"name\",\"nonce\",\"noshade\",\"novali" ++ "date\",\"nowrap\",\"open\",\"optimum\",\"pattern\",\"placeholder\",\"playsinline\",\"popover\",\"popovertarget\",\"popovertargetaction\",\"poster\",\"preload\",\"pubdate\",\"radiogroup\",\"readonly\",\"rel\",\"required\",\"rev\",\"reversed\",\"role\",\"rows\",\"rowspan\",\"spellcheck\",\"scope\",\"selected\",\"shape\",\"size\",\"sizes\",\"span\",\"srclang\",\"start\",\"src\",\"srcset\",\"step\",\"style\",\"summary\",\"tabindex\",\"title\",\"translate\",\"type\",\"usemap\",\"valign\",\"value\",\"width\",\"wrap\",\"xmlns\",\"slot\"]),P=i([\"accent-height\",\"accumulate\",\"additive\",\"alignment-baseline\",\"" ++ "amplitude\",\"ascent\",\"attributename\",\"attributetype\",\"azimuth\",\"basefrequency\",\"baseline-shift\",\"begin\",\"bias\",\"by\",\"class\",\"clip\",\"clippathunits\",\"clip-path\",\"clip-rule\",\"color\",\"color-interpolation\",\"color-interpolation-filters\",\"color-profile\",\"color-rendering\",\"cx\",\"cy\",\"d\",\"dx\",\"dy\",\"diffuseconstant\",\"direction\",\"display\",\"divisor\",\"dur\",\"edgemode\",\"elevation\",\"end\",\"exponent\",\"fill\",\"fill-opacity\",\"fill-rule\",\"filter\",\"filterunits\",\"flood-color\",\"flood-opacity\",\"font-family\",\"font-size\",\"font-size-adju" ++ "st\",\"font-stretch\",\"font-style\",\"font-variant\",\"font-weight\",\"fx\",\"fy\",\"g1\",\"g2\",\"glyph-name\",\"glyphref\",\"gradientunits\",\"gradienttransform\",\"height\",\"href\",\"id\",\"image-rendering\",\"in\",\"in2\",\"intercept\",\"k\",\"k1\",\"k2\",\"k3\",\"k4\",\"kerning\",\"keypoints\",\"keysplines\",\"keytimes\",\"lang\",\"lengthadjust\",\"letter-spacing\",\"kernelmatrix\",\"kernelunitlength\",\"lighting-color\",\"local\",\"marker-end\",\"marker-mid\",\"marker-start\",\"markerheight\",\"markerunits\",\"markerwidth\",\"maskcontentunits\",\"maskunits\",\"max\",\"mask\",\"media\",\"meth" ++ "od\",\"mode\",\"min\",\"name\",\"numoctaves\",\"offset\",\"operator\",\"opacity\",\"order\",\"orient\",\"orientation\",\"origin\",\"overflow\",\"paint-order\",\"path\",\"pathlength\",\"patterncontentunits\",\"patterntransform\",\"patternunits\",\"points\",\"preservealpha\",\"preserveaspectratio\",\"primitiveunits\",\"r\",\"rx\",\"ry\",\"radius\",\"refx\",\"refy\",\"repeatcount\",\"repeatdur\",\"restart\",\"result\",\"rotate\",\"scale\",\"seed\",\"shape-rendering\",\"slope\",\"specularconstant\",\"specularexponent\",\"spreadmethod\",\"startoffset\",\"stddeviation\",\"stitchtiles\",\"stop-color\"" ++ ",\"stop-opacity\",\"stroke-dasharray\",\"stroke-dashoffset\",\"stroke-linecap\",\"stroke-linejoin\",\"stroke-miterlimit\",\"stroke-opacity\",\"stroke\",\"stroke-width\",\"style\",\"surfacescale\",\"systemlanguage\",\"tabindex\",\"tablevalues\",\"targetx\",\"targety\",\"transform\",\"transform-origin\",\"text-anchor\",\"text-decoration\",\"text-rendering\",\"textlength\",\"type\",\"u1\",\"u2\",\"unicode\",\"values\",\"viewbox\",\"visibility\",\"version\",\"vert-adv-y\",\"vert-origin-x\",\"vert-origin-y\",\"width\",\"word-spacing\",\"wrap\",\"writing-mode\",\"xchannelselector\",\"ycha" ++ "nnelselector\",\"x\",\"x1\",\"x2\",\"xmlns\",\"y\",\"y1\",\"y2\",\"z\",\"zoomandpan\"]),H=i([\"accent\",\"accentunder\",\"align\",\"bevelled\",\"close\",\"columnsalign\",\"columnlines\",\"columnspan\",\"denomalign\",\"depth\",\"dir\",\"display\",\"displaystyle\",\"encoding\",\"fence\",\"frame\",\"height\",\"href\",\"id\",\"largeop\",\"length\",\"linethickness\",\"lspace\",\"lquote\",\"mathbackground\",\"mathcolor\",\"mathsize\",\"mathvariant\",\"maxsize\",\"minsize\",\"movablelimits\",\"notation\",\"numalign\",\"open\",\"rowalign\",\"rowlines\",\"rowspacing\",\"rowspan\",\"rspace\",\"rquote\",\"scriptleve" ++ "l\",\"scriptminsize\",\"scriptsizemultiplier\",\"selection\",\"separator\",\"separators\",\"stretchy\",\"subscriptshift\",\"supscriptshift\",\"symmetric\",\"voffset\",\"width\",\"xmlns\"]),F=i([\"xlink:href\",\"xml:id\",\"xlink:title\",\"xml:space\",\"xmlns:xlink\"]),B=a(/\\{\\{[\\w\\W]*|[\\w\\W]*\\}\\}/gm),W=a(/<%[\\w\\W]*|[\\w\\W]*%>/gm),G=a(/\\$\\{[\\w\\W]*/gm),Y=a(/^data-[\\-\\w.\\u00B7-\\uFFFF]+$/),j=a(/^aria-[\\-\\w]+$/),X=a(/^(?:(?:(?:f|ht)tps?|mailto|tel|callto|sms|cid|xmpp|matrix):|[^a-z]|[a-z+.\\-]+(?:[^a-z+.\\-:]|$))/i),q=a(/^(?:\\w+script|data):/i),$=a(/" ++ "[\\u0000-\\u0020\\u00A0\\u1680\\u180E\\u2000-\\u2029\\u205F\\u3000]/g),K=a(/^html$/i),V=a(/^[a-z][.\\w]*(-[.\\w]+)+$/i);var Z=Object.freeze({__proto__:null,ARIA_ATTR:j,ATTR_WHITESPACE:$,CUSTOM_ELEMENT:V,DATA_ATTR:Y,DOCTYPE_NAME:K,ERB_EXPR:W,IS_ALLOWED_URI:X,IS_SCRIPT_OR_DATA:q,MUSTACHE_EXPR:B,TMPLIT_EXPR:G});const J=1,Q=3,ee=7,te=8,ne=9,oe=function(){return\"undefined\"==typeof window?null:window};var re=function t(){let n=arguments.length>0&&void 0!==arguments[0]?arguments[0]:oe();const o=e=>t(e);if(o.version=\"3.2.6\",o" ++ ".removed=[],!n||!n.document||n.document.nodeType!==ne||!n.Element)return o.isSupported=!1,o;let{document:r}=n;const a=r,c=a.currentScript,{DocumentFragment:s,HTMLTemplateElement:N,Node:R,Element:O,NodeFilter:B,NamedNodeMap:W=n.NamedNodeMap||n.MozNamedAttrMap,HTMLFormElement:G,DOMParser:Y,trustedTypes:j}=n,q=O.prototype,$=v(q,\"cloneNode\"),V=v(q,\"remove\"),re=v(q,\"nextSibling\"),ie=v(q,\"childNodes\"),ae=v(q,\"parentNode\");if(\"function\"==typeof N){const e=r.createElement(\"template\");e.content&&e.content.ownerDocum" ++ "ent&&(r=e.content.ownerDocument)}let le,ce=\"\";const{implementation:se,createNodeIterator:ue,createDocumentFragment:me,getElementsByTagName:pe}=r,{importNode:fe}=a;let de={afterSanitizeAttributes:[],afterSanitizeElements:[],afterSanitizeShadowDOM:[],beforeSanitizeAttributes:[],beforeSanitizeElements:[],beforeSanitizeShadowDOM:[],uponSanitizeAttribute:[],uponSanitizeElement:[],uponSanitizeShadowNode:[]};o.isSupported=\"function\"==typeof e&&\"function\"==typeof ae&&se&&void 0!==se.createHTMLDocument;const{MUSTACH" ++ "E_EXPR:he,ERB_EXPR:ge,TMPLIT_EXPR:Te,DATA_ATTR:ye,ARIA_ATTR:Ee,IS_SCRIPT_OR_DATA:Ae,ATTR_WHITESPACE:_e,CUSTOM_ELEMENT:Se}=Z;let{IS_ALLOWED_URI:be}=Z,Ne=null;const Re=w({},[...L,...C,...x,...M,...U]);let we=null;const Oe=w({},[...z,...P,...H,...F]);let De=Object.seal(l(null,{tagNameCheck:{writable:!0,configurable:!1,enumerable:!0,value:null},attributeNameCheck:{writable:!0,configurable:!1,enumerable:!0,value:null},allowCustomizedBuiltInElements:{writable:!0,configurable:!1,enumerable:!0,value:!1}})),ve=null," ++ "Le=null,Ce=!0,xe=!0,Ie=!1,Me=!0,ke=!1,Ue=!0,ze=!1,Pe=!1,He=!1,Fe=!1,Be=!1,We=!1,Ge=!0,Ye=!1,je=!0,Xe=!1,qe={},$e=null;const Ke=w({},[\"annotation-xml\",\"audio\",\"colgroup\",\"desc\",\"foreignobject\",\"head\",\"iframe\",\"math\",\"mi\",\"mn\",\"mo\",\"ms\",\"mtext\",\"noembed\",\"noframes\",\"noscript\",\"plaintext\",\"script\",\"style\",\"svg\",\"template\",\"thead\",\"title\",\"video\",\"xmp\"]);let Ve=null;const Ze=w({},[\"audio\",\"video\",\"img\",\"source\",\"image\",\"track\"]);let Je=null;const Qe=w({},[\"alt\",\"class\",\"for\",\"id\",\"label\",\"name\",\"pattern\",\"place" ++ "holder\",\"role\",\"summary\",\"title\",\"value\",\"style\",\"xmlns\"]),et=\"http://www.w3.org/1998/Math/MathML\",tt=\"http://www.w3.org/2000/svg\",nt=\"http://www.w3.org/1999/xhtml\";let ot=nt,rt=!1,it=null;const at=w({},[et,tt,nt],g);let lt=w({},[\"mi\",\"mo\",\"mn\",\"ms\",\"mtext\"]),ct=w({},[\"annotation-xml\"]);const st=w({},[\"title\",\"style\",\"font\",\"a\",\"script\"]);let ut=null;const mt=[\"application/xhtml+xml\",\"text/html\"];let pt=null,ft=null;const dt=r.createElement(\"form\"),ht=function(e){return e instanceof RegExp||e instanceof Fun" ++ "ction},gt=function(){let e=arguments.length>0&&void 0!==arguments[0]?arguments[0]:{};if(!ft||ft!==e){if(e&&\"object\"==typeof e||(e={}),e=D(e),ut=-1===mt.indexOf(e.PARSER_MEDIA_TYPE)?\"text/html\":e.PARSER_MEDIA_TYPE,pt=\"application/xhtml+xml\"===ut?g:h,Ne=_(e,\"ALLOWED_TAGS\")?w({},e.ALLOWED_TAGS,pt):Re,we=_(e,\"ALLOWED_ATTR\")?w({},e.ALLOWED_ATTR,pt):Oe,it=_(e,\"ALLOWED_NAMESPACES\")?w({},e.ALLOWED_NAMESPACES,g):at,Je=_(e,\"ADD_URI_SAFE_ATTR\")?w(D(Qe),e.ADD_URI_SAFE_ATTR,pt):Qe,Ve=_(e,\"ADD_DATA_URI_TAGS\")?w(D(Ze),e.A" ++ "DD_DATA_URI_TAGS,pt):Ze,$e=_(e,\"FORBID_CONTENTS\")?w({},e.FORBID_CONTENTS,pt):Ke,ve=_(e,\"FORBID_TAGS\")?w({},e.FORBID_TAGS,pt):D({}),Le=_(e,\"FORBID_ATTR\")?w({},e.FORBID_ATTR,pt):D({}),qe=!!_(e,\"USE_PROFILES\")&&e.USE_PROFILES,Ce=!1!==e.ALLOW_ARIA_ATTR,xe=!1!==e.ALLOW_DATA_ATTR,Ie=e.ALLOW_UNKNOWN_PROTOCOLS||!1,Me=!1!==e.ALLOW_SELF_CLOSE_IN_ATTR,ke=e.SAFE_FOR_TEMPLATES||!1,Ue=!1!==e.SAFE_FOR_XML,ze=e.WHOLE_DOCUMENT||!1,Fe=e.RETURN_DOM||!1,Be=e.RETURN_DOM_FRAGMENT||!1,We=e.RETURN_TRUSTED_TYPE||!1,He=e.FORCE_BODY|" ++ "|!1,Ge=!1!==e.SANITIZE_DOM,Ye=e.SANITIZE_NAMED_PROPS||!1,je=!1!==e.KEEP_CONTENT,Xe=e.IN_PLACE||!1,be=e.ALLOWED_URI_REGEXP||X,ot=e.NAMESPACE||nt,lt=e.MATHML_TEXT_INTEGRATION_POINTS||lt,ct=e.HTML_INTEGRATION_POINTS||ct,De=e.CUSTOM_ELEMENT_HANDLING||{},e.CUSTOM_ELEMENT_HANDLING&&ht(e.CUSTOM_ELEMENT_HANDLING.tagNameCheck)&&(De.tagNameCheck=e.CUSTOM_ELEMENT_HANDLING.tagNameCheck),e.CUSTOM_ELEMENT_HANDLING&&ht(e.CUSTOM_ELEMENT_HANDLING.attributeNameCheck)&&(De.attributeNameCheck=e.CUSTOM_ELEMENT_HANDLING.attribut" ++ "eNameCheck),e.CUSTOM_ELEMENT_HANDLING&&\"boolean\"==typeof e.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements&&(De.allowCustomizedBuiltInElements=e.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements),ke&&(xe=!1),Be&&(Fe=!0),qe&&(Ne=w({},U),we=[],!0===qe.html&&(w(Ne,L),w(we,z)),!0===qe.svg&&(w(Ne,C),w(we,P),w(we,F)),!0===qe.svgFilters&&(w(Ne,x),w(we,P),w(we,F)),!0===qe.mathMl&&(w(Ne,M),w(we,H),w(we,F))),e.ADD_TAGS&&(Ne===Re&&(Ne=D(Ne)),w(Ne,e.ADD_TAGS,pt)),e.ADD_ATTR&&(we===Oe&&(we=D(we)),w(we,e.ADD_" ++ "ATTR,pt)),e.ADD_URI_SAFE_ATTR&&w(Je,e.ADD_URI_SAFE_ATTR,pt),e.FORBID_CONTENTS&&($e===Ke&&($e=D($e)),w($e,e.FORBID_CONTENTS,pt)),je&&(Ne[\"#text\"]=!0),ze&&w(Ne,[\"html\",\"head\",\"body\"]),Ne.table&&(w(Ne,[\"tbody\"]),delete ve.tbody),e.TRUSTED_TYPES_POLICY){if(\"function\"!=typeof e.TRUSTED_TYPES_POLICY.createHTML)throw b('TRUSTED_TYPES_POLICY configuration option must provide a \"createHTML\" hook.');if(\"function\"!=typeof e.TRUSTED_TYPES_POLICY.createScriptURL)throw b('TRUSTED_TYPES_POLICY configuration option must pr" ++ "ovide a \"createScriptURL\" hook.');le=e.TRUSTED_TYPES_POLICY,ce=le.createHTML(\"\")}else void 0===le&&(le=function(e,t){if(\"object\"!=typeof e||\"function\"!=typeof e.createPolicy)return null;let n=null;const o=\"data-tt-policy-suffix\";t&&t.hasAttribute(o)&&(n=t.getAttribute(o));const r=\"dompurify\"+(n?\"#\"+n:\"\");try{return e.createPolicy(r,{createHTML:e=>e,createScriptURL:e=>e})}catch(e){return console.warn(\"TrustedTypes policy \"+r+\" could not be created.\"),null}}(j,c)),null!==le&&\"string\"==typeof ce&&(ce=le.create" ++ "HTML(\"\"));i&&i(e),ft=e}},Tt=w({},[...C,...x,...I]),yt=w({},[...M,...k]),Et=function(e){f(o.removed,{element:e});try{ae(e).removeChild(e)}catch(t){V(e)}},At=function(e,t){try{f(o.removed,{attribute:t.getAttributeNode(e),from:t})}catch(e){f(o.removed,{attribute:null,from:t})}if(t.removeAttribute(e),\"is\"===e)if(Fe||Be)try{Et(t)}catch(e){}else try{t.setAttribute(e,\"\")}catch(e){}},_t=function(e){let t=null,n=null;if(He)e=\"<remove></remove>\"+e;else{const t=T(e,/^[\\r\\n\\t ]+/);n=t&&t[0]}\"application/xhtml+xml\"===ut" ++ "&&ot===nt&&(e='<html xmlns=\"http://www.w3.org/1999/xhtml\"><head></head><body>'+e+\"</body></html>\");const o=le?le.createHTML(e):e;if(ot===nt)try{t=(new Y).parseFromString(o,ut)}catch(e){}if(!t||!t.documentElement){t=se.createDocument(ot,\"template\",null);try{t.documentElement.innerHTML=rt?ce:o}catch(e){}}const i=t.body||t.documentElement;return e&&n&&i.insertBefore(r.createTextNode(n),i.childNodes[0]||null),ot===nt?pe.call(t,ze?\"html\":\"body\")[0]:ze?t.documentElement:i},St=function(e){return ue.call(e.ownerDoc" ++ "ument||e,e,B.SHOW_ELEMENT|B.SHOW_COMMENT|B.SHOW_TEXT|B.SHOW_PROCESSING_INSTRUCTION|B.SHOW_CDATA_SECTION,null)},bt=function(e){return e instanceof G&&(\"string\"!=typeof e.nodeName||\"string\"!=typeof e.textContent||\"function\"!=typeof e.removeChild||!(e.attributes instanceof W)||\"function\"!=typeof e.removeAttribute||\"function\"!=typeof e.setAttribute||\"string\"!=typeof e.namespaceURI||\"function\"!=typeof e.insertBefore||\"function\"!=typeof e.hasChildNodes)},Nt=function(e){return\"function\"==typeof R&&e instanceof R};" ++ "function Rt(e,t,n){u(e,(e=>{e.call(o,t,n,ft)}))}const wt=function(e){let t=null;if(Rt(de.beforeSanitizeElements,e,null),bt(e))return Et(e),!0;const n=pt(e.nodeName);if(Rt(de.uponSanitizeElement,e,{tagName:n,allowedTags:Ne}),Ue&&e.hasChildNodes()&&!Nt(e.firstElementChild)&&S(/<[/\\w!]/g,e.innerHTML)&&S(/<[/\\w!]/g,e.textContent))return Et(e),!0;if(e.nodeType===ee)return Et(e),!0;if(Ue&&e.nodeType===te&&S(/<[/\\w]/g,e.data))return Et(e),!0;if(!Ne[n]||ve[n]){if(!ve[n]&&Dt(n)){if(De.tagNameCheck instanceof RegExp&" ++ "&S(De.tagNameCheck,n))return!1;if(De.tagNameCheck instanceof Function&&De.tagNameCheck(n))return!1}if(je&&!$e[n]){const t=ae(e)||e.parentNode,n=ie(e)||e.childNodes;if(n&&t){for(let o=n.length-1;o>=0;--o){const r=$(n[o],!0);r.__removalCount=(e.__removalCount||0)+1,t.insertBefore(r,re(e))}}}return Et(e),!0}return e instanceof O&&!function(e){let t=ae(e);t&&t.tagName||(t={namespaceURI:ot,tagName:\"template\"});const n=h(e.tagName),o=h(t.tagName);return!!it[e.namespaceURI]&&(e.namespaceURI===tt?t.namespaceURI===n" ++ "t?\"svg\"===n:t.namespaceURI===et?\"svg\"===n&&(\"annotation-xml\"===o||lt[o]):Boolean(Tt[n]):e.namespaceURI===et?t.namespaceURI===nt?\"math\"===n:t.namespaceURI===tt?\"math\"===n&&ct[o]:Boolean(yt[n]):e.namespaceURI===nt?!(t.namespaceURI===tt&&!ct[o])&&!(t.namespaceURI===et&&!lt[o])&&!yt[n]&&(st[n]||!Tt[n]):!(\"application/xhtml+xml\"!==ut||!it[e.namespaceURI]))}(e)?(Et(e),!0):\"noscript\"!==n&&\"noembed\"!==n&&\"noframes\"!==n||!S(/<\\/no(script|embed|frames)/i,e.innerHTML)?(ke&&e.nodeType===Q&&(t=e.textContent,u([he,ge,Te]" ++ ",(e=>{t=y(t,e,\" \")})),e.textContent!==t&&(f(o.removed,{element:e.cloneNode()}),e.textContent=t)),Rt(de.afterSanitizeElements,e,null),!1):(Et(e),!0)},Ot=function(e,t,n){if(Ge&&(\"id\"===t||\"name\"===t)&&(n in r||n in dt))return!1;if(xe&&!Le[t]&&S(ye,t));else if(Ce&&S(Ee,t));else if(!we[t]||Le[t]){if(!(Dt(e)&&(De.tagNameCheck instanceof RegExp&&S(De.tagNameCheck,e)||De.tagNameCheck instanceof Function&&De.tagNameCheck(e))&&(De.attributeNameCheck instanceof RegExp&&S(De.attributeNameCheck,t)||De.attributeNameChec" ++ "k instanceof Function&&De.attributeNameCheck(t))||\"is\"===t&&De.allowCustomizedBuiltInElements&&(De.tagNameCheck instanceof RegExp&&S(De.tagNameCheck,n)||De.tagNameCheck instanceof Function&&De.tagNameCheck(n))))return!1}else if(Je[t]);else if(S(be,y(n,_e,\"\")));else if(\"src\"!==t&&\"xlink:href\"!==t&&\"href\"!==t||\"script\"===e||0!==E(n,\"data:\")||!Ve[e]){if(Ie&&!S(Ae,y(n,_e,\"\")));else if(n)return!1}else;return!0},Dt=function(e){return\"annotation-xml\"!==e&&T(e,Se)},vt=function(e){Rt(de.beforeSanitizeAttributes,e,nu" ++ "ll);const{attributes:t}=e;if(!t||bt(e))return;const n={attrName:\"\",attrValue:\"\",keepAttr:!0,allowedAttributes:we,forceKeepAttr:void 0};let r=t.length;for(;r--;){const i=t[r],{name:a,namespaceURI:l,value:c}=i,s=pt(a),m=c;let f=\"value\"===a?m:A(m);if(n.attrName=s,n.attrValue=f,n.keepAttr=!0,n.forceKeepAttr=void 0,Rt(de.uponSanitizeAttribute,e,n),f=n.attrValue,!Ye||\"id\"!==s&&\"name\"!==s||(At(a,e),f=\"user-content-\"+f),Ue&&S(/((--!?|])>)|<\\/(style|title)/i,f)){At(a,e);continue}if(n.forceKeepAttr)continue;if(!n.kee" ++ "pAttr){At(a,e);continue}if(!Me&&S(/\\/>/i,f)){At(a,e);continue}ke&&u([he,ge,Te],(e=>{f=y(f,e,\" \")}));const d=pt(e.nodeName);if(Ot(d,s,f)){if(le&&\"object\"==typeof j&&\"function\"==typeof j.getAttributeType)if(l);else switch(j.getAttributeType(d,s)){case\"TrustedHTML\":f=le.createHTML(f);break;case\"TrustedScriptURL\":f=le.createScriptURL(f)}if(f!==m)try{l?e.setAttributeNS(l,a,f):e.setAttribute(a,f),bt(e)?Et(e):p(o.removed)}catch(t){At(a,e)}}else At(a,e)}Rt(de.afterSanitizeAttributes,e,null)},Lt=function e(t){let n=" ++ "null;const o=St(t);for(Rt(de.beforeSanitizeShadowDOM,t,null);n=o.nextNode();)Rt(de.uponSanitizeShadowNode,n,null),wt(n),vt(n),n.content instanceof s&&e(n.content);Rt(de.afterSanitizeShadowDOM,t,null)};return o.sanitize=function(e){let t=arguments.length>1&&void 0!==arguments[1]?arguments[1]:{},n=null,r=null,i=null,l=null;if(rt=!e,rt&&(e=\"\\x3c!--\\x3e\"),\"string\"!=typeof e&&!Nt(e)){if(\"function\"!=typeof e.toString)throw b(\"toString is not a function\");if(\"string\"!=typeof(e=e.toString()))throw b(\"dirty is not a" ++ " string, aborting\")}if(!o.isSupported)return e;if(Pe||gt(t),o.removed=[],\"string\"==typeof e&&(Xe=!1),Xe){if(e.nodeName){const t=pt(e.nodeName);if(!Ne[t]||ve[t])throw b(\"root node is forbidden and cannot be sanitized in-place\")}}else if(e instanceof R)n=_t(\"\\x3c!----\\x3e\"),r=n.ownerDocument.importNode(e,!0),r.nodeType===J&&\"BODY\"===r.nodeName||\"HTML\"===r.nodeName?n=r:n.appendChild(r);else{if(!Fe&&!ke&&!ze&&-1===e.indexOf(\"<\"))return le&&We?le.createHTML(e):e;if(n=_t(e),!n)return Fe?null:We?ce:\"\"}n&&He&&Et(n." ++ "firstChild);const c=St(Xe?e:n);for(;i=c.nextNode();)wt(i),vt(i),i.content instanceof s&&Lt(i.content);if(Xe)return e;if(Fe){if(Be)for(l=me.call(n.ownerDocument);n.firstChild;)l.appendChild(n.firstChild);else l=n;return(we.shadowroot||we.shadowrootmode)&&(l=fe.call(a,l,!0)),l}let m=ze?n.outerHTML:n.innerHTML;return ze&&Ne[\"!doctype\"]&&n.ownerDocument&&n.ownerDocument.doctype&&n.ownerDocument.doctype.name&&S(K,n.ownerDocument.doctype.name)&&(m=\"<!DOCTYPE \"+n.ownerDocument.doctype.name+\">\\n\"+m),ke&&u([he,ge,Te" ++ "],(e=>{m=y(m,e,\" \")})),le&&We?le.createHTML(m):m},o.setConfig=function(){gt(arguments.length>0&&void 0!==arguments[0]?arguments[0]:{}),Pe=!0},o.clearConfig=function(){ft=null,Pe=!1},o.isValidAttribute=function(e,t,n){ft||gt({});const o=pt(e),r=pt(t);return Ot(o,r,n)},o.addHook=function(e,t){\"function\"==typeof t&&f(de[e],t)},o.removeHook=function(e,t){if(void 0!==t){const n=m(de[e],t);return-1===n?void 0:d(de[e],n,1)[0]}return p(de[e])},o.removeHooks=function(e){de[e]=[]},o.removeAllHooks=function(){de={afte" ++ "rSanitizeAttributes:[],afterSanitizeElements:[],afterSanitizeShadowDOM:[],beforeSanitizeAttributes:[],beforeSanitizeElements:[],beforeSanitizeShadowDOM:[],uponSanitizeAttribute:[],uponSanitizeElement:[],uponSanitizeShadowNode:[]}},o}();return re}));\n//# sourceMappingURL=purify.min.js.map\n";
const view_app_css: []const u8 =
    "/* pin viewer: proposal content first */\n:root {\n  color-scheme: light dark;\n  --bg: oklch(0.975 0.006 75);\n  --surface: oklch(0.992 0.004 75);\n  --soft: oklch(0.945 0.008 75);\n  --selected: oklch(0.925 0.025 245);\n  --ink: oklch(0.22 0.012 75);\n  --muted: oklch(0.48 0.012 75);\n  --faint: oklch(0.5 0.01 75);\n  --line: oklch(0.885 0.01 75);\n  --accent: oklch(0.48 0.11 245);\n  --focus: oklch(0.58 0.16 245);\n  --success: oklch(0.94 0.035 145);\n  --success-ink: oklch(0.36 0.07 145);\n  --danger: oklch(0.94 0.035" ++ " 28);\n  --danger-ink: oklch(0.4 0.09 28);\n  --font: -apple-system, BlinkMacSystemFont, \"Segoe UI\", system-ui, sans-serif;\n  --mono: ui-monospace, \"SFMono-Regular\", Consolas, monospace;\n  --top: 3.5rem;\n  --index: 20rem;\n}\n@media (prefers-color-scheme: dark) {\n  :root {\n    --bg: oklch(0.17 0.008 75); --surface: oklch(0.195 0.009 75); --soft: oklch(0.235 0.01 75);\n    --selected: oklch(0.27 0.035 245); --ink: oklch(0.92 0.008 75); --muted: oklch(0.69 0.01 75);\n    --faint: oklch(0.68 0.01 75); --line: oklch(" ++ "0.3 0.01 75); --accent: oklch(0.72 0.11 245);\n    --focus: oklch(0.74 0.15 245); --success: oklch(0.25 0.035 145); --success-ink: oklch(0.82 0.07 145);\n    --danger: oklch(0.25 0.04 28); --danger-ink: oklch(0.84 0.08 28);\n  }\n}\n* { box-sizing: border-box; }\nhtml { font-size: 100%; -webkit-text-size-adjust: 100%; }\nbody { margin: 0; min-height: 100vh; color: var(--ink); background: var(--bg); font: 0.875rem/1.5 var(--font); }\nbutton, input, select { color: inherit; font: inherit; }\nbutton { cursor: pointer; " ++ "}\na { color: var(--accent); text-underline-offset: .16em; }\n:focus { outline: 0; }\n:focus-visible { outline: 2px solid var(--focus); outline-offset: 2px; }\n[hidden] { display: none !important; }\n.visually-hidden { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; }\n.skip { position: fixed; top: -4rem; left: .75rem; z-index: 100; padding: .45rem .7rem; color: var(--ink); background: var(--surface); border: 1px solid var(--line); }\n.skip:focus { top: .75r" ++ "em; }\n\n.topbar { position: sticky; top: 0; z-index: 20; height: var(--top); display: grid; grid-template-columns: var(--index) minmax(14rem, 34rem) 1fr; align-items: center; gap: 1rem; padding: 0 1rem; background: color-mix(in oklch, var(--surface) 94%, transparent); border-bottom: 1px solid var(--line); }\n.identity { min-width: 0; display: flex; align-items: baseline; gap: .6rem; }\n.identity strong { font-size: 1rem; letter-spacing: -.02em; }\n.identity span { overflow: hidden; color: var(--muted); font-siz" ++ "e: .75rem; text-overflow: ellipsis; white-space: nowrap; }\n.search { position: relative; display: block; }\n.search input { width: 100%; height: 2.15rem; padding: 0 2.2rem 0 .7rem; border: 1px solid var(--line); border-radius: .35rem; background: var(--bg); }\n.search input:focus { border-color: var(--focus); background: var(--surface); }\n.search kbd { position: absolute; right: .6rem; top: .45rem; color: var(--faint); font: .7rem var(--mono); }\n.topbar-actions { justify-self: end; display: flex; align-items:" ++ " center; gap: .8rem; }\n.control { min-height: 2rem; padding: .3rem .65rem; border: 1px solid var(--line); border-radius: .35rem; background: var(--surface); }\n.control:hover { background: var(--soft); }\n.count { color: var(--muted); font-size: .75rem; font-variant-numeric: tabular-nums; white-space: nowrap; }\n.filters { position: sticky; top: var(--top); z-index: 15; display: flex; align-items: end; gap: .75rem; padding: .65rem 1rem; background: var(--surface); border-bottom: 1px solid var(--line); }\n.filte" ++ "rs label { display: grid; gap: .2rem; min-width: 9rem; color: var(--muted); font-size: .7rem; font-weight: 600; text-transform: uppercase; letter-spacing: .04em; }\n.filters select, .filters input { height: 2rem; padding: 0 .55rem; border: 1px solid var(--line); border-radius: .3rem; background: var(--bg); font-size: .8rem; text-transform: none; letter-spacing: 0; }\n.text-button { margin: 0 0 .35rem .25rem; padding: 0; color: var(--accent); background: none; border: 0; }\n\n.workspace { display: grid; grid-tem" ++ "plate-columns: var(--index) minmax(0, 1fr); min-height: calc(100vh - var(--top) - 2rem); }\n.index { position: sticky; top: var(--top); align-self: start; height: calc(100vh - var(--top) - 2rem); overflow-y: auto; border-right: 1px solid var(--line); }\n.filters:not([hidden]) + .workspace .index { top: calc(var(--top) + 3.9rem); height: calc(100vh - var(--top) - 5.9rem); }\n.proposal-list { list-style: none; margin: 0; padding: .35rem 0; }\n.proposal-item { width: 100%; padding: .75rem 1rem .8rem; color: inheri" ++ "t; text-align: left; background: transparent; border: 0; }\n.proposal-item:hover { background: var(--soft); }\n.proposal-item[aria-current=\"true\"] { background: var(--selected); }\n.proposal-item strong { display: block; margin-bottom: .16rem; overflow: hidden; font-size: .875rem; font-weight: 600; line-height: 1.35; letter-spacing: -.01em; text-overflow: ellipsis; white-space: nowrap; }\n.proposal-item span { display: flex; gap: .4rem; color: var(--muted); font-size: .72rem; }\n.proposal-item time { margin-left" ++ ": auto; color: var(--faint); font-variant-numeric: tabular-nums; }\n.empty { padding: 2rem 1rem; color: var(--muted); }\n.reader { min-width: 0; padding: 3.5rem clamp(2rem, 6vw, 6rem) 6rem; background: var(--surface); }\n.reader-empty { display: grid; min-height: 22rem; place-items: center; text-align: center; }\n.reader article { width: min(100%, 72ch); margin: 0 auto; }\n.back { display: none; margin-bottom: 1.5rem; }\n.proposal-header { margin-bottom: 2.5rem; }\n.proposal-context { margin: 0 0 .55rem; color: va" ++ "r(--muted); font-size: .72rem; font-weight: 600; text-transform: uppercase; letter-spacing: .06em; }\n#proposal-title { margin: 0 0 .8rem; font-size: 2rem; line-height: 1.15; font-weight: 680; letter-spacing: -.035em; }\n.proposal-summary { display: flex; flex-wrap: wrap; gap: .35rem .85rem; color: var(--muted); font-size: .78rem; }\n.proposal-summary .priority-high { color: var(--ink); font-weight: 650; }\n.proposal-more { margin-top: .75rem; color: var(--muted); font-size: .75rem; }\n.proposal-more summary { w" ++ "idth: max-content; cursor: pointer; }\n.proposal-more dl { display: grid; grid-template-columns: max-content 1fr; gap: .25rem .75rem; margin: .7rem 0 0; }\n.proposal-more dt { color: var(--faint); }\n.proposal-more dd { margin: 0; overflow-wrap: anywhere; font-family: var(--mono); }\n.resolution { margin-top: 1rem; padding: .7rem .85rem; color: var(--success-ink); background: var(--success); border-radius: .35rem; }\n.prose { color: var(--ink); font-size: .98rem; line-height: 1.7; }\n.prose > :first-child { margi" ++ "n-top: 0; }\n.prose > :last-child { margin-bottom: 0; }\n.prose h1, .prose h2, .prose h3, .prose h4 { margin: 1.8em 0 .6em; line-height: 1.25; letter-spacing: -.02em; }\n.prose h1 { font-size: 1.55em; } .prose h2 { font-size: 1.3em; } .prose h3 { font-size: 1.12em; } .prose h4 { font-size: 1em; }\n.prose p, .prose ul, .prose ol, .prose blockquote, .prose pre, .prose table { margin: 0 0 1.1em; }\n.prose ul, .prose ol { padding-left: 1.4em; }\n.prose li + li { margin-top: .3em; }\n.prose blockquote { margin-left: 0;" ++ " padding: .1rem 0 .1rem 1rem; color: var(--muted); border-left: 1px solid var(--faint); }\n.prose code { padding: .1em .3em; background: var(--soft); border-radius: .25rem; font: .88em var(--mono); }\n.prose pre { overflow: auto; padding: 1rem; background: var(--soft); border-radius: .35rem; font-size: .85rem; }\n.prose pre code { padding: 0; background: none; }\n.prose table { display: block; width: 100%; overflow-x: auto; border-collapse: collapse; font-size: .85rem; }\n.prose th, .prose td { padding: .45rem ." ++ "65rem; text-align: left; vertical-align: top; border-bottom: 1px solid var(--line); }\n.prose th { font-weight: 650; }\n.prose hr { margin: 2rem 0; border: 0; border-top: 1px solid var(--line); }\nfooter { min-height: 2rem; display: flex; justify-content: space-between; gap: 1rem; padding: .45rem 1rem; color: var(--faint); border-top: 1px solid var(--line); font-size: .68rem; }\n\n@media (max-width: 799.98px) {\n  :root { --top: auto; }\n  .topbar { position: static; height: auto; grid-template-columns: 1fr auto; " ++ "gap: .65rem; padding: .75rem; }\n  .identity span { display: none; }\n  .search { grid-column: 1 / -1; grid-row: 2; }\n  .topbar-actions { grid-column: 2; grid-row: 1; }\n  .filters { position: static; flex-wrap: wrap; padding: .75rem; }\n  .filters label { flex: 1 1 8rem; }\n  .workspace { display: block; min-height: 0; }\n  .index { position: static; height: auto; min-height: calc(100vh - 9rem); border-right: 0; }\n  .reader { display: none; min-height: calc(100vh - 9rem); padding: 1.25rem 1.15rem 4rem; }\n  body." ++ "detail-open .index { display: none; }\n  body.detail-open .reader { display: block; }\n  .back { display: inline-flex; }\n  #proposal-title { font-size: 1.65rem; }\n  footer { display: none; }\n}\n@media (prefers-reduced-motion: reduce) { *, *::before, *::after { scroll-behavior: auto !important; transition-duration: .01ms !important; animation-duration: .01ms !important; } }\n";
const view_app_js: []const u8 =
    "(function () {\n  'use strict';\n\n  var BASE = document.documentElement.dataset.base || '';\n  var PURIFY_CONFIG = {\n    ALLOWED_TAGS: ['p','br','strong','em','b','i','code','pre','blockquote','h1','h2','h3','h4','h5','h6','ul','ol','li','a','hr','table','thead','tbody','tr','th','td','del','ins','sub','sup'],\n    ALLOWED_ATTR: ['href','title','class'],\n    ALLOWED_URI_REGEXP: /^(?:(?:https?|mailto):|[^a-z]|[a-z+.\\-]+(?:[^a-z+.\\-:]|$))/i,\n    FORBID_TAGS: ['script','iframe','object','embed','form','input','but" ++ "ton','textarea','select','style','link','meta','base','img','svg','math'],\n    FORBID_ATTR: ['style','src','srcdoc','onerror','onclick','onload','onfocus','onblur','onsubmit'],\n    KEEP_CONTENT: true\n  };\n  var ORDER = { kind: ['technical','product','business','project','unspecified'], priority: ['high','medium','low','unset'] };\n  var $ = function (id) { return document.getElementById(id); };\n  var els = {\n    scope: $('scope'), search: $('search'), count: $('count'), filterToggle: $('filter-toggle'), filt" ++ "ers: $('filters'),\n    kind: $('kind-filter'), priority: $('priority-filter'), projectWrap: $('project-wrap'), project: $('project-filter'), projects: $('projects'), clear: $('clear-filters'),\n    list: $('list'), listEmpty: $('list-empty'), reader: $('reader'), detailEmpty: $('detail-empty'), proposal: $('proposal'), back: $('back'),\n    context: $('proposal-context'), title: $('proposal-title'), summary: $('proposal-summary'), more: $('proposal-more'), meta: $('proposal-meta'), resolution: $('resolution')" ++ ", body: $('proposal-body'), snapshot: $('snapshot')\n  };\n  var state = { items: [], shown: [], selected: null, scope: '', archive: '', captured: '', filters: { text: '', kind: '', priority: '', project: '' } };\n\n  function norm(value) { return String(value == null ? '' : value).toLowerCase(); }\n  function text(node, value) { node.textContent = value == null ? '' : String(value); }\n  function clear(node) { while (node.firstChild) node.removeChild(node.firstChild); }\n  function node(tag, className, value) { v" ++ "ar n = document.createElement(tag); if (className) n.className = className; if (value != null) text(n, value); return n; }\n  function kind(item) { return item.kind || 'unspecified'; }\n  function priority(item) { return item.priority || 'unset'; }\n  function tags(item) { return Array.isArray(item.tags) ? item.tags.map(String) : []; }\n  function isNarrow() { return window.matchMedia('(max-width: 799.98px)').matches; }\n  function date(value, long) {\n    var n = Number(value); if (!isFinite(n)) return '';\n    i" ++ "f (n < 1e12) n *= 1000;\n    var opts = long ? { year: 'numeric', month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' } : { month: 'short', day: 'numeric' };\n    try { return new Date(n).toLocaleDateString(undefined, opts); } catch (_) { return new Date(n).toISOString(); }\n  }\n  function captured(value) {\n    var d = new Date(value); if (isNaN(d.getTime())) return value || '';\n    try { return d.toLocaleString(undefined, { month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' }); " ++ "} catch (_) { return d.toISOString(); }\n  }\n  function route() {\n    var match = (location.hash || '').match(/^#\\/idea\\/(.+)$/);\n    if (!match) return null;\n    try { return decodeURIComponent(match[1]); } catch (_) { return null; }\n  }\n  function setRoute(id) {\n    var next = id ? '#/idea/' + encodeURIComponent(id) : '#/';\n    if (location.hash !== next) location.hash = next; else applyRoute();\n  }\n  function find(id) { for (var i = 0; i < state.items.length; i++) if (state.items[i].id === id) return stat" ++ "e.items[i]; return null; }\n\n  function unique(pick) {\n    var seen = Object.create(null), out = [];\n    state.items.forEach(function (item) { var value = pick(item); if (!seen[value]) { seen[value] = true; out.push(value); } });\n    return out;\n  }\n  function fillSelect(select, values, first, order) {\n    clear(select); var base = node('option', '', first); base.value = ''; select.appendChild(base);\n    if (order) values.sort(function (a, b) { return order.indexOf(a) - order.indexOf(b); }); else values.sort" ++ "();\n    values.forEach(function (value) { var option = node('option', '', value); option.value = value; select.appendChild(option); });\n  }\n  function setupFilters() {\n    fillSelect(els.kind, unique(kind), 'Any kind', ORDER.kind);\n    fillSelect(els.priority, unique(priority), 'Any priority', ORDER.priority);\n    var projects = unique(function (item) { return item.project || ''; }).filter(Boolean).sort();\n    clear(els.projects);\n    projects.forEach(function (value) { var option = node('option'); option.v" ++ "alue = value; els.projects.appendChild(option); });\n    els.projectWrap.hidden = projects.length < 2;\n  }\n  function matches(item) {\n    var f = state.filters, q = norm(f.text).trim();\n    if (f.kind && kind(item) !== f.kind) return false;\n    if (f.priority && priority(item) !== f.priority) return false;\n    if (f.project && norm(item.project) !== norm(f.project).trim()) return false;\n    if (!q) return true;\n    return norm([item.title, item.project, item.id, tags(item).join(' '), item.body].join('\\n')).i" ++ "ndexOf(q) !== -1;\n  }\n  function filtersActive() { var f = state.filters; return !!(f.text || f.kind || f.priority || f.project); }\n\n  function renderList() {\n    state.shown = state.items.filter(matches);\n    clear(els.list);\n    text(els.count, state.shown.length === state.items.length ? state.items.length + (state.items.length === 1 ? ' proposal' : ' proposals') : state.shown.length + ' of ' + state.items.length);\n    els.list.hidden = !state.shown.length;\n    els.listEmpty.hidden = !!state.shown.length;" ++ "\n    if (!state.shown.length) {\n      clear(els.listEmpty);\n      els.listEmpty.appendChild(node('strong', '', state.items.length ? 'No proposals match.' : 'No proposals in this snapshot.'));\n      els.listEmpty.appendChild(node('p', '', state.items.length ? 'Try a broader search or clear the filters.' : 'Try another scope or archive mode.'));\n      return;\n    }\n    state.shown.forEach(function (item) {\n      var li = node('li');\n      var button = node('button', 'proposal-item'); button.type = 'button'; b" ++ "utton.dataset.id = item.id;\n      button.setAttribute('aria-current', item.id === state.selected ? 'true' : 'false');\n      button.appendChild(node('strong', '', item.title || '(untitled)'));\n      var meta = node('span');\n      meta.appendChild(node('span', '', state.scope === 'all' && item.project ? item.project : kind(item)));\n      if (priority(item) !== 'unset') meta.appendChild(node('span', '', priority(item)));\n      var time = node('time', '', date(item.timestamp, false)); time.dateTime = String(ite" ++ "m.timestamp || ''); meta.appendChild(time);\n      button.appendChild(meta);\n      button.addEventListener('click', function () { setRoute(item.id); });\n      li.appendChild(button); els.list.appendChild(li);\n    });\n  }\n\n  function addSummary(value, className) { if (value) els.summary.appendChild(node('span', className || '', value)); }\n  function addMeta(label, value) { if (!value) return; els.meta.appendChild(node('dt', '', label)); els.meta.appendChild(node('dd', '', value)); }\n  function bodyWithoutDupl" ++ "icateTitle(body, title) {\n    var source = String(body || ''), match = source.match(/^\\s*#\\s+(.+?)\\s*(?:\\n|$)/);\n    return match && norm(match[1].replace(/[*_`]/g, '').trim()) === norm(title).trim() ? source.slice(match[0].length).replace(/^\\s+/, '') : source;\n  }\n  function renderMarkdown(item) {\n    var source = bodyWithoutDuplicateTitle(item.body, item.title), dirty;\n    try { dirty = marked.parse(source, { async: false }); } catch (_) { text(els.body, source); return; }\n    try { els.body.innerHTML = D" ++ "OMPurify.sanitize(dirty, PURIFY_CONFIG); } catch (_) { text(els.body, source); }\n  }\n  function renderDetail() {\n    var item = find(state.selected);\n    document.body.classList.toggle('detail-open', !!item);\n    els.proposal.hidden = !item; els.detailEmpty.hidden = !!item;\n    if (!item) { text(els.detailEmpty, state.selected ? 'This proposal is not in the snapshot.' : 'Select a proposal to read it.'); return; }\n    var context = [];\n    if (item.project) context.push(item.project); context.push(kind(item)" ++ ");\n    text(els.context, context.join(' \xc2\xb7 ')); text(els.title, item.title || '(untitled)');\n    clear(els.summary); addSummary(priority(item) === 'unset' ? '' : priority(item) + ' priority', 'priority-' + priority(item)); addSummary(date(item.timestamp, true)); addSummary(tags(item).join(' \xc2\xb7 '));\n    clear(els.meta); addMeta('ID', item.id); addMeta('File', item.filename); if (item.archived_at) addMeta('Archived', date(item.archived_at, true));\n    els.more.hidden = !els.meta.children.length;\n    clear(els" ++ ".resolution); els.resolution.hidden = !(item.resolution || item.resolution_note);\n    if (!els.resolution.hidden) { els.resolution.appendChild(node('strong', '', item.resolution ? 'Resolution: ' + item.resolution : 'Resolution')); if (item.resolution_note) els.resolution.appendChild(node('div', '', item.resolution_note)); }\n    renderMarkdown(item);\n  }\n  function applyRoute() {\n    state.selected = route();\n    renderList(); renderDetail();\n    if (state.selected && isNarrow()) { try { els.reader.focus({ p" ++ "reventScroll: true }); } catch (_) { els.reader.focus(); } }\n  }\n\n  function bind() {\n    els.search.addEventListener('input', function () { state.filters.text = this.value; renderList(); });\n    els.kind.addEventListener('change', function () { state.filters.kind = this.value; renderList(); });\n    els.priority.addEventListener('change', function () { state.filters.priority = this.value; renderList(); });\n    els.project.addEventListener('input', function () { state.filters.project = this.value; renderList" ++ "(); });\n    els.filterToggle.addEventListener('click', function () { var open = els.filters.hidden; els.filters.hidden = !open; this.setAttribute('aria-expanded', String(open)); });\n    els.clear.addEventListener('click', function () { state.filters = { text: '', kind: '', priority: '', project: '' }; els.search.value = els.kind.value = els.priority.value = els.project.value = ''; renderList(); });\n    els.back.addEventListener('click', function () { setRoute(null); });\n    window.addEventListener('hashchan" ++ "ge', applyRoute);\n    document.addEventListener('keydown', function (event) {\n      var input = /^(INPUT|SELECT|TEXTAREA)$/.test(event.target.tagName);\n      if (event.key === '/' && !input) { event.preventDefault(); els.search.focus(); return; }\n      if (event.key === 'Escape') { if (input) event.target.blur(); else if (state.selected) setRoute(null); return; }\n      if (input || event.metaKey || event.ctrlKey || event.altKey || (event.key !== 'j' && event.key !== 'k' && event.key !== 'Enter')) return;\n  " ++ "    var index = state.shown.findIndex(function (item) { return item.id === state.selected; });\n      if (event.key === 'j' || event.key === 'k') { event.preventDefault(); index = Math.max(0, Math.min(state.shown.length - 1, (index < 0 ? 0 : index) + (event.key === 'j' ? 1 : -1))); if (state.shown[index]) setRoute(state.shown[index].id); }\n      else if (event.key === 'Enter' && state.shown[index]) { event.preventDefault(); setRoute(state.shown[index].id); }\n    });\n  }\n\n  function ingest(data) {\n    state.i" ++ "tems = Array.isArray(data.items) ? data.items : []; state.scope = String(data.scope || 'all'); state.archive = String(data.archive_filter || ''); state.captured = String(data.captured_at || '');\n    text(els.scope, state.scope === 'all' ? 'All projects' : state.scope); text(els.snapshot, 'Captured ' + captured(state.captured) + (state.archive ? ' \xc2\xb7 ' + state.archive : ''));\n    setupFilters();\n    if (!route() && !isNarrow() && state.items.length) history.replaceState(null, '', '#/idea/' + encodeURICompone" ++ "nt(state.items[0].id));\n    applyRoute();\n  }\n  function fatal(message) { text(els.count, 'Unavailable'); clear(els.listEmpty); els.listEmpty.hidden = false; els.listEmpty.appendChild(node('p', '', message)); }\n  function boot() { bind(); fetch(BASE + '/data.json', { credentials: 'same-origin', cache: 'no-store' }).then(function (res) { if (!res.ok) throw new Error('Could not load snapshot (' + res.status + ')'); return res.json(); }).then(ingest).catch(function (err) { fatal(err.message || 'Could not load " ++ "snapshot.'); }); }\n  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', boot); else boot();\n})();\n";
const view_index_html: []const u8 =
    "<!doctype html>\n<html lang=\"en\" data-base=\"\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n<title>pin</title>\n<link rel=\"stylesheet\" href=\"app.css\">\n</head>\n<body>\n<a class=\"skip\" href=\"#reader\">Skip to proposal</a>\n<header class=\"topbar\">\n  <div class=\"identity\"><strong>pin</strong><span id=\"scope\"></span></div>\n  <label class=\"search\"><span class=\"visually-hidden\">Search proposals</span><input id=\"search\" type=\"search\" placeholder=\"Search proposals\" aut" ++ "ocomplete=\"off\" spellcheck=\"false\"><kbd>/</kbd></label>\n  <div class=\"topbar-actions\">\n    <button id=\"filter-toggle\" class=\"control\" type=\"button\" aria-expanded=\"false\" aria-controls=\"filters\">Filters</button>\n    <span id=\"count\" class=\"count\" aria-live=\"polite\">Loading\xe2\x80\xa6</span>\n  </div>\n</header>\n\n<div id=\"filters\" class=\"filters\" hidden>\n  <label>Kind<select id=\"kind-filter\"><option value=\"\">Any kind</option></select></label>\n  <label>Priority<select id=\"priority-filter\"><option value=\"\">Any priority</" ++ "option></select></label>\n  <label id=\"project-wrap\" hidden>Project<input id=\"project-filter\" list=\"projects\" placeholder=\"All projects\" autocomplete=\"off\"><datalist id=\"projects\"></datalist></label>\n  <button id=\"clear-filters\" class=\"text-button\" type=\"button\">Clear</button>\n</div>\n\n<main class=\"workspace\">\n  <section id=\"index\" class=\"index\" aria-label=\"Proposals\">\n    <div id=\"list-empty\" class=\"empty\" hidden></div>\n    <ul id=\"list\" class=\"proposal-list\"></ul>\n  </section>\n\n  <section id=\"reader\" class=" ++ "\"reader\" tabindex=\"-1\" aria-labelledby=\"proposal-title\">\n    <button id=\"back\" class=\"back control\" type=\"button\">\xe2\x86\x90 Proposals</button>\n    <div id=\"detail-empty\" class=\"empty reader-empty\"><p>Select a proposal to read it.</p></div>\n    <article id=\"proposal\" hidden>\n      <header class=\"proposal-header\">\n        <p id=\"proposal-context\" class=\"proposal-context\"></p>\n        <h1 id=\"proposal-title\"></h1>\n        <div id=\"proposal-summary\" class=\"proposal-summary\"></div>\n        <details id=\"proposal-more\" " ++ "class=\"proposal-more\">\n          <summary>More details</summary>\n          <dl id=\"proposal-meta\"></dl>\n        </details>\n        <div id=\"resolution\" class=\"resolution\" hidden></div>\n      </header>\n      <div id=\"proposal-body\" class=\"prose\"></div>\n    </article>\n  </section>\n</main>\n\n<footer><span id=\"snapshot\"></span><span>Read-only local snapshot</span></footer>\n<script src=\"marked.min.js\"></script>\n<script src=\"purify.min.js\"></script>\n<script src=\"app.js\"></script>\n</body>\n</html>\n";
const ArchiveFilter = enum { active, archived, all };
const IssueSeverity = enum { info, warning, err };

const Issue = struct {
    code: []const u8,
    severity: IssueSeverity,
    filename: []const u8,
    line: usize,
    field: []const u8,
    message: []const u8,
    repairable: bool,
};

const IdeaMeta = struct {
    schema: u16,
    has_schema: bool,
    has_id: bool,
    has_kind: bool,
    id: []const u8,
    filename: []const u8,
    project: []const u8,
    kind: []const u8,
    timestamp: i64,
    created_at_ns: i128,
    title: []const u8,
    tags: []const u8,
    priority: []const u8,
    archived_at: i64,
    resolution: []const u8,
    resolution_note: []const u8,
    body: []const u8,
    score: usize,

    fn lessThan(_: void, a: IdeaMeta, b: IdeaMeta) bool {
        if (a.timestamp != b.timestamp) return a.timestamp > b.timestamp;
        if (a.created_at_ns != b.created_at_ns) return a.created_at_ns > b.created_at_ns;
        return std.mem.order(u8, a.filename, b.filename) == .gt;
    }

    fn contextLessThan(_: void, a: IdeaMeta, b: IdeaMeta) bool {
        const a_rank = priority_rank(a.priority);
        const b_rank = priority_rank(b.priority);
        if (a_rank != b_rank) return a_rank > b_rank;
        return lessThan({}, a, b);
    }

    fn searchLessThan(_: void, a: IdeaMeta, b: IdeaMeta) bool {
        if (a.score != b.score) return a.score > b.score;
        return contextLessThan({}, a, b);
    }
};

fn priority_rank(priority: []const u8) u8 {
    if (std.mem.eql(u8, priority, "high")) return 3;
    if (std.mem.eql(u8, priority, "low")) return 1;
    return 2;
}

fn truncate_title(allocator: std.mem.Allocator, text: []const u8) ![]const u8 {
    const first_line_end = std.mem.indexOfAny(u8, text, "\r\n") orelse text.len;
    const line = std.mem.trim(u8, text[0..first_line_end], " \t");
    var limit = @min(line.len, 60);
    if (limit < line.len) {
        while (limit > 0 and (line[limit] & 0xC0) == 0x80) limit -= 1;
    }
    return allocator.dupe(u8, line[0..limit]);
}

fn get_default_title(allocator: std.mem.Allocator, text: []const u8) ![]const u8 {
    var lines = std.mem.splitScalar(u8, text, '\n');
    while (lines.next()) |raw_line| {
        const line = std.mem.trim(u8, raw_line, " \t\r");
        var hashes: usize = 0;
        while (hashes < line.len and hashes < 6 and line[hashes] == '#') hashes += 1;
        if (hashes > 0 and hashes < line.len and line[hashes] == ' ') {
            const heading = std.mem.trim(u8, line[hashes + 1 ..], " \t");
            if (heading.len > 0) return truncate_title(allocator, heading);
        }
    }
    return truncate_title(allocator, text);
}

fn derive_id(allocator: std.mem.Allocator, seed: []const u8) ![]const u8 {
    var hash: u64 = 14695981039346656037;
    for (seed) |byte| {
        hash ^= byte;
        hash *%= 1099511628211;
    }
    return std.fmt.allocPrint(allocator, "{x:0>12}", .{hash & 0xffffffffffff});
}

fn print_escaped_json(writer: *std.Io.Writer, s: []const u8) !void {
    for (s) |c| {
        switch (c) {
            '\\' => try writer.writeAll("\\\\"),
            '\"' => try writer.writeAll("\\\""),
            '\n' => try writer.writeAll("\\n"),
            '\r' => try writer.writeAll("\\r"),
            '\t' => try writer.writeAll("\\t"),
            else => {
                if (c < 0x20) {
                    try writer.print("\\u{x:0>4}", .{c});
                } else {
                    try writer.writeByte(c);
                }
            },
        }
    }
}

fn emit_tags_json(writer: *std.Io.Writer, tags: []const u8) !void {
    try writer.writeAll("[");
    var first = true;
    var pos: usize = 0;
    while (pos < tags.len) {
        const end = std.mem.indexOfScalarPos(u8, tags, pos, ',') orelse tags.len;
        const tag = std.mem.trim(u8, tags[pos..end], " ");
        if (tag.len > 0) {
            if (!first) try writer.writeAll(",");
            try writer.writeAll("\"");
            try print_escaped_json(writer, tag);
            try writer.writeAll("\"");
            first = false;
        }
        if (end >= tags.len) break;
        pos = end + 1;
    }
    try writer.writeAll("]");
}

fn emit_idea_json(writer: *std.Io.Writer, meta: IdeaMeta, first: bool) !void {
    if (!first) try writer.writeAll(",");
    try writer.writeAll("{\"id\":\"");
    try print_escaped_json(writer, meta.id);
    try writer.writeAll("\",\"filename\":\"");
    try print_escaped_json(writer, meta.filename);
    try writer.writeAll("\",\"project\":\"");
    try print_escaped_json(writer, meta.project);
    try writer.writeAll("\",\"kind\":\"");
    try print_escaped_json(writer, meta.kind);
    try writer.writeAll("\",\"title\":\"");
    try print_escaped_json(writer, meta.title);
    try writer.print("\",\"timestamp\":{d}", .{meta.timestamp});
    if (meta.created_at_ns > 0) try writer.print(",\"created_at_ns\":{d}", .{meta.created_at_ns});
    try writer.writeAll(",\"tags\":");
    try emit_tags_json(writer, meta.tags);
    if (meta.priority.len > 0) {
        try writer.writeAll(",\"priority\":\"");
        try print_escaped_json(writer, meta.priority);
        try writer.writeAll("\"");
    }
    if (meta.archived_at > 0) {
        try writer.print(",\"archived_at\":{d}", .{meta.archived_at});
        if (meta.resolution.len > 0) {
            try writer.writeAll(",\"resolution\":\"");
            try print_escaped_json(writer, meta.resolution);
            try writer.writeAll("\"");
        }
        if (meta.resolution_note.len > 0) {
            try writer.writeAll(",\"resolution_note\":\"");
            try print_escaped_json(writer, meta.resolution_note);
            try writer.writeAll("\"");
        }
    }
    if (meta.score > 0) try writer.print(",\"score\":{d}", .{meta.score});
    try writer.writeAll("}");
}

fn emit_idea_plain(writer: *std.Io.Writer, meta: IdeaMeta) !void {
    try writer.print("{s}  {s:<11}  {s}", .{ meta.id, meta.kind, meta.title });
    if (meta.tags.len > 0) try writer.print("  [{s}]", .{meta.tags});
    if (meta.priority.len > 0) try writer.print("  ({s})", .{meta.priority});
    try writer.writeAll("\n");
}

fn emit_idea_table(writer: *std.Io.Writer, meta: IdeaMeta) !void {
    // Truncate title to 40 chars for table display
    var title_limit = @min(meta.title.len, 40);
    if (title_limit < meta.title.len) {
        while (title_limit > 0 and (meta.title[title_limit] & 0xC0) == 0x80) {
            title_limit -= 1;
        }
    }
    const display_title = meta.title[0..title_limit];
    const ellipsis: []const u8 = if (title_limit < meta.title.len) "..." else "";

    // Format timestamp as YYYY-MM-DD
    if (meta.timestamp > 0) {
        const epoch_secs = std.time.epoch.EpochSeconds{ .secs = @intCast(meta.timestamp) };
        const epoch_day = epoch_secs.getEpochDay();
        const year_day = epoch_day.calculateYearDay();
        const month_day = year_day.calculateMonthDay();
        try writer.print("{d:0>4}-{d:0>2}-{d:0>2}", .{ year_day.year, @intFromEnum(month_day.month), month_day.day_index + 1 });
    } else {
        try writer.writeAll("          ");
    }

    try writer.print("  {s:<16}  {s:<11}  {s:<12}  {s}{s}", .{ meta.project, meta.kind, meta.id, display_title, ellipsis });
    if (meta.tags.len > 0) try writer.print("  [{s}]", .{meta.tags});
    if (meta.priority.len > 0) try writer.print("  ({s})", .{meta.priority});
    try writer.writeAll("\n");
}

fn escape_yaml_string(arena: std.mem.Allocator, value: []const u8) ![]const u8 {
    var result: std.ArrayList(u8) = .empty;
    for (value) |c| {
        switch (c) {
            '\\', '"' => {
                try result.append(arena, '\\');
                try result.append(arena, c);
            },
            '\n' => try result.appendSlice(arena, "\\n"),
            '\r' => try result.appendSlice(arena, "\\r"),
            '\t' => try result.appendSlice(arena, "\\t"),
            else => try result.append(arena, c),
        }
    }
    return result.items;
}

fn unescape_yaml_string(arena: std.mem.Allocator, value: []const u8) ![]const u8 {
    const stripped = strip_yaml_quotes(value);
    // If it doesn't contain any backslashes, we don't need to allocate/unescape
    if (std.mem.indexOfScalar(u8, stripped, '\\') == null) {
        return try arena.dupe(u8, stripped);
    }
    var result: std.ArrayList(u8) = .empty;
    var i: usize = 0;
    while (i < stripped.len) {
        if (stripped[i] == '\\' and i + 1 < stripped.len) {
            const next = stripped[i + 1];
            const decoded: ?u8 = switch (next) {
                '"', '\\' => next,
                'n' => '\n',
                'r' => '\r',
                't' => '\t',
                else => null,
            };
            if (decoded) |c| {
                try result.append(arena, c);
                i += 2;
                continue;
            }
        }
        try result.append(arena, stripped[i]);
        i += 1;
    }
    return result.items;
}

fn add_issue(
    arena: std.mem.Allocator,
    issues: ?*std.ArrayList(Issue),
    filename: []const u8,
    line: usize,
    code: []const u8,
    severity: IssueSeverity,
    field: []const u8,
    repairable: bool,
    comptime fmt: []const u8,
    args: anytype,
) !void {
    const list = issues orelse return;
    try list.append(arena, .{
        .code = code,
        .severity = severity,
        .filename = try arena.dupe(u8, filename),
        .line = line,
        .field = field,
        .message = try std.fmt.allocPrint(arena, fmt, args),
        .repairable = repairable,
    });
}

fn canonical_kind(value: []const u8) ?[]const u8 {
    inline for (.{ "technical", "product", "business", "project", "unspecified" }) |kind| {
        if (std.ascii.eqlIgnoreCase(value, kind)) return kind;
    }
    return null;
}

fn canonical_priority(value: []const u8) ?[]const u8 {
    inline for (.{ "low", "medium", "high" }) |priority| {
        if (std.ascii.eqlIgnoreCase(value, priority)) return priority;
    }
    return null;
}

fn valid_resolution(value: []const u8) bool {
    return std.mem.eql(u8, value, "implemented") or
        std.mem.eql(u8, value, "rejected") or
        std.mem.eql(u8, value, "superseded") or
        std.mem.eql(u8, value, "stale");
}

fn valid_id(value: []const u8) bool {
    if (value.len != 12) return false;
    for (value) |c| if (!std.ascii.isHex(c)) return false;
    return true;
}

fn note_field(
    arena: std.mem.Allocator,
    issues: ?*std.ArrayList(Issue),
    filename: []const u8,
    line: usize,
    key: []const u8,
    seen: *bool,
) !void {
    if (seen.*) try add_issue(arena, issues, filename, line, "duplicate_field", .err, key, false, "Duplicate front-matter field '{s}'", .{key});
    seen.* = true;
}

/// Parse the constrained YAML front matter used by pin and optionally collect diagnostics.
fn parse_front_matter_detailed(
    arena: std.mem.Allocator,
    content: []const u8,
    filename: []const u8,
    issues: ?*std.ArrayList(Issue),
) !?IdeaMeta {
    if (content.len < 4) {
        try add_issue(arena, issues, filename, 1, "missing_front_matter", .err, "", false, "Missing front matter", .{});
        return null;
    }

    const first_nl = std.mem.indexOfScalar(u8, content, '\n') orelse {
        try add_issue(arena, issues, filename, 1, "missing_front_matter", .err, "", false, "Missing front matter", .{});
        return null;
    };
    const first_line = std.mem.trim(u8, content[0..first_nl], " \r");
    if (!std.mem.eql(u8, first_line, "---")) {
        try add_issue(arena, issues, filename, 1, "missing_front_matter", .err, "", false, "Missing opening front-matter delimiter", .{});
        return null;
    }

    var schema: u16 = 0;
    var id: ?[]const u8 = null;
    var project: ?[]const u8 = null;
    var kind: ?[]const u8 = null;
    var timestamp: ?i64 = null;
    var created_at_ns: ?i128 = null;
    var title: ?[]const u8 = null;
    var tags: ?[]const u8 = null;
    var priority: ?[]const u8 = null;
    var archived_at: i64 = 0;
    var resolution: ?[]const u8 = null;
    var resolution_note: ?[]const u8 = null;

    var seen_schema = false;
    var seen_id = false;
    var seen_project = false;
    var seen_kind = false;
    var seen_timestamp = false;
    var seen_created = false;
    var seen_title = false;
    var seen_tags = false;
    var seen_priority = false;
    var seen_archived = false;
    var seen_resolution = false;
    var seen_resolution_note = false;
    var unknown_keys: std.ArrayList([]const u8) = .empty;
    var closed = false;
    var body_start = content.len;
    var line_number: usize = 2;
    var pos = first_nl + 1;

    while (pos < content.len) : (line_number += 1) {
        const line_end = std.mem.indexOfScalarPos(u8, content, pos, '\n') orelse content.len;
        const next_pos = if (line_end < content.len) line_end + 1 else content.len;
        const line = std.mem.trim(u8, content[pos..line_end], " \r");
        pos = next_pos;

        if (std.mem.eql(u8, line, "---")) {
            closed = true;
            body_start = pos;
            break;
        }

        const colon_idx = std.mem.indexOfScalar(u8, line, ':') orelse continue;
        const key = std.mem.trim(u8, line[0..colon_idx], " ");
        const value = std.mem.trim(u8, line[colon_idx + 1 ..], " ");

        if (std.mem.eql(u8, key, "schema")) {
            try note_field(arena, issues, filename, line_number, key, &seen_schema);
            schema = std.fmt.parseInt(u16, value, 10) catch blk: {
                try add_issue(arena, issues, filename, line_number, "invalid_integer", .err, key, false, "Invalid schema value '{s}'", .{value});
                break :blk 0;
            };
            if (schema > 1) try add_issue(arena, issues, filename, line_number, "unsupported_schema", .err, key, false, "Unsupported schema version {d}", .{schema});
        } else if (std.mem.eql(u8, key, "id")) {
            try note_field(arena, issues, filename, line_number, key, &seen_id);
            id = try unescape_yaml_string(arena, value);
            if (id.?.len > 0 and !valid_id(id.?)) try add_issue(arena, issues, filename, line_number, "invalid_id", .err, key, false, "ID must be 12 hexadecimal characters", .{});
        } else if (std.mem.eql(u8, key, "project")) {
            try note_field(arena, issues, filename, line_number, key, &seen_project);
            project = try unescape_yaml_string(arena, value);
        } else if (std.mem.eql(u8, key, "kind")) {
            try note_field(arena, issues, filename, line_number, key, &seen_kind);
            const raw = try unescape_yaml_string(arena, value);
            if (canonical_kind(raw)) |canonical| {
                kind = canonical;
                if (!std.mem.eql(u8, raw, canonical)) try add_issue(arena, issues, filename, line_number, "noncanonical_value", .warning, key, true, "Normalize kind '{s}' to '{s}'", .{ raw, canonical });
            } else {
                kind = "unspecified";
                try add_issue(arena, issues, filename, line_number, "invalid_kind", .err, key, false, "Invalid kind '{s}'", .{raw});
            }
        } else if (std.mem.eql(u8, key, "title")) {
            try note_field(arena, issues, filename, line_number, key, &seen_title);
            title = try unescape_yaml_string(arena, value);
        } else if (std.mem.eql(u8, key, "timestamp")) {
            try note_field(arena, issues, filename, line_number, key, &seen_timestamp);
            timestamp = std.fmt.parseInt(i64, value, 10) catch blk: {
                try add_issue(arena, issues, filename, line_number, "invalid_integer", .err, key, false, "Invalid timestamp '{s}'", .{value});
                break :blk 0;
            };
        } else if (std.mem.eql(u8, key, "created_at_ns")) {
            try note_field(arena, issues, filename, line_number, key, &seen_created);
            created_at_ns = std.fmt.parseInt(i128, value, 10) catch blk: {
                try add_issue(arena, issues, filename, line_number, "invalid_integer", .err, key, false, "Invalid created_at_ns '{s}'", .{value});
                break :blk 0;
            };
        } else if (std.mem.eql(u8, key, "tags")) {
            try note_field(arena, issues, filename, line_number, key, &seen_tags);
            tags = try unescape_yaml_string(arena, value);
        } else if (std.mem.eql(u8, key, "priority")) {
            try note_field(arena, issues, filename, line_number, key, &seen_priority);
            const raw = try unescape_yaml_string(arena, value);
            if (canonical_priority(raw)) |canonical| {
                priority = canonical;
                if (!std.mem.eql(u8, raw, canonical)) try add_issue(arena, issues, filename, line_number, "noncanonical_value", .warning, key, true, "Normalize priority '{s}' to '{s}'", .{ raw, canonical });
            } else {
                priority = raw;
                try add_issue(arena, issues, filename, line_number, "invalid_priority", .err, key, false, "Invalid priority '{s}'", .{raw});
            }
        } else if (std.mem.eql(u8, key, "archived_at")) {
            try note_field(arena, issues, filename, line_number, key, &seen_archived);
            archived_at = std.fmt.parseInt(i64, value, 10) catch blk: {
                try add_issue(arena, issues, filename, line_number, "invalid_integer", .err, key, false, "Invalid archived_at '{s}'", .{value});
                break :blk 0;
            };
        } else if (std.mem.eql(u8, key, "resolution")) {
            try note_field(arena, issues, filename, line_number, key, &seen_resolution);
            resolution = try unescape_yaml_string(arena, value);
            if (resolution.?.len > 0 and !valid_resolution(resolution.?)) try add_issue(arena, issues, filename, line_number, "invalid_resolution", .err, key, false, "Invalid resolution '{s}'", .{resolution.?});
        } else if (std.mem.eql(u8, key, "resolution_note")) {
            try note_field(arena, issues, filename, line_number, key, &seen_resolution_note);
            resolution_note = try unescape_yaml_string(arena, value);
        } else {
            var duplicate = false;
            for (unknown_keys.items) |seen_key| if (std.mem.eql(u8, seen_key, key)) {
                duplicate = true;
                break;
            };
            if (duplicate) {
                try add_issue(arena, issues, filename, line_number, "duplicate_field", .err, key, false, "Duplicate front-matter field '{s}'", .{key});
            } else {
                try unknown_keys.append(arena, try arena.dupe(u8, key));
            }
        }
    }

    if (!closed) try add_issue(arena, issues, filename, line_number, "unterminated_front_matter", .err, "", false, "Missing closing front-matter delimiter", .{});
    if (!seen_schema) try add_issue(arena, issues, filename, 1, "missing_schema", .warning, "schema", true, "Legacy file has no schema version", .{});
    if (!seen_id) try add_issue(arena, issues, filename, 1, "missing_id", .warning, "id", true, "Legacy file has no explicit ID", .{});
    if (!seen_kind) try add_issue(arena, issues, filename, 1, "missing_kind", .info, "kind", false, "Legacy file has no kind", .{});
    if (project == null or project.?.len == 0) try add_issue(arena, issues, filename, 1, "missing_required_field", .err, "project", false, "Missing required field 'project'", .{});
    if (title == null or title.?.len == 0) try add_issue(arena, issues, filename, 1, "missing_required_field", .err, "title", false, "Missing required field 'title'", .{});

    return .{
        .schema = schema,
        .has_schema = seen_schema,
        .has_id = seen_id,
        .has_kind = seen_kind,
        .id = id orelse "",
        .filename = "",
        .project = project orelse "",
        .kind = kind orelse "unspecified",
        .timestamp = timestamp orelse 0,
        .created_at_ns = created_at_ns orelse 0,
        .title = title orelse "",
        .tags = tags orelse "",
        .priority = priority orelse "",
        .archived_at = archived_at,
        .resolution = resolution orelse "",
        .resolution_note = resolution_note orelse "",
        .body = if (closed) content[body_start..] else "",
        .score = 0,
    };
}

fn parse_front_matter_from_buf(arena: std.mem.Allocator, content: []const u8) !?IdeaMeta {
    var issues: std.ArrayList(Issue) = .empty;
    const meta = try parse_front_matter_detailed(arena, content, "", &issues);
    for (issues.items) |issue| if (issue.severity == .err) return null;
    return meta;
}

const FieldUpdate = struct {
    key: []const u8,
    value: ?[]const u8,
};

fn yaml_quoted(arena: std.mem.Allocator, value: []const u8) ![]const u8 {
    return std.fmt.allocPrint(arena, "\"{s}\"", .{try escape_yaml_string(arena, value)});
}

fn append_field_line(arena: std.mem.Allocator, output: *std.ArrayList(u8), key: []const u8, value: []const u8) !void {
    try output.appendSlice(arena, try std.fmt.allocPrint(arena, "{s}: {s}\n", .{ key, value }));
}

fn rewrite_front_matter(arena: std.mem.Allocator, content: []const u8, updates: []const FieldUpdate) ![]const u8 {
    const first_nl = std.mem.indexOfScalar(u8, content, '\n') orelse return error.InvalidFrontMatter;
    if (!std.mem.eql(u8, std.mem.trim(u8, content[0..first_nl], " \r"), "---")) return error.InvalidFrontMatter;

    const applied = try arena.alloc(bool, updates.len);
    @memset(applied, false);
    var output: std.ArrayList(u8) = .empty;
    try output.appendSlice(arena, content[0 .. first_nl + 1]);

    var pos = first_nl + 1;
    while (pos < content.len) {
        const line_start = pos;
        const line_end = std.mem.indexOfScalarPos(u8, content, pos, '\n') orelse content.len;
        const next_pos = if (line_end < content.len) line_end + 1 else content.len;
        const line = std.mem.trim(u8, content[line_start..line_end], " \r");
        pos = next_pos;

        if (std.mem.eql(u8, line, "---")) {
            for (updates, 0..) |update, i| {
                if (!applied[i]) if (update.value) |value| try append_field_line(arena, &output, update.key, value);
            }
            try output.appendSlice(arena, content[line_start..]);
            return output.items;
        }

        const colon_idx = std.mem.indexOfScalar(u8, line, ':');
        var matched = false;
        if (colon_idx) |index| {
            const key = std.mem.trim(u8, line[0..index], " ");
            for (updates, 0..) |update, i| {
                if (std.mem.eql(u8, key, update.key)) {
                    matched = true;
                    if (!applied[i]) {
                        if (update.value) |value| try append_field_line(arena, &output, update.key, value);
                        applied[i] = true;
                    }
                    break;
                }
            }
        }
        if (!matched) try output.appendSlice(arena, content[line_start..next_pos]);
    }
    return error.InvalidFrontMatter;
}

fn atomic_write(arena: std.mem.Allocator, io: std.Io, dir: std.Io.Dir, filename: []const u8, content: []const u8) !void {
    const nonce = std.Io.Timestamp.now(io, .real).toNanoseconds();
    const temp_name = try std.fmt.allocPrint(arena, ".{s}.{d}.tmp", .{ filename, nonce });
    dir.writeFile(io, .{ .sub_path = temp_name, .data = content }) catch |err| return err;
    std.Io.Dir.rename(dir, temp_name, dir, filename, io) catch |err| {
        dir.deleteFile(io, temp_name) catch {};
        return err;
    };
}

const VaultScan = struct {
    ideas: []IdeaMeta,
    issues: []Issue,
    files_scanned: usize,
};

fn scan_vault(arena: std.mem.Allocator, io: std.Io, dir: std.Io.Dir) !VaultScan {
    var ideas: std.ArrayList(IdeaMeta) = .empty;
    var issues: std.ArrayList(Issue) = .empty;
    var files_scanned: usize = 0;
    var it = dir.iterate();
    while (try it.next(io)) |entry| {
        if (entry.kind != .file or !std.mem.endsWith(u8, entry.name, ".md")) continue;
        files_scanned += 1;
        const content = dir.readFileAlloc(io, entry.name, arena, .unlimited) catch |err| {
            try add_issue(arena, &issues, entry.name, 0, "unreadable_file", .err, "", false, "Failed to read file: {any}", .{err});
            continue;
        };
        const issue_start = issues.items.len;
        if (try parse_front_matter_detailed(arena, content, entry.name, &issues)) |raw_meta| {
            var valid = true;
            for (issues.items[issue_start..]) |issue| if (issue.severity == .err) {
                valid = false;
                break;
            };
            var meta = raw_meta;
            meta.filename = try arena.dupe(u8, entry.name);
            meta.id = if (raw_meta.id.len > 0) raw_meta.id else try derive_id(arena, entry.name);
            if (raw_meta.has_id) {
                const stem = entry.name[0 .. entry.name.len - 3];
                const legacy_id = try derive_id(arena, entry.name);
                if (!std.mem.eql(u8, stem, raw_meta.id) and !std.mem.eql(u8, legacy_id, raw_meta.id)) try add_issue(arena, &issues, entry.name, 1, "id_filename_mismatch", .warning, "id", false, "ID '{s}' does not match filename stem '{s}'", .{ raw_meta.id, stem });
            }
            if (valid) try ideas.append(arena, meta);
        }
    }

    for (ideas.items, 0..) |idea, i| {
        for (ideas.items[i + 1 ..]) |other| {
            if (std.mem.eql(u8, idea.id, other.id)) try add_issue(arena, &issues, other.filename, 1, "duplicate_id", .err, "id", false, "ID '{s}' is also used by '{s}'", .{ other.id, idea.filename });
        }
    }
    return .{ .ideas = ideas.items, .issues = issues.items, .files_scanned = files_scanned };
}

fn repair_vault(arena: std.mem.Allocator, io: std.Io, dir: std.Io.Dir) !usize {
    var repaired: usize = 0;
    var it = dir.iterate();
    while (try it.next(io)) |entry| {
        if (entry.kind != .file or !std.mem.endsWith(u8, entry.name, ".md")) continue;
        const content = dir.readFileAlloc(io, entry.name, arena, .unlimited) catch continue;
        var issues: std.ArrayList(Issue) = .empty;
        const raw_meta = (try parse_front_matter_detailed(arena, content, entry.name, &issues)) orelse continue;
        var updates: std.ArrayList(FieldUpdate) = .empty;
        if (!raw_meta.has_schema) try updates.append(arena, .{ .key = "schema", .value = "1" });
        if (!raw_meta.has_id) {
            const derived = try derive_id(arena, entry.name);
            try updates.append(arena, .{ .key = "id", .value = try yaml_quoted(arena, derived) });
        }
        for (issues.items) |issue| {
            if (!std.mem.eql(u8, issue.code, "noncanonical_value")) continue;
            if (std.mem.eql(u8, issue.field, "kind")) try updates.append(arena, .{ .key = "kind", .value = try yaml_quoted(arena, raw_meta.kind) });
            if (std.mem.eql(u8, issue.field, "priority")) try updates.append(arena, .{ .key = "priority", .value = try yaml_quoted(arena, raw_meta.priority) });
        }
        if (updates.items.len == 0) continue;
        const rewritten = rewrite_front_matter(arena, content, updates.items) catch continue;
        try atomic_write(arena, io, dir, entry.name, rewritten);
        repaired += 1;
    }
    return repaired;
}

fn severity_name(severity: IssueSeverity) []const u8 {
    return switch (severity) {
        .info => "info",
        .warning => "warning",
        .err => "error",
    };
}

fn scan_has_failures(scan: VaultScan, strict: bool) bool {
    for (scan.issues) |issue| {
        if (issue.severity == .err or (strict and issue.severity == .warning)) return true;
    }
    return false;
}

fn emit_doctor_report(io: std.Io, vault_path: []const u8, scan: VaultScan, repaired: usize, format: OutputFormat) !void {
    var errors: usize = 0;
    var warnings: usize = 0;
    var infos: usize = 0;
    var repairable: usize = 0;
    for (scan.issues) |issue| {
        switch (issue.severity) {
            .err => errors += 1,
            .warning => warnings += 1,
            .info => infos += 1,
        }
        if (issue.repairable) repairable += 1;
    }

    var buf: [8192]u8 = undefined;
    var w = std.Io.File.stdout().writer(io, &buf);
    if (format == .json) {
        try w.interface.print("{{\"healthy\":{s},\"vault\":\"", .{if (errors == 0) "true" else "false"});
        try print_escaped_json(&w.interface, vault_path);
        try w.interface.print("\",\"files_scanned\":{d},\"repaired\":{d},\"issues\":[", .{ scan.files_scanned, repaired });
        for (scan.issues, 0..) |issue, i| {
            if (i > 0) try w.interface.writeAll(",");
            try w.interface.writeAll("{\"code\":\"");
            try print_escaped_json(&w.interface, issue.code);
            try w.interface.writeAll("\",\"severity\":\"");
            try w.interface.writeAll(severity_name(issue.severity));
            try w.interface.writeAll("\",\"filename\":\"");
            try print_escaped_json(&w.interface, issue.filename);
            try w.interface.print("\",\"line\":{d}", .{issue.line});
            if (issue.field.len > 0) {
                try w.interface.writeAll(",\"field\":\"");
                try print_escaped_json(&w.interface, issue.field);
                try w.interface.writeAll("\"");
            }
            try w.interface.writeAll(",\"message\":\"");
            try print_escaped_json(&w.interface, issue.message);
            try w.interface.print("\",\"repairable\":{s}}}", .{if (issue.repairable) "true" else "false"});
        }
        try w.interface.print("],\"summary\":{{\"errors\":{d},\"warnings\":{d},\"info\":{d},\"repairable\":{d}}}}}\n", .{ errors, warnings, infos, repairable });
    } else {
        try w.interface.print("Vault: {s}\nScanned: {d} Markdown file(s)\n", .{ vault_path, scan.files_scanned });
        if (repaired > 0) try w.interface.print("Repaired: {d} file(s)\n", .{repaired});
        for (scan.issues) |issue| {
            try w.interface.print("{s}: {s}", .{ severity_name(issue.severity), issue.filename });
            if (issue.line > 0) try w.interface.print(":{d}", .{issue.line});
            try w.interface.print(": {s} [{s}]", .{ issue.message, issue.code });
            if (issue.repairable) try w.interface.writeAll(" (repairable)");
            try w.interface.writeAll("\n");
        }
        if (scan.issues.len == 0) try w.interface.writeAll("No integrity issues found.\n");
        try w.interface.print("Summary: {d} error(s), {d} warning(s), {d} info\n", .{ errors, warnings, infos });
    }
    try w.end();
}

fn strip_yaml_quotes(value: []const u8) []const u8 {
    if (value.len >= 2 and value[0] == '"' and value[value.len - 1] == '"') {
        return value[1 .. value.len - 1];
    } else if (value.len >= 2 and value[0] == '\'' and value[value.len - 1] == '\'') {
        return value[1 .. value.len - 1];
    }
    return value;
}

fn validate_filename(filename: []const u8) bool {
    if (filename.len == 0) return false;
    if (std.mem.indexOf(u8, filename, "..") != null) return false;
    if (std.mem.indexOfScalar(u8, filename, '/') != null) return false;
    if (std.mem.indexOfScalar(u8, filename, '\\') != null) return false;
    if (filename[0] == '.') return false;
    return true;
}

fn get_cwd(arena: std.mem.Allocator, io: std.Io) ![]const u8 {
    var buf: [std.fs.max_path_bytes]u8 = undefined;
    const len = try std.process.currentPath(io, &buf);
    return arena.dupe(u8, buf[0..len]);
}

fn find_repo_root(arena: std.mem.Allocator, io: std.Io, cwd: []const u8) !?[]const u8 {
    var current = cwd;
    while (true) {
        const marker = try std.fs.path.join(arena, &.{ current, ".git" });
        if (std.Io.Dir.accessAbsolute(io, marker, .{})) |_| {
            return try arena.dupe(u8, current);
        } else |err| switch (err) {
            error.FileNotFound => {},
            else => return err,
        }

        const parent = std.fs.path.dirname(current) orelse break;
        if (std.mem.eql(u8, parent, current)) break;
        current = parent;
    }
    return null;
}

fn resolve_project(arena: std.mem.Allocator, io: std.Io, environ_map: anytype, override: ?[]const u8) ![]const u8 {
    if (override) |project| return project;
    if (environ_map.get("PIN_PROJECT")) |project| {
        if (project.len > 0) return project;
    }

    const cwd = try get_cwd(arena, io);
    const root = (try find_repo_root(arena, io, cwd)) orelse cwd;
    const config_path = try std.fs.path.join(arena, &.{ root, ".pin-project" });
    const configured = std.Io.Dir.cwd().readFileAlloc(io, config_path, arena, .limited(4096)) catch |err| switch (err) {
        error.FileNotFound => null,
        else => return err,
    };
    if (configured) |raw| {
        const project = std.mem.trim(u8, raw, " \t\r\n");
        if (project.len > 0) return project;
    }
    return std.fs.path.basename(root);
}

fn resolve_vault_path(arena: std.mem.Allocator, io: std.Io, environ_map: anytype) []const u8 {
    if (environ_map.get("PIN_VAULT")) |custom| {
        if (custom.len > 0) {
            if (std.fs.path.isAbsolute(custom)) return custom;
            const base = get_cwd(arena, io) catch {
                std.debug.print("Error: Failed to resolve relative PIN_VAULT.\n", .{});
                std.process.exit(1);
            };
            return std.fs.path.join(arena, &.{ base, custom }) catch {
                std.debug.print("Error: Failed to construct PIN_VAULT path.\n", .{});
                std.process.exit(1);
            };
        }
    }

    const cwd = get_cwd(arena, io) catch {
        std.debug.print("Error: Failed to get current directory.\n", .{});
        std.process.exit(1);
    };
    const root = (find_repo_root(arena, io, cwd) catch null) orelse cwd;
    const local_vault = std.fs.path.join(arena, &.{ root, ".pin_vault" }) catch {
        std.debug.print("Error: Failed to construct local vault path.\n", .{});
        std.process.exit(1);
    };
    if (std.Io.Dir.accessAbsolute(io, local_vault, .{})) |_| return local_vault else |_| {}

    const home = environ_map.get("HOME") orelse {
        std.debug.print("Error: HOME environment variable is not set.\n", .{});
        std.process.exit(1);
    };
    return std.fs.path.join(arena, &.{ home, ".pin_vault" }) catch {
        std.debug.print("Error: Failed to construct vault path.\n", .{});
        std.process.exit(1);
    };
}

fn parse_format(value: []const u8) ?OutputFormat {
    if (std.mem.eql(u8, value, "json")) return .json;
    if (std.mem.eql(u8, value, "table")) return .table;
    if (std.mem.eql(u8, value, "plain")) return .plain;
    return null;
}

fn default_format(io: std.Io, human_format: OutputFormat) OutputFormat {
    return if (std.Io.File.stdout().isTty(io) catch false) human_format else .json;
}

fn valid_priority(priority: []const u8) bool {
    return std.mem.eql(u8, priority, "low") or std.mem.eql(u8, priority, "medium") or std.mem.eql(u8, priority, "high");
}

fn valid_kind(kind: []const u8) bool {
    return std.mem.eql(u8, kind, "technical") or
        std.mem.eql(u8, kind, "product") or
        std.mem.eql(u8, kind, "business") or
        std.mem.eql(u8, kind, "project") or
        std.mem.eql(u8, kind, "unspecified");
}

fn kind_label(kind: []const u8) []const u8 {
    if (std.mem.eql(u8, kind, "technical")) return "Technical";
    if (std.mem.eql(u8, kind, "product")) return "Product";
    if (std.mem.eql(u8, kind, "business")) return "Business";
    if (std.mem.eql(u8, kind, "project")) return "Project";
    return "Unspecified";
}

fn kind_index(kind: []const u8) usize {
    if (std.mem.eql(u8, kind, "technical")) return 0;
    if (std.mem.eql(u8, kind, "product")) return 1;
    if (std.mem.eql(u8, kind, "business")) return 2;
    if (std.mem.eql(u8, kind, "project")) return 3;
    return 4;
}

const kind_names = [_][]const u8{ "technical", "product", "business", "project", "unspecified" };

fn open_vault_dir(io: std.Io, vault_path: []const u8, iterate: bool) ?std.Io.Dir {
    return std.Io.Dir.openDirAbsolute(io, vault_path, .{ .iterate = iterate }) catch |err| {
        if (err == error.FileNotFound) return null;
        std.debug.print("Error: Failed to open vault directory: {any}\n", .{err});
        std.process.exit(1);
    };
}

fn is_word_byte(c: u8) bool {
    return std.ascii.isAlphanumeric(c) or c == '_';
}

fn contains_word_ignore_case(haystack: []const u8, needle: []const u8) bool {
    if (needle.len == 0 or needle.len > haystack.len) return false;
    var pos: usize = 0;
    while (std.ascii.findIgnoreCase(haystack[pos..], needle)) |relative| {
        const start = pos + relative;
        const end = start + needle.len;
        if ((start == 0 or !is_word_byte(haystack[start - 1])) and (end == haystack.len or !is_word_byte(haystack[end]))) return true;
        pos = start + 1;
        if (pos + needle.len > haystack.len) break;
    }
    return false;
}

fn search_score(meta: IdeaMeta, query: []const u8) ?usize {
    var score: usize = 0;
    var terms = std.mem.tokenizeAny(u8, query, " \t\r\n");
    var term_count: usize = 0;
    while (terms.next()) |term| {
        term_count += 1;
        var matched = false;
        if (std.ascii.findIgnoreCase(meta.title, term) != null) {
            score += 8;
            matched = true;
            if (contains_word_ignore_case(meta.title, term)) score += 2;
        }
        if (std.ascii.findIgnoreCase(meta.tags, term) != null) {
            score += 4;
            matched = true;
            if (contains_word_ignore_case(meta.tags, term)) score += 1;
        }
        if (std.ascii.findIgnoreCase(meta.body, term) != null) {
            score += 1;
            matched = true;
            if (contains_word_ignore_case(meta.body, term)) score += 1;
        }
        if (!matched) return null;
    }
    if (term_count == 0) return 1;
    if (std.ascii.eqlIgnoreCase(std.mem.trim(u8, meta.title, " \t\r\n"), std.mem.trim(u8, query, " \t\r\n"))) score += 32 else if (std.ascii.findIgnoreCase(meta.title, query) != null) score += 16;
    if (std.ascii.findIgnoreCase(meta.body, query) != null) score += 2;
    return score;
}

fn archive_matches(meta: IdeaMeta, filter: ArchiveFilter) bool {
    return switch (filter) {
        .active => meta.archived_at == 0,
        .archived => meta.archived_at > 0,
        .all => true,
    };
}

fn collect_ideas_filtered(
    arena: std.mem.Allocator,
    io: std.Io,
    dir: std.Io.Dir,
    filter_project: ?[]const u8,
    filter_query: ?[]const u8,
    filter_tag: ?[]const u8,
    filter_kind: ?[]const u8,
    archive_filter: ArchiveFilter,
) ![]IdeaMeta {
    var list: std.ArrayList(IdeaMeta) = .empty;
    var it = dir.iterate();

    while (try it.next(io)) |entry| {
        if (entry.kind != .file or !std.mem.endsWith(u8, entry.name, ".md")) continue;
        const content = dir.readFileAlloc(io, entry.name, arena, .unlimited) catch continue;
        const meta_opt = try parse_front_matter_from_buf(arena, content);
        if (meta_opt) |raw_meta| {
            var meta = raw_meta;
            meta.id = if (raw_meta.id.len > 0) raw_meta.id else try derive_id(arena, entry.name);
            meta.filename = try arena.dupe(u8, entry.name);

            if (!archive_matches(meta, archive_filter)) continue;
            if (filter_project) |proj| if (!std.mem.eql(u8, meta.project, proj)) continue;
            if (filter_tag) |tag| if (!tag_matches(meta.tags, tag)) continue;
            if (filter_kind) |kind| if (!std.ascii.eqlIgnoreCase(meta.kind, kind)) continue;
            if (filter_query) |query| meta.score = search_score(meta, query) orelse continue;
            try list.append(arena, meta);
        }
    }

    const items = list.items;
    if (filter_query != null) {
        std.sort.block(IdeaMeta, items, {}, IdeaMeta.searchLessThan);
    } else {
        std.sort.block(IdeaMeta, items, {}, IdeaMeta.lessThan);
    }
    return items;
}

fn collect_ideas(
    arena: std.mem.Allocator,
    io: std.Io,
    dir: std.Io.Dir,
    filter_project: ?[]const u8,
    filter_query: ?[]const u8,
    filter_tag: ?[]const u8,
    filter_kind: ?[]const u8,
) ![]IdeaMeta {
    return collect_ideas_filtered(arena, io, dir, filter_project, filter_query, filter_tag, filter_kind, .active);
}

fn tag_matches(tags_csv: []const u8, needle: []const u8) bool {
    if (tags_csv.len == 0) return false;
    var pos: usize = 0;
    while (pos < tags_csv.len) {
        const end = std.mem.indexOfScalarPos(u8, tags_csv, pos, ',') orelse tags_csv.len;
        const tag = std.mem.trim(u8, tags_csv[pos..end], " ");
        if (std.ascii.eqlIgnoreCase(tag, needle)) return true;
        if (end >= tags_csv.len) break;
        pos = end + 1;
    }
    return false;
}

const SelectorError = error{ SelectorNotFound, AmbiguousSelector };

fn resolve_selector(arena: std.mem.Allocator, io: std.Io, dir: std.Io.Dir, selector: []const u8) ![]const u8 {
    if (validate_filename(selector) and std.mem.endsWith(u8, selector, ".md")) {
        if (dir.statFile(io, selector, .{})) |_| {
            return try arena.dupe(u8, selector);
        } else |err| switch (err) {
            error.FileNotFound => {},
            else => return err,
        }
    }

    const ideas = try collect_ideas_filtered(arena, io, dir, null, null, null, null, .all);
    var match: ?[]const u8 = null;
    for (ideas) |meta| {
        if (std.mem.eql(u8, meta.id, selector)) return meta.filename;
        if (std.mem.startsWith(u8, meta.id, selector)) {
            if (match != null) return SelectorError.AmbiguousSelector;
            match = meta.filename;
        }
    }
    return match orelse SelectorError.SelectorNotFound;
}

fn read_stdin(arena: std.mem.Allocator, io: std.Io) !?[]const u8 {
    const stdin_file = std.Io.File.stdin();

    // Check if stdin is a pipe (not a terminal)
    if (stdin_file.isTty(io) catch return null) return null;

    var buf: [4096]u8 = undefined;
    var r = stdin_file.reader(io, &buf);
    const content = r.interface.allocRemaining(arena, .unlimited) catch return null;

    if (content.len == 0) return null;
    return content;
}

fn format_date(writer: *std.Io.Writer, timestamp: i64) !void {
    const epoch_secs = std.time.epoch.EpochSeconds{ .secs = @intCast(timestamp) };
    const epoch_day = epoch_secs.getEpochDay();
    const year_day = epoch_day.calculateYearDay();
    const month_day = year_day.calculateMonthDay();
    try writer.print("{d:0>4}-{d:0>2}-{d:0>2}", .{ year_day.year, @intFromEnum(month_day.month), month_day.day_index + 1 });
}

fn require_flag_value(args: []const []const u8, idx: *usize, flag: []const u8) []const u8 {
    if (idx.* + 1 >= args.len) {
        std.debug.print("Error: {s} requires a value\n", .{flag});
        std.process.exit(1);
    }
    idx.* += 1;
    return args[idx.*];
}

fn parse_format_arg(value: []const u8, allow_table: bool, command: []const u8) OutputFormat {
    const format = parse_format(value) orelse {
        if (allow_table) {
            std.debug.print("Error: --format must be json, table, or plain for '{s}'\n", .{command});
        } else {
            std.debug.print("Error: --format must be json or plain for '{s}'\n", .{command});
        }
        std.process.exit(1);
    };
    if (!allow_table and format == .table) {
        std.debug.print("Error: --format must be json or plain for '{s}'\n", .{command});
        std.process.exit(1);
    }
    return format;
}

const SelectorArgs = struct {
    selector: []const u8,
    format: ?OutputFormat,
};

fn parse_selector_args(args: []const []const u8, command: []const u8) SelectorArgs {
    if (args.len < 3) {
        std.debug.print("Error: '{s}' requires an ID, ID prefix, or filename\n", .{command});
        std.process.exit(1);
    }
    var result = SelectorArgs{ .selector = args[2], .format = null };
    var idx: usize = 3;
    while (idx < args.len) : (idx += 1) {
        if (!std.mem.eql(u8, args[idx], "--format")) {
            std.debug.print("Error: Unexpected argument '{s}'\n", .{args[idx]});
            std.process.exit(1);
        }
        result.format = parse_format_arg(require_flag_value(args, &idx, "--format"), false, command);
    }
    if (!validate_filename(result.selector)) {
        std.debug.print("Error: Invalid selector '{s}'\n", .{result.selector});
        std.process.exit(1);
    }
    return result;
}

fn open_vault_or_exit(io: std.Io, vault_path: []const u8) std.Io.Dir {
    return std.Io.Dir.openDirAbsolute(io, vault_path, .{ .iterate = true }) catch |err| {
        if (err == error.FileNotFound) std.debug.print("Error: Vault not found.\n", .{}) else std.debug.print("Error: Failed to open vault directory: {any}\n", .{err});
        std.process.exit(1);
    };
}

fn resolve_selector_or_exit(arena: std.mem.Allocator, io: std.Io, dir: std.Io.Dir, selector: []const u8) []const u8 {
    return resolve_selector(arena, io, dir, selector) catch |err| {
        if (err == SelectorError.AmbiguousSelector) {
            std.debug.print("Error: Selector '{s}' is ambiguous. Use more ID characters.\n", .{selector});
        } else if (err == SelectorError.SelectorNotFound) {
            std.debug.print("Error: No idea matches '{s}'.\n", .{selector});
        } else {
            std.debug.print("Error: Failed to resolve '{s}': {any}\n", .{ selector, err });
        }
        std.process.exit(1);
    };
}

fn idea_id_from_content(arena: std.mem.Allocator, filename: []const u8, content: []const u8) ![]const u8 {
    const meta = try parse_front_matter_from_buf(arena, content);
    if (meta) |value| if (value.id.len > 0) return value.id;
    return derive_id(arena, filename);
}

fn idea_id_for_file(arena: std.mem.Allocator, io: std.Io, dir: std.Io.Dir, filename: []const u8) ![]const u8 {
    return idea_id_from_content(arena, filename, try dir.readFileAlloc(io, filename, arena, .unlimited));
}

// --- view: snapshot + HTTP server ---

const view_csp =
    "default-src 'none'; base-uri 'none'; form-action 'none'; frame-ancestors 'none'; img-src 'self' data:; style-src 'self'; script-src 'self'; connect-src 'self'; sandbox allow-scripts allow-same-origin";

const ViewArgs = struct {
    filter_project: ?[]const u8 = null,
    filter_tag: ?[]const u8 = null,
    filter_kind: ?[]const u8 = null,
    archive_filter: ArchiveFilter = .active,
    port: u16 = 0,
    no_open: bool = false,
    format: OutputFormat = .plain,
    format_explicit: bool = false,
    project_scoped: bool = false,
};

const ViewSnapshot = struct {
    captured_at_unix: i64,
    captured_at_iso: []const u8,
    scope_label: []const u8,
    vault_path: []const u8,
    archive_filter: ArchiveFilter,
    token: []const u8,
    items: []const IdeaMeta,
    data_json: []const u8,
    html: []const u8,
};

fn archive_filter_label(filter: ArchiveFilter) []const u8 {
    return switch (filter) {
        .active => "active",
        .archived => "archived",
        .all => "all",
    };
}

fn format_iso8601_utc(writer: *std.Io.Writer, timestamp: i64) !void {
    const secs: u64 = if (timestamp > 0) @intCast(timestamp) else 0;
    const epoch_secs = std.time.epoch.EpochSeconds{ .secs = secs };
    const epoch_day = epoch_secs.getEpochDay();
    const year_day = epoch_day.calculateYearDay();
    const month_day = year_day.calculateMonthDay();
    const day_seconds = epoch_secs.getDaySeconds();
    try writer.print("{d:0>4}-{d:0>2}-{d:0>2}T{d:0>2}:{d:0>2}:{d:0>2}Z", .{
        year_day.year,
        @intFromEnum(month_day.month),
        month_day.day_index + 1,
        day_seconds.getHoursIntoDay(),
        day_seconds.getMinutesIntoHour(),
        day_seconds.getSecondsIntoMinute(),
    });
}

fn generate_view_token(arena: std.mem.Allocator, io: std.Io) ![]const u8 {
    var bytes: [16]u8 = undefined;
    io.randomSecure(&bytes) catch io.random(&bytes);
    var hex_buf: [32]u8 = undefined;
    const hex_charset = "0123456789abcdef";
    for (bytes, 0..) |b, i| {
        hex_buf[i * 2] = hex_charset[b >> 4];
        hex_buf[i * 2 + 1] = hex_charset[b & 15];
    }
    return try arena.dupe(u8, &hex_buf);
}

fn emit_view_item_json(writer: *std.Io.Writer, meta: IdeaMeta, first: bool) !void {
    if (!first) try writer.writeAll(",");
    try writer.writeAll("{\"id\":\"");
    try print_escaped_json(writer, meta.id);
    try writer.writeAll("\",\"filename\":\"");
    try print_escaped_json(writer, meta.filename);
    try writer.writeAll("\",\"project\":\"");
    try print_escaped_json(writer, meta.project);
    try writer.writeAll("\",\"kind\":\"");
    try print_escaped_json(writer, meta.kind);
    try writer.writeAll("\",\"title\":\"");
    try print_escaped_json(writer, meta.title);
    try writer.print("\",\"timestamp\":{d}", .{meta.timestamp});
    try writer.writeAll(",\"tags\":");
    try emit_tags_json(writer, meta.tags);
    if (meta.priority.len > 0) {
        try writer.writeAll(",\"priority\":\"");
        try print_escaped_json(writer, meta.priority);
        try writer.writeAll("\"");
    }
    if (meta.archived_at > 0) {
        try writer.print(",\"archived_at\":{d}", .{meta.archived_at});
        if (meta.resolution.len > 0) {
            try writer.writeAll(",\"resolution\":\"");
            try print_escaped_json(writer, meta.resolution);
            try writer.writeAll("\"");
        }
        if (meta.resolution_note.len > 0) {
            try writer.writeAll(",\"resolution_note\":\"");
            try print_escaped_json(writer, meta.resolution_note);
            try writer.writeAll("\"");
        }
    }
    try writer.writeAll(",\"body\":\"");
    try print_escaped_json(writer, meta.body);
    try writer.writeAll("\"}");
}

fn build_view_data_json(
    arena: std.mem.Allocator,
    snap: *const ViewSnapshot,
) ![]const u8 {
    var aw: std.Io.Writer.Allocating = .init(arena);
    errdefer aw.deinit();
    const w = &aw.writer;
    try w.writeAll("{\"captured_at\":\"");
    try print_escaped_json(w, snap.captured_at_iso);
    try w.writeAll("\",\"scope\":\"");
    try print_escaped_json(w, snap.scope_label);
    try w.writeAll("\",\"vault\":\"");
    try print_escaped_json(w, snap.vault_path);
    try w.writeAll("\",\"archive_filter\":\"");
    try w.writeAll(archive_filter_label(snap.archive_filter));
    try w.print("\",\"count\":{d},\"items\":[", .{snap.items.len});
    for (snap.items, 0..) |meta, i| {
        try emit_view_item_json(w, meta, i == 0);
    }
    try w.writeAll("]}");
    return try aw.toOwnedSlice();
}

fn inject_view_html_base(arena: std.mem.Allocator, token: []const u8) ![]const u8 {
    const needle = "data-base=\"\"";
    const idx = std.mem.indexOf(u8, view_index_html, needle) orelse {
        return try arena.dupe(u8, view_index_html);
    };
    const replacement = try std.fmt.allocPrint(arena, "data-base=\"/{s}\"", .{token});
    const out_len = view_index_html.len - needle.len + replacement.len;
    var out = try arena.alloc(u8, out_len);
    @memcpy(out[0..idx], view_index_html[0..idx]);
    @memcpy(out[idx..][0..replacement.len], replacement);
    @memcpy(out[idx + replacement.len ..], view_index_html[idx + needle.len ..]);
    return out;
}

fn strip_http_query(target: []const u8) []const u8 {
    if (std.mem.indexOfScalar(u8, target, '?')) |q| return target[0..q];
    return target;
}

/// Returns the path after `/<token>` (leading slash stripped from remainder),
/// or null when the token does not match.
fn view_route_rest(target: []const u8, token: []const u8) ?[]const u8 {
    const path = strip_http_query(target);
    if (path.len == 0 or path[0] != '/') return null;
    const after_slash = path[1..];
    if (!std.mem.startsWith(u8, after_slash, token)) return null;
    if (after_slash.len == token.len) return "";
    if (after_slash[token.len] != '/') return null;
    return after_slash[token.len + 1 ..];
}

fn respond_view(
    request: *std.http.Server.Request,
    status: std.http.Status,
    content_type: []const u8,
    body: []const u8,
    extra: []const std.http.Header,
) void {
    var headers_buf: [8]std.http.Header = undefined;
    var n: usize = 0;
    if (content_type.len > 0) {
        headers_buf[n] = .{ .name = "Content-Type", .value = content_type };
        n += 1;
    }
    headers_buf[n] = .{ .name = "Content-Security-Policy", .value = view_csp };
    n += 1;
    headers_buf[n] = .{ .name = "X-Content-Type-Options", .value = "nosniff" };
    n += 1;
    headers_buf[n] = .{ .name = "Referrer-Policy", .value = "no-referrer" };
    n += 1;
    headers_buf[n] = .{ .name = "X-Frame-Options", .value = "DENY" };
    n += 1;
    headers_buf[n] = .{ .name = "Cache-Control", .value = "no-store" };
    n += 1;
    for (extra) |h| {
        if (n >= headers_buf.len) break;
        headers_buf[n] = h;
        n += 1;
    }
    request.respond(body, .{
        .status = status,
        .keep_alive = false,
        .extra_headers = headers_buf[0..n],
    }) catch {};
}

fn handle_view_request(request: *std.http.Server.Request, snap: *const ViewSnapshot) void {
    const method = request.head.method;
    if (method != .GET and method != .HEAD) {
        respond_view(request, .method_not_allowed, "text/plain; charset=utf-8", "Method Not Allowed", &.{
            .{ .name = "Allow", .value = "GET" },
        });
        return;
    }

    const rest = view_route_rest(request.head.target, snap.token) orelse {
        respond_view(request, .not_found, "text/plain; charset=utf-8", "Not Found", &.{});
        return;
    };

    if (rest.len == 0 or std.mem.eql(u8, rest, "index.html")) {
        respond_view(request, .ok, "text/html; charset=utf-8", snap.html, &.{});
        return;
    }
    if (std.mem.eql(u8, rest, "data.json")) {
        respond_view(request, .ok, "application/json; charset=utf-8", snap.data_json, &.{});
        return;
    }
    if (std.mem.eql(u8, rest, "app.css")) {
        respond_view(request, .ok, "text/css; charset=utf-8", view_app_css, &.{});
        return;
    }
    if (std.mem.eql(u8, rest, "app.js")) {
        respond_view(request, .ok, "text/javascript; charset=utf-8", view_app_js, &.{});
        return;
    }
    if (std.mem.eql(u8, rest, "marked.min.js")) {
        respond_view(request, .ok, "text/javascript; charset=utf-8", marked_js, &.{});
        return;
    }
    if (std.mem.eql(u8, rest, "purify.min.js")) {
        respond_view(request, .ok, "text/javascript; charset=utf-8", dompurify_js, &.{});
        return;
    }

    respond_view(request, .not_found, "text/plain; charset=utf-8", "Not Found", &.{});
}

fn handle_connection_thread(io: std.Io, stream: std.Io.net.Stream, snap: *const ViewSnapshot) void {
    defer {
        var copy = stream;
        copy.close(io);
    }
    var send_buf: [8192]u8 = undefined;
    var recv_buf: [16384]u8 = undefined;
    var conn_r = stream.reader(io, &recv_buf);
    var conn_w = stream.writer(io, &send_buf);
    var http_server = std.http.Server.init(&conn_r.interface, &conn_w.interface);

    var request = http_server.receiveHead() catch |err| {
        if (err != error.HttpConnectionClosing) {
            if (err == error.HttpHeadersInvalid or err == error.HttpHeadersOversize) {
                // cannot reliably respond without a Request; just drop
            }
        }
        return;
    };
    handle_view_request(&request, snap);
}

fn serve_view(io: std.Io, server: *std.Io.net.Server, snap: *const ViewSnapshot) void {
    while (true) {
        var stream = server.accept(io) catch |err| {
            std.debug.print("Warning: accept failed: {any}\n", .{err});
            continue;
        };
        if (comptime builtin.single_threaded) {
            handle_connection_thread(io, stream, snap);
        } else {
            const thread = std.Thread.spawn(.{}, handle_connection_thread, .{ io, stream, snap }) catch |err| {
                std.debug.print("Warning: failed to spawn connection thread: {any}\n", .{err});
                stream.close(io);
                continue;
            };
            thread.detach();
        }
    }
}

fn open_browser(io: std.Io, base_url: []const u8) void {
    const argv: []const []const u8 = switch (builtin.os.tag) {
        .linux => &.{ "xdg-open", base_url },
        .macos => &.{ "open", base_url },
        .windows => &.{ "cmd", "/c", "start", "", base_url },
        else => {
            std.debug.print("Warning: no browser launcher for this OS; open {s} manually\n", .{base_url});
            return;
        },
    };

    var child = std.process.spawn(io, .{
        .argv = argv,
        .stdin = .ignore,
        .stdout = .ignore,
        .stderr = .ignore,
    }) catch |err| {
        std.debug.print("Warning: failed to open browser: {any}\n", .{err});
        return;
    };
    _ = child.wait(io) catch {
        std.debug.print("Warning: browser launcher did not exit cleanly\n", .{});
        return;
    };
    std.debug.print("Opened in browser.\n", .{});
}

fn parse_view_args(args: []const []const u8, command: []const u8, project_scoped: bool) ViewArgs {
    var result = ViewArgs{ .project_scoped = project_scoped };
    var idx: usize = 2;
    while (idx < args.len) : (idx += 1) {
        const arg = args[idx];
        if (std.mem.eql(u8, arg, "--archived")) {
            result.archive_filter = .archived;
        } else if (std.mem.eql(u8, arg, "--all")) {
            result.archive_filter = .all;
        } else if (std.mem.eql(u8, arg, "--no-open")) {
            result.no_open = true;
        } else if (std.mem.eql(u8, arg, "--project") or std.mem.eql(u8, arg, "--tag") or
            std.mem.eql(u8, arg, "--kind") or std.mem.eql(u8, arg, "--format") or
            std.mem.eql(u8, arg, "--port"))
        {
            const value = require_flag_value(args, &idx, arg);
            if (std.mem.eql(u8, arg, "--project")) {
                if (project_scoped) {
                    std.debug.print("Error: '{s}' does not accept --project\n", .{command});
                    std.process.exit(1);
                }
                result.filter_project = value;
            } else if (std.mem.eql(u8, arg, "--tag")) {
                result.filter_tag = value;
            } else if (std.mem.eql(u8, arg, "--kind")) {
                if (!valid_kind(value)) {
                    std.debug.print("Error: --kind must be technical, product, business, project, or unspecified\n", .{});
                    std.process.exit(1);
                }
                result.filter_kind = value;
            } else if (std.mem.eql(u8, arg, "--format")) {
                result.format = parse_format_arg(value, false, command);
                result.format_explicit = true;
            } else if (std.mem.eql(u8, arg, "--port")) {
                const parsed = std.fmt.parseInt(u16, value, 10) catch {
                    std.debug.print("Error: --port must be an integer 0-65535\n", .{});
                    std.process.exit(1);
                };
                result.port = parsed;
            }
        } else {
            std.debug.print("Error: Unknown flag '{s}'\n", .{arg});
            std.process.exit(1);
        }
    }
    return result;
}

fn run_view_command(
    arena: std.mem.Allocator,
    io: std.Io,
    environ_map: anytype,
    vault_path: []const u8,
    args: ViewArgs,
) !void {
    const stdout_is_tty = std.Io.File.stdout().isTty(io) catch false;
    if (!stdout_is_tty and !args.no_open and !args.format_explicit) {
        std.debug.print("Error: non-interactive view requires --no-open or explicit --format json|plain\n", .{});
        std.process.exit(1);
    }

    var filter_project = args.filter_project;
    if (args.project_scoped) {
        filter_project = resolve_project(arena, io, environ_map, null) catch |err| {
            std.debug.print("Error: Failed to resolve project: {any}\n", .{err});
            std.process.exit(1);
        };
    }

    const scope_label: []const u8 = if (filter_project) |p| p else "all";

    var items: []IdeaMeta = &.{};
    if (open_vault_dir(io, vault_path, true)) |dir_opened| {
        var dir = dir_opened;
        defer dir.close(io);
        items = try collect_ideas_filtered(
            arena,
            io,
            dir,
            filter_project,
            null,
            args.filter_tag,
            args.filter_kind,
            args.archive_filter,
        );
    }

    const captured_at_unix = std.Io.Timestamp.now(io, .real).toSeconds();
    var iso_buf: [32]u8 = undefined;
    var iso_w = std.Io.Writer.fixed(&iso_buf);
    try format_iso8601_utc(&iso_w, captured_at_unix);
    const captured_at_iso = try arena.dupe(u8, iso_w.buffered());

    const token = try generate_view_token(arena, io);
    const html = try inject_view_html_base(arena, token);

    var snap = ViewSnapshot{
        .captured_at_unix = captured_at_unix,
        .captured_at_iso = captured_at_iso,
        .scope_label = scope_label,
        .vault_path = vault_path,
        .archive_filter = args.archive_filter,
        .token = token,
        .items = items,
        .data_json = undefined,
        .html = html,
    };
    snap.data_json = try build_view_data_json(arena, &snap);

    var addr = try std.Io.net.IpAddress.parse("127.0.0.1", args.port);
    var server = addr.listen(io, .{ .reuse_address = true }) catch |err| {
        std.debug.print("Error: Failed to listen on 127.0.0.1:{d}: {any}\n", .{ args.port, err });
        std.process.exit(1);
    };
    defer server.deinit(io);

    const bound_port = server.socket.address.getPort();
    const base_url = try std.fmt.allocPrint(arena, "http://127.0.0.1:{d}/{s}/", .{ bound_port, token });

    {
        var out_buf: [512]u8 = undefined;
        var w = std.Io.File.stdout().writer(io, &out_buf);
        if (args.format == .json) {
            try w.interface.writeAll("{\"url\":\"");
            try print_escaped_json(&w.interface, base_url);
            try w.interface.print("\",\"count\":{d},\"scope\":\"", .{items.len});
            try print_escaped_json(&w.interface, scope_label);
            try w.interface.writeAll("\",\"vault\":\"");
            try print_escaped_json(&w.interface, vault_path);
            try w.interface.writeAll("\",\"captured_at\":\"");
            try print_escaped_json(&w.interface, captured_at_iso);
            try w.interface.writeAll("\",\"token\":\"");
            try print_escaped_json(&w.interface, token);
            try w.interface.writeAll("\"}\n");
        } else {
            try w.interface.print("{s}\n", .{base_url});
        }
        try w.end();
    }

    std.debug.print(
        "Snapshot captured at {s} · {d} ideas · Ctrl+C to stop\n",
        .{ captured_at_iso, items.len },
    );

    const should_open = stdout_is_tty and !args.no_open;
    if (should_open) open_browser(io, base_url);

    serve_view(io, &server, &snap);
}

fn print_usage(io: std.Io) !void {
    var buf: [4096]u8 = undefined;
    var w = std.Io.File.stdout().writer(io, &buf);
    try w.interface.print(
        \\pin {s} — a small idea registry for humans and agents
        \\
        \\Usage:
        \\  pin init --local [--project <name>] [--format json|plain]
        \\  pin add "<markdown>" --kind technical|product|business|project [--project <name>] [--title <title>] [--tags <csv>] [--priority low|medium|high] [--format json|plain]
        \\  pin add --stdin [options]
        \\  pin list [--project <name>] [--tag <name>] [--kind <kind>] [--archived|--all] [--format json|table|plain]
        \\  pin list-project [--tag <name>] [--kind <kind>] [--archived|--all] [--format json|table|plain]
        \\  pin search "<query>" [--project <name>] [--tag <name>] [--kind <kind>] [--limit <n>] [--archived|--all] [--format json|table|plain]
        \\  pin context [--project <name>] [--kind <kind>] [--limit <n>] [--group kind] [--archived|--all] [--format json|plain]
        \\  pin doctor [--repair] [--strict] [--format json|plain]
        \\  pin archive <id|id-prefix|filename> [--resolution implemented|rejected|superseded|stale] [--note <text>] [--format json|plain]
        \\  pin unarchive <id|id-prefix|filename> [--format json|plain]
        \\  pin import <directory> [--force] [--format json|plain]
        \\  pin export <directory> [--force] [--format json|plain]
        \\  pin read <id|id-prefix|filename> [--format json|plain]
        \\  pin rm <id|id-prefix|filename> [--format json|plain]
        \\  pin edit <id|id-prefix|filename> [--format json|plain]
        \\  pin stats [--format json|plain]
        \\  pin view [--project <name>] [--tag <name>] [--kind <kind>] [--archived|--all] [--port <n>] [--no-open] [--format json|plain]
        \\  pin view-project [--tag <name>] [--kind <kind>] [--archived|--all] [--port <n>] [--no-open] [--format json|plain]
        \\  pin help | --help | --version
        \\
        \\Environment:
        \\  PIN_VAULT      Override vault path (~/.pin_vault)
        \\  PIN_PROJECT    Override repository-aware project name
        \\
        \\A .pin-project file at the repository root can also set the project name.
        \\
    , .{version});
    try w.end();
}

pub fn main(init: std.process.Init) !void {
    const io = init.io;
    const arena = init.arena.allocator();

    const args = try init.minimal.args.toSlice(arena);
    if (args.len < 2) {
        try print_usage(io);
        std.process.exit(1);
    }

    const cmd = args[1];
    if (std.mem.eql(u8, cmd, "--help") or std.mem.eql(u8, cmd, "-h") or std.mem.eql(u8, cmd, "help")) {
        try print_usage(io);
        return;
    }
    if (std.mem.eql(u8, cmd, "--version") or std.mem.eql(u8, cmd, "-V")) {
        try std.Io.File.stdout().writeStreamingAll(io, "pin " ++ version ++ "\n");
        return;
    }
    if (args.len > 2 and (std.mem.eql(u8, args[2], "--help") or std.mem.eql(u8, args[2], "-h"))) {
        try print_usage(io);
        return;
    }

    const vault_path = resolve_vault_path(arena, io, init.environ_map);

    // ── init ─────────────────────────────────────────────────────────────
    if (std.mem.eql(u8, cmd, "init")) {
        var local = false;
        var project: ?[]const u8 = null;
        var format: ?OutputFormat = null;
        var idx: usize = 2;
        while (idx < args.len) : (idx += 1) {
            if (std.mem.eql(u8, args[idx], "--local")) {
                local = true;
            } else if (std.mem.eql(u8, args[idx], "--project") or std.mem.eql(u8, args[idx], "--format")) {
                const flag = args[idx];
                const value = require_flag_value(args, &idx, flag);
                if (std.mem.eql(u8, flag, "--project")) project = value else format = parse_format_arg(value, false, cmd);
            } else {
                std.debug.print("Error: Unknown flag '{s}'\n", .{args[idx]});
                std.process.exit(1);
            }
        }
        if (!local) {
            std.debug.print("Error: 'init' currently requires --local\n", .{});
            std.process.exit(1);
        }
        const cwd = try get_cwd(arena, io);
        const root = (try find_repo_root(arena, io, cwd)) orelse cwd;
        const local_path = try std.fs.path.join(arena, &.{ root, ".pin_vault" });
        try std.Io.Dir.cwd().createDirPath(io, local_path);
        var local_dir = try std.Io.Dir.openDirAbsolute(io, local_path, .{});
        defer local_dir.close(io);
        try local_dir.writeFile(io, .{ .sub_path = ".gitkeep", .data = "" });
        const config_path = try std.fs.path.join(arena, &.{ root, ".pin-project" });
        if (std.Io.Dir.accessAbsolute(io, config_path, .{})) |_| {} else |_| {
            const project_name = project orelse std.fs.path.basename(root);
            const config = try std.fmt.allocPrint(arena, "{s}\n", .{project_name});
            try std.Io.Dir.cwd().writeFile(io, .{ .sub_path = config_path, .data = config });
        }
        var out_buf: [std.fs.max_path_bytes + 128]u8 = undefined;
        var w = std.Io.File.stdout().writer(io, &out_buf);
        if ((format orelse default_format(io, .plain)) == .json) {
            try w.interface.writeAll("{\"vault\":\"");
            try print_escaped_json(&w.interface, local_path);
            try w.interface.writeAll("\",\"scope\":\"local\"}\n");
        } else try w.interface.print("Initialized local vault at {s}\n", .{local_path});
        try w.end();

        // ── add ──────────────────────────────────────────────────────────
    } else if (std.mem.eql(u8, cmd, "add")) {
        var content: ?[]const u8 = null;
        var project: ?[]const u8 = null;
        var title: ?[]const u8 = null;
        var tags: ?[]const u8 = null;
        var kind: ?[]const u8 = null;
        var priority: ?[]const u8 = null;
        var format: ?OutputFormat = null;
        var use_stdin = false;
        var allow_duplicate = false;

        var idx: usize = 2;
        while (idx < args.len) : (idx += 1) {
            const arg = args[idx];
            if (std.mem.eql(u8, arg, "--stdin")) {
                use_stdin = true;
            } else if (std.mem.eql(u8, arg, "--allow-duplicate")) {
                allow_duplicate = true;
            } else if (std.mem.eql(u8, arg, "--project") or std.mem.eql(u8, arg, "--title") or std.mem.eql(u8, arg, "--tags") or std.mem.eql(u8, arg, "--kind") or std.mem.eql(u8, arg, "--priority") or std.mem.eql(u8, arg, "--format")) {
                const value = require_flag_value(args, &idx, arg);
                if (std.mem.eql(u8, arg, "--project")) project = value else if (std.mem.eql(u8, arg, "--title")) title = value else if (std.mem.eql(u8, arg, "--tags")) tags = value else if (std.mem.eql(u8, arg, "--kind")) {
                    if (!valid_kind(value) or std.mem.eql(u8, value, "unspecified")) {
                        std.debug.print("Error: --kind must be technical, product, business, or project\n", .{});
                        std.process.exit(1);
                    }
                    kind = value;
                } else if (std.mem.eql(u8, arg, "--priority")) {
                    if (!valid_priority(value)) {
                        std.debug.print("Error: --priority must be low, medium, or high\n", .{});
                        std.process.exit(1);
                    }
                    priority = value;
                } else format = parse_format_arg(value, false, cmd);
            } else if (std.mem.startsWith(u8, arg, "--")) {
                std.debug.print("Error: Unknown flag '{s}'\n", .{arg});
                std.process.exit(1);
            } else if (content != null) {
                std.debug.print("Error: Multiple content arguments provided\n", .{});
                std.process.exit(1);
            } else {
                content = arg;
            }
        }

        if (use_stdin or content == null) {
            if (try read_stdin(arena, io)) |stdin_content| content = stdin_content;
        }
        const final_content = content orelse {
            std.debug.print("Error: Content argument is required for 'add'\n", .{});
            std.process.exit(1);
        };
        const kind_val = kind orelse {
            std.debug.print("Error: --kind is required (technical, product, business, or project)\n", .{});
            std.process.exit(1);
        };
        const proj_name = resolve_project(arena, io, init.environ_map, project) catch |err| {
            std.debug.print("Error: Failed to resolve project: {any}\n", .{err});
            std.process.exit(1);
        };
        const title_val = if (title) |t| try truncate_title(arena, t) else try get_default_title(arena, final_content);
        if (title_val.len == 0) {
            std.debug.print("Error: Could not determine a non-empty title\n", .{});
            std.process.exit(1);
        }

        std.Io.Dir.cwd().createDirPath(io, vault_path) catch |err| {
            std.debug.print("Error: Failed to create vault directory at '{s}': {any}\n", .{ vault_path, err });
            std.process.exit(1);
        };
        var dir = std.Io.Dir.openDirAbsolute(io, vault_path, .{ .iterate = true }) catch |err| {
            std.debug.print("Error: Failed to open vault directory: {any}\n", .{err});
            std.process.exit(1);
        };
        defer dir.close(io);

        if (!allow_duplicate) {
            const existing = try collect_ideas(arena, io, dir, proj_name, null, null, null);
            for (existing) |meta| {
                if (std.ascii.eqlIgnoreCase(meta.title, title_val)) {
                    std.debug.print("Error: An idea titled '{s}' already exists for project '{s}' ({s}). Use --allow-duplicate to add it anyway.\n", .{ title_val, proj_name, meta.id });
                    std.process.exit(1);
                }
            }
        }

        const ts = std.Io.Timestamp.now(io, .real);
        const seconds = ts.toSeconds();
        const total_ns = ts.toNanoseconds();
        const id_seed = try std.fmt.allocPrint(arena, "{d}:{s}:{s}", .{ total_ns, proj_name, title_val });
        const id = try derive_id(arena, id_seed);
        const filename = try std.fmt.allocPrint(arena, "{s}.md", .{id});
        if (dir.statFile(io, filename, .{})) |_| {
            std.debug.print("Error: Generated ID collision for '{s}'. Retry the command.\n", .{id});
            std.process.exit(1);
        } else |err| switch (err) {
            error.FileNotFound => {},
            else => return err,
        }
        const escaped_proj = try escape_yaml_string(arena, proj_name);
        const escaped_title = try escape_yaml_string(arena, title_val);
        const tags_line = if (tags) |value| try std.fmt.allocPrint(arena, "tags: \"{s}\"\n", .{try escape_yaml_string(arena, value)}) else "";
        const priority_line = if (priority) |value| try std.fmt.allocPrint(arena, "priority: \"{s}\"\n", .{value}) else "";
        const file_content = try std.fmt.allocPrint(arena,
            \\---
            \\schema: 1
            \\id: "{s}"
            \\project: "{s}"
            \\kind: "{s}"
            \\timestamp: {d}
            \\created_at_ns: {d}
            \\title: "{s}"
            \\{s}{s}---
            \\{s}
            \\
        , .{ id, escaped_proj, kind_val, seconds, total_ns, escaped_title, tags_line, priority_line, final_content });

        dir.writeFile(io, .{ .sub_path = filename, .data = file_content }) catch |err| {
            std.debug.print("Error: Failed to write file '{s}': {any}\n", .{ filename, err });
            std.process.exit(1);
        };

        const meta = IdeaMeta{ .schema = 1, .has_schema = true, .has_id = true, .has_kind = true, .id = id, .filename = filename, .project = proj_name, .kind = kind_val, .timestamp = seconds, .created_at_ns = total_ns, .title = title_val, .tags = tags orelse "", .priority = priority orelse "", .archived_at = 0, .resolution = "", .resolution_note = "", .body = final_content, .score = 0 };
        var out_buf: [1024]u8 = undefined;
        var w = std.Io.File.stdout().writer(io, &out_buf);
        switch (format orelse default_format(io, .plain)) {
            .json => {
                try emit_idea_json(&w.interface, meta, true);
                try w.interface.writeAll("\n");
            },
            .plain => try w.interface.print("Saved {s}  {s}\n", .{ id, title_val }),
            .table => unreachable,
        }
        try w.end();

        // ── list ─────────────────────────────────────────────────────────
    } else if (std.mem.eql(u8, cmd, "list") or std.mem.eql(u8, cmd, "list-project")) {
        var filter_project: ?[]const u8 = null;
        var filter_tag: ?[]const u8 = null;
        var filter_kind: ?[]const u8 = null;
        var archive_filter: ArchiveFilter = .active;
        var format: ?OutputFormat = null;

        if (std.mem.eql(u8, cmd, "list-project")) {
            filter_project = resolve_project(arena, io, init.environ_map, null) catch |err| {
                std.debug.print("Error: Failed to resolve project: {any}\n", .{err});
                std.process.exit(1);
            };
        }

        var idx: usize = 2;
        while (idx < args.len) : (idx += 1) {
            const arg = args[idx];
            if (std.mem.eql(u8, arg, "--archived")) {
                archive_filter = .archived;
            } else if (std.mem.eql(u8, arg, "--all")) {
                archive_filter = .all;
            } else if (std.mem.eql(u8, arg, "--project") or std.mem.eql(u8, arg, "--tag") or std.mem.eql(u8, arg, "--kind") or std.mem.eql(u8, arg, "--format")) {
                const value = require_flag_value(args, &idx, arg);
                if (std.mem.eql(u8, arg, "--project")) filter_project = value else if (std.mem.eql(u8, arg, "--tag")) filter_tag = value else if (std.mem.eql(u8, arg, "--kind")) {
                    if (!valid_kind(value)) {
                        std.debug.print("Error: --kind must be technical, product, business, project, or unspecified\n", .{});
                        std.process.exit(1);
                    }
                    filter_kind = value;
                } else format = parse_format_arg(value, true, cmd);
            } else {
                std.debug.print("Error: Unknown flag '{s}'\n", .{arg});
                std.process.exit(1);
            }
        }
        const output_format = format orelse default_format(io, .table);

        var dir = open_vault_dir(io, vault_path, true) orelse {
            var out_buf: [32]u8 = undefined;
            var w = std.Io.File.stdout().writer(io, &out_buf);
            if (output_format == .json) try w.interface.writeAll("[]\n") else if (output_format == .table) try w.interface.writeAll("No ideas found.\n");
            try w.end();
            return;
        };
        defer dir.close(io);

        const ideas = try collect_ideas_filtered(arena, io, dir, filter_project, null, filter_tag, filter_kind, archive_filter);

        var out_buf: [4096]u8 = undefined;
        var w = std.Io.File.stdout().writer(io, &out_buf);

        switch (output_format) {
            .table => if (ideas.len == 0) {
                try w.interface.writeAll("No ideas found.\n");
            } else {
                try w.interface.writeAll("DATE        PROJECT           KIND         ID            TITLE\n");
                try w.interface.writeAll("----------  ----------------  -----------  ------------  ----------------------------------------\n");
                for (ideas) |meta| try emit_idea_table(&w.interface, meta);
                try w.interface.print("\n{d} idea(s)\n", .{ideas.len});
            },
            .plain => for (ideas) |meta| try emit_idea_plain(&w.interface, meta),
            .json => {
                try w.interface.writeAll("[");
                for (ideas, 0..) |meta, i| try emit_idea_json(&w.interface, meta, i == 0);
                try w.interface.writeAll("]\n");
            },
        }
        try w.end();

        // ── search ───────────────────────────────────────────────────────
    } else if (std.mem.eql(u8, cmd, "search")) {
        if (args.len < 3) {
            std.debug.print("Error: 'search' subcommand requires a query argument\n", .{});
            std.process.exit(1);
        }
        const query = args[2];
        var filter_project: ?[]const u8 = null;
        var filter_tag: ?[]const u8 = null;
        var filter_kind: ?[]const u8 = null;
        var archive_filter: ArchiveFilter = .active;
        var limit: usize = std.math.maxInt(usize);
        var format: ?OutputFormat = null;

        var idx: usize = 3;
        while (idx < args.len) : (idx += 1) {
            const arg = args[idx];
            if (std.mem.eql(u8, arg, "--archived")) {
                archive_filter = .archived;
            } else if (std.mem.eql(u8, arg, "--all")) {
                archive_filter = .all;
            } else if (std.mem.eql(u8, arg, "--project") or std.mem.eql(u8, arg, "--tag") or std.mem.eql(u8, arg, "--kind") or std.mem.eql(u8, arg, "--limit") or std.mem.eql(u8, arg, "--format")) {
                const value = require_flag_value(args, &idx, arg);
                if (std.mem.eql(u8, arg, "--project")) filter_project = value else if (std.mem.eql(u8, arg, "--tag")) filter_tag = value else if (std.mem.eql(u8, arg, "--kind")) {
                    if (!valid_kind(value)) {
                        std.debug.print("Error: --kind must be technical, product, business, project, or unspecified\n", .{});
                        std.process.exit(1);
                    }
                    filter_kind = value;
                } else if (std.mem.eql(u8, arg, "--limit")) {
                    limit = std.fmt.parseInt(usize, value, 10) catch {
                        std.debug.print("Error: --limit must be a non-negative integer\n", .{});
                        std.process.exit(1);
                    };
                } else format = parse_format_arg(value, true, cmd);
            } else {
                std.debug.print("Error: Unknown flag '{s}'\n", .{arg});
                std.process.exit(1);
            }
        }
        const output_format = format orelse default_format(io, .table);

        var dir = open_vault_dir(io, vault_path, true) orelse {
            var out_buf: [32]u8 = undefined;
            var w = std.Io.File.stdout().writer(io, &out_buf);
            if (output_format == .json) try w.interface.writeAll("[]\n") else if (output_format == .table) try w.interface.writeAll("No ideas found.\n");
            try w.end();
            return;
        };
        defer dir.close(io);

        const ideas = try collect_ideas_filtered(arena, io, dir, filter_project, query, filter_tag, filter_kind, archive_filter);
        const selected = ideas[0..@min(limit, ideas.len)];

        var out_buf: [4096]u8 = undefined;
        var w = std.Io.File.stdout().writer(io, &out_buf);

        switch (output_format) {
            .table => if (selected.len == 0) {
                try w.interface.print("No results for \"{s}\".\n", .{query});
            } else {
                try w.interface.writeAll("DATE        PROJECT           KIND         ID            TITLE\n");
                try w.interface.writeAll("----------  ----------------  -----------  ------------  ----------------------------------------\n");
                for (selected) |meta| try emit_idea_table(&w.interface, meta);
                try w.interface.print("\n{d} result(s) for \"{s}\"\n", .{ selected.len, query });
            },
            .plain => for (selected) |meta| try emit_idea_plain(&w.interface, meta),
            .json => {
                try w.interface.writeAll("[");
                for (selected, 0..) |meta, i| try emit_idea_json(&w.interface, meta, i == 0);
                try w.interface.writeAll("]\n");
            },
        }
        try w.end();

        // ── context ──────────────────────────────────────────────────────
    } else if (std.mem.eql(u8, cmd, "context")) {
        var filter_project: ?[]const u8 = null;
        var filter_kind: ?[]const u8 = null;
        var archive_filter: ArchiveFilter = .active;
        var limit: usize = 10;
        var group_kind = false;
        var format: ?OutputFormat = null;
        var idx: usize = 2;
        while (idx < args.len) : (idx += 1) {
            const arg = args[idx];
            if (std.mem.eql(u8, arg, "--archived")) {
                archive_filter = .archived;
            } else if (std.mem.eql(u8, arg, "--all")) {
                archive_filter = .all;
            } else if (std.mem.eql(u8, arg, "--project") or std.mem.eql(u8, arg, "--kind") or std.mem.eql(u8, arg, "--limit") or std.mem.eql(u8, arg, "--group") or std.mem.eql(u8, arg, "--format")) {
                const value = require_flag_value(args, &idx, arg);
                if (std.mem.eql(u8, arg, "--project")) filter_project = value else if (std.mem.eql(u8, arg, "--kind")) {
                    if (!valid_kind(value)) {
                        std.debug.print("Error: --kind must be technical, product, business, project, or unspecified\n", .{});
                        std.process.exit(1);
                    }
                    filter_kind = value;
                } else if (std.mem.eql(u8, arg, "--limit")) {
                    limit = std.fmt.parseInt(usize, value, 10) catch {
                        std.debug.print("Error: --limit must be a non-negative integer\n", .{});
                        std.process.exit(1);
                    };
                } else if (std.mem.eql(u8, arg, "--group")) {
                    if (!std.mem.eql(u8, value, "kind")) {
                        std.debug.print("Error: --group currently supports only 'kind'\n", .{});
                        std.process.exit(1);
                    }
                    group_kind = true;
                } else format = parse_format_arg(value, false, cmd);
            } else {
                std.debug.print("Error: Unknown flag '{s}'\n", .{arg});
                std.process.exit(1);
            }
        }
        if (filter_project == null) {
            filter_project = resolve_project(arena, io, init.environ_map, null) catch |err| {
                std.debug.print("Error: Failed to resolve project: {any}\n", .{err});
                std.process.exit(1);
            };
        }
        const output_format = format orelse default_format(io, .plain);
        var dir = open_vault_dir(io, vault_path, true) orelse {
            var empty_buf: [4]u8 = undefined;
            var empty_writer = std.Io.File.stdout().writer(io, &empty_buf);
            if (output_format == .json) try empty_writer.interface.writeAll("[]\n");
            try empty_writer.end();
            return;
        };
        defer dir.close(io);
        const ideas = try collect_ideas_filtered(arena, io, dir, filter_project, null, null, filter_kind, archive_filter);
        std.sort.block(IdeaMeta, ideas, {}, IdeaMeta.contextLessThan);
        const selected = ideas[0..@min(limit, ideas.len)];
        var out_buf: [4096]u8 = undefined;
        var w = std.Io.File.stdout().writer(io, &out_buf);
        switch (output_format) {
            .json => {
                try w.interface.writeAll("[");
                for (selected, 0..) |meta, i| try emit_idea_json(&w.interface, meta, i == 0);
                try w.interface.writeAll("]\n");
            },
            .plain => {
                if (selected.len > 0) try w.interface.print("{s} proposals for {s}:\n", .{ switch (archive_filter) {
                    .active => "Active",
                    .archived => "Archived",
                    .all => "All",
                }, filter_project.? });
                if (group_kind) {
                    for (kind_names) |kind| {
                        var has_kind = false;
                        for (selected) |meta| {
                            if (std.mem.eql(u8, meta.kind, kind)) {
                                if (!has_kind) {
                                    try w.interface.print("\n{s}:\n", .{kind_label(kind)});
                                    has_kind = true;
                                }
                                try w.interface.print("- [{s}] {s}", .{ meta.id, meta.title });
                                if (meta.tags.len > 0) try w.interface.print(" [{s}]", .{meta.tags});
                                if (meta.priority.len > 0) try w.interface.print(" ({s})", .{meta.priority});
                                try w.interface.writeAll("\n");
                            }
                        }
                    }
                } else {
                    for (selected) |meta| {
                        try w.interface.print("- [{s}] [{s}] {s}", .{ meta.id, meta.kind, meta.title });
                        if (meta.tags.len > 0) try w.interface.print(" [{s}]", .{meta.tags});
                        if (meta.priority.len > 0) try w.interface.print(" ({s})", .{meta.priority});
                        try w.interface.writeAll("\n");
                    }
                }
            },
            .table => unreachable,
        }
        try w.end();

        // ── doctor ────────────────────────────────────────────────────────
    } else if (std.mem.eql(u8, cmd, "doctor")) {
        var repair = false;
        var strict = false;
        var format: ?OutputFormat = null;
        var idx: usize = 2;
        while (idx < args.len) : (idx += 1) {
            const arg = args[idx];
            if (std.mem.eql(u8, arg, "--repair")) {
                repair = true;
            } else if (std.mem.eql(u8, arg, "--strict")) {
                strict = true;
            } else if (std.mem.eql(u8, arg, "--format")) {
                format = parse_format_arg(require_flag_value(args, &idx, arg), false, cmd);
            } else {
                std.debug.print("Error: Unknown flag '{s}'\n", .{arg});
                std.process.exit(2);
            }
        }
        const output_format = format orelse default_format(io, .plain);
        var dir = open_vault_dir(io, vault_path, true) orelse {
            const empty_scan = VaultScan{ .ideas = &.{}, .issues = &.{}, .files_scanned = 0 };
            try emit_doctor_report(io, vault_path, empty_scan, 0, output_format);
            return;
        };
        defer dir.close(io);
        const repaired = if (repair) try repair_vault(arena, io, dir) else 0;
        const scan = try scan_vault(arena, io, dir);
        try emit_doctor_report(io, vault_path, scan, repaired, output_format);
        if (scan_has_failures(scan, strict)) std.process.exit(1);

        // ── archive / unarchive ───────────────────────────────────────────
    } else if (std.mem.eql(u8, cmd, "archive") or std.mem.eql(u8, cmd, "unarchive")) {
        if (args.len < 3) {
            std.debug.print("Error: '{s}' requires an ID, ID prefix, or filename\n", .{cmd});
            std.process.exit(1);
        }
        const selector = args[2];
        const is_archive = std.mem.eql(u8, cmd, "archive");
        var resolution: ?[]const u8 = null;
        var note: ?[]const u8 = null;
        var format: ?OutputFormat = null;
        var idx: usize = 3;
        while (idx < args.len) : (idx += 1) {
            const arg = args[idx];
            if (std.mem.eql(u8, arg, "--format")) {
                format = parse_format_arg(require_flag_value(args, &idx, arg), false, cmd);
            } else if (is_archive and std.mem.eql(u8, arg, "--resolution")) {
                const value = require_flag_value(args, &idx, arg);
                if (!valid_resolution(value)) {
                    std.debug.print("Error: --resolution must be implemented, rejected, superseded, or stale\n", .{});
                    std.process.exit(1);
                }
                resolution = value;
            } else if (is_archive and std.mem.eql(u8, arg, "--note")) {
                note = require_flag_value(args, &idx, arg);
            } else {
                std.debug.print("Error: Unknown flag '{s}'\n", .{arg});
                std.process.exit(1);
            }
        }
        if (!validate_filename(selector)) {
            std.debug.print("Error: Invalid selector '{s}'\n", .{selector});
            std.process.exit(1);
        }
        var dir = open_vault_or_exit(io, vault_path);
        defer dir.close(io);
        const filename = resolve_selector_or_exit(arena, io, dir, selector);
        const content = try dir.readFileAlloc(io, filename, arena, .unlimited);
        var validation_issues: std.ArrayList(Issue) = .empty;
        const raw_meta = (try parse_front_matter_detailed(arena, content, filename, &validation_issues)) orelse {
            std.debug.print("Error: Cannot {s} malformed pin '{s}'\n", .{ cmd, filename });
            std.process.exit(1);
        };
        for (validation_issues.items) |issue| if (issue.severity == .err) {
            std.debug.print("Error: Cannot {s} '{s}': {s}\n", .{ cmd, filename, issue.message });
            std.process.exit(1);
        };
        if (is_archive and raw_meta.archived_at > 0) {
            std.debug.print("Error: '{s}' is already archived\n", .{filename});
            std.process.exit(1);
        }
        if (!is_archive and raw_meta.archived_at == 0) {
            std.debug.print("Error: '{s}' is not archived\n", .{filename});
            std.process.exit(1);
        }

        const idea_id = if (raw_meta.id.len > 0) raw_meta.id else try derive_id(arena, filename);
        var updates: std.ArrayList(FieldUpdate) = .empty;
        if (!raw_meta.has_schema) try updates.append(arena, .{ .key = "schema", .value = "1" });
        if (!raw_meta.has_id) try updates.append(arena, .{ .key = "id", .value = try yaml_quoted(arena, idea_id) });
        if (is_archive) {
            const archived_value = try std.fmt.allocPrint(arena, "{d}", .{std.Io.Timestamp.now(io, .real).toSeconds()});
            try updates.append(arena, .{ .key = "archived_at", .value = archived_value });
            if (resolution) |value| try updates.append(arena, .{ .key = "resolution", .value = try yaml_quoted(arena, value) });
            if (note) |value| try updates.append(arena, .{ .key = "resolution_note", .value = try yaml_quoted(arena, value) });
        } else {
            try updates.append(arena, .{ .key = "archived_at", .value = null });
            try updates.append(arena, .{ .key = "resolution", .value = null });
            try updates.append(arena, .{ .key = "resolution_note", .value = null });
        }
        const rewritten = try rewrite_front_matter(arena, content, updates.items);
        try atomic_write(arena, io, dir, filename, rewritten);

        var out_buf: [512]u8 = undefined;
        var w = std.Io.File.stdout().writer(io, &out_buf);
        if ((format orelse default_format(io, .plain)) == .json) {
            try w.interface.print("{{\"{s}\":\"", .{if (is_archive) "archived" else "unarchived"});
            try print_escaped_json(&w.interface, idea_id);
            try w.interface.writeAll("\",\"filename\":\"");
            try print_escaped_json(&w.interface, filename);
            try w.interface.writeAll("\"}\n");
        } else {
            try w.interface.print("{s} {s}  {s}\n", .{ if (is_archive) "Archived" else "Unarchived", idea_id, filename });
        }
        try w.end();

        // ── import / export ───────────────────────────────────────────────
    } else if (std.mem.eql(u8, cmd, "import") or std.mem.eql(u8, cmd, "export")) {
        if (args.len < 3) {
            std.debug.print("Error: '{s}' requires a directory\n", .{cmd});
            std.process.exit(1);
        }
        const path = args[2];
        var force = false;
        var format: ?OutputFormat = null;
        var idx: usize = 3;
        while (idx < args.len) : (idx += 1) {
            if (std.mem.eql(u8, args[idx], "--force")) {
                force = true;
            } else if (std.mem.eql(u8, args[idx], "--format")) {
                format = parse_format_arg(require_flag_value(args, &idx, "--format"), false, cmd);
            } else {
                std.debug.print("Error: Unknown flag '{s}'\n", .{args[idx]});
                std.process.exit(1);
            }
        }
        const is_import = std.mem.eql(u8, cmd, "import");
        if (is_import) try std.Io.Dir.cwd().createDirPath(io, vault_path) else try std.Io.Dir.cwd().createDirPath(io, path);

        var source = if (is_import)
            std.Io.Dir.cwd().openDir(io, path, .{ .iterate = true }) catch |err| {
                std.debug.print("Error: Failed to open import directory '{s}': {any}\n", .{ path, err });
                std.process.exit(1);
            }
        else
            open_vault_dir(io, vault_path, true) orelse {
                std.debug.print("Error: Vault not found.\n", .{});
                std.process.exit(1);
            };
        defer source.close(io);
        var destination = if (is_import)
            try std.Io.Dir.openDirAbsolute(io, vault_path, .{})
        else
            try std.Io.Dir.cwd().openDir(io, path, .{});
        defer destination.close(io);

        // Validate every import candidate before writing anything so malformed
        // input cannot leave the destination vault partially populated.
        if (is_import) {
            var validation_iterator = source.iterate();
            while (try validation_iterator.next(io)) |entry| {
                if (entry.kind != .file or !std.mem.endsWith(u8, entry.name, ".md")) continue;
                const content = try source.readFileAlloc(io, entry.name, arena, .unlimited);
                var import_issues: std.ArrayList(Issue) = .empty;
                const meta = try parse_front_matter_detailed(arena, content, entry.name, &import_issues);
                var invalid = meta == null;
                for (import_issues.items) |issue| if (issue.severity == .err) {
                    invalid = true;
                    break;
                };
                if (invalid) {
                    std.debug.print("Error: '{s}' is not a valid pin file\n", .{entry.name});
                    std.process.exit(1);
                }
            }
        }

        var copied: usize = 0;
        var skipped: usize = 0;
        var iterator = source.iterate();
        while (try iterator.next(io)) |entry| {
            if (entry.kind != .file or !std.mem.endsWith(u8, entry.name, ".md")) continue;
            if (!force) {
                if (destination.statFile(io, entry.name, .{})) |_| {
                    skipped += 1;
                    continue;
                } else |err| switch (err) {
                    error.FileNotFound => {},
                    else => return err,
                }
            }
            const content = try source.readFileAlloc(io, entry.name, arena, .unlimited);
            try destination.writeFile(io, .{ .sub_path = entry.name, .data = content });
            copied += 1;
        }

        var out_buf: [256]u8 = undefined;
        var w = std.Io.File.stdout().writer(io, &out_buf);
        if ((format orelse default_format(io, .plain)) == .json) {
            try w.interface.print("{{\"operation\":\"{s}\",\"copied\":{d},\"skipped\":{d}}}\n", .{ cmd, copied, skipped });
        } else try w.interface.print("{s}: {d} copied, {d} skipped\n", .{ cmd, copied, skipped });
        try w.end();

        // ── read ─────────────────────────────────────────────────────────
    } else if (std.mem.eql(u8, cmd, "read")) {
        const selector_args = parse_selector_args(args, cmd);
        const format = selector_args.format orelse .plain;
        var dir = open_vault_or_exit(io, vault_path);
        defer dir.close(io);
        const filename = resolve_selector_or_exit(arena, io, dir, selector_args.selector);

        const content = dir.readFileAlloc(io, filename, arena, .unlimited) catch |err| {
            if (err == error.FileNotFound) {
                std.debug.print("Error: File '{s}' not found in vault.\n", .{filename});
            } else {
                std.debug.print("Error: Failed to read file '{s}': {any}\n", .{ filename, err });
            }
            std.process.exit(1);
        };

        if (format == .plain) {
            const stdout_file = std.Io.File.stdout();
            stdout_file.writeStreamingAll(io, content) catch |err| {
                std.debug.print("Error: Failed to write to stdout: {any}\n", .{err});
                std.process.exit(1);
            };
        } else {
            var out_buf: [4096]u8 = undefined;
            var w = std.Io.File.stdout().writer(io, &out_buf);
            try w.interface.writeAll("{\"filename\":\"");
            try print_escaped_json(&w.interface, filename);
            try w.interface.writeAll("\",\"content\":\"");
            try print_escaped_json(&w.interface, content);
            try w.interface.writeAll("\"}\n");
            try w.end();
        }

        // ── rm ───────────────────────────────────────────────────────────
    } else if (std.mem.eql(u8, cmd, "rm")) {
        const selector_args = parse_selector_args(args, cmd);
        var dir = open_vault_or_exit(io, vault_path);
        defer dir.close(io);
        const filename = resolve_selector_or_exit(arena, io, dir, selector_args.selector);
        const removed_id = try idea_id_for_file(arena, io, dir, filename);
        dir.deleteFile(io, filename) catch |err| {
            std.debug.print("Error: Failed to delete file '{s}': {any}\n", .{ filename, err });
            std.process.exit(1);
        };

        var out_buf: [256]u8 = undefined;
        var w = std.Io.File.stdout().writer(io, &out_buf);
        if ((selector_args.format orelse default_format(io, .plain)) == .json) {
            try w.interface.writeAll("{\"removed\":\"");
            try print_escaped_json(&w.interface, removed_id);
            try w.interface.writeAll("\",\"filename\":\"");
            try print_escaped_json(&w.interface, filename);
            try w.interface.writeAll("\"}\n");
        } else try w.interface.print("Removed {s}  {s}\n", .{ removed_id, filename });
        try w.end();

        // ── edit ─────────────────────────────────────────────────────────
    } else if (std.mem.eql(u8, cmd, "edit")) {
        const selector_args = parse_selector_args(args, cmd);
        var dir = open_vault_or_exit(io, vault_path);
        const filename = resolve_selector_or_exit(arena, io, dir, selector_args.selector);
        const original_content = try dir.readFileAlloc(io, filename, arena, .unlimited);
        const edited_id = try idea_id_from_content(arena, filename, original_content);
        dir.close(io);

        const full_path = try std.fs.path.join(arena, &.{ vault_path, filename });

        const fallback_editor: []const u8 = if (builtin.os.tag == .windows) "notepad" else "vi";
        const editor = init.environ_map.get("EDITOR") orelse
            init.environ_map.get("VISUAL") orelse
            fallback_editor;

        var editor_args: std.ArrayList([]const u8) = .empty;
        var words = std.mem.tokenizeAny(u8, editor, " \t");
        while (words.next()) |word| try editor_args.append(arena, word);
        if (editor_args.items.len == 0) try editor_args.append(arena, fallback_editor);
        try editor_args.append(arena, full_path);

        var child = std.process.spawn(io, .{
            .argv = editor_args.items,
            .stdin = .inherit,
            .stdout = .inherit,
            .stderr = .inherit,
        }) catch |err| {
            std.debug.print("Error: Failed to launch editor '{s}': {any}\n", .{ editor, err });
            std.process.exit(1);
        };
        const term = try child.wait(io);

        switch (term) {
            .exited => |code| {
                if (code != 0) {
                    std.debug.print("Editor exited with code {d}\n", .{code});
                    std.process.exit(1);
                }
            },
            else => {
                std.debug.print("Editor terminated abnormally\n", .{});
                std.process.exit(1);
            },
        }

        var validation_dir = open_vault_or_exit(io, vault_path);
        defer validation_dir.close(io);
        const edited_content = validation_dir.readFileAlloc(io, filename, arena, .unlimited) catch |err| {
            try atomic_write(arena, io, validation_dir, filename, original_content);
            std.debug.print("Error: Editor removed or made '{s}' unreadable ({any}); original restored\n", .{ filename, err });
            std.process.exit(1);
        };
        var edit_issues: std.ArrayList(Issue) = .empty;
        const edited_meta = try parse_front_matter_detailed(arena, edited_content, filename, &edit_issues);
        var invalid_edit = edited_meta == null;
        for (edit_issues.items) |issue| if (issue.severity == .err) {
            invalid_edit = true;
            std.debug.print("Error: {s}: {s} [{s}]\n", .{ filename, issue.message, issue.code });
        };
        if (invalid_edit) {
            const recovery_name = try std.fmt.allocPrint(arena, ".{s}.edit-recovery.tmp", .{edited_id});
            try validation_dir.writeFile(io, .{ .sub_path = recovery_name, .data = edited_content });
            try atomic_write(arena, io, validation_dir, filename, original_content);
            std.debug.print("Error: Invalid edit was not applied. Recovery saved to {s}/{s}\n", .{ vault_path, recovery_name });
            std.process.exit(1);
        }

        var out_buf: [256]u8 = undefined;
        var w = std.Io.File.stdout().writer(io, &out_buf);
        if ((selector_args.format orelse default_format(io, .plain)) == .json) {
            try w.interface.writeAll("{\"edited\":\"");
            try print_escaped_json(&w.interface, edited_id);
            try w.interface.writeAll("\",\"filename\":\"");
            try print_escaped_json(&w.interface, filename);
            try w.interface.writeAll("\"}\n");
        } else try w.interface.print("Edited {s}  {s}\n", .{ edited_id, filename });
        try w.end();

        // ── stats ────────────────────────────────────────────────────────
    } else if (std.mem.eql(u8, cmd, "stats")) {
        var format: ?OutputFormat = null;
        var idx: usize = 2;
        while (idx < args.len) : (idx += 1) {
            if (std.mem.eql(u8, args[idx], "--format")) {
                format = parse_format_arg(require_flag_value(args, &idx, "--format"), false, cmd);
            } else {
                std.debug.print("Error: Unexpected argument '{s}'\n", .{args[idx]});
                std.process.exit(1);
            }
        }
        const output_format = format orelse default_format(io, .plain);
        var dir = open_vault_dir(io, vault_path, true) orelse {
            var out_buf: [std.fs.max_path_bytes + 128]u8 = undefined;
            var w = std.Io.File.stdout().writer(io, &out_buf);
            if (output_format == .json) {
                try w.interface.writeAll("{\"vault\":\"");
                try print_escaped_json(&w.interface, vault_path);
                try w.interface.writeAll("\",\"ideas\":0,\"active\":0,\"archived\":0,\"invalid\":0,\"projects\":[],\"tags\":[],\"kinds\":{\"technical\":0,\"product\":0,\"business\":0,\"project\":0,\"unspecified\":0}}\n");
            } else try w.interface.print("Vault:    {s}\nIdeas:    0\nActive:   0\nArchived: 0\nInvalid:  0\n", .{vault_path});
            try w.end();
            return;
        };
        defer dir.close(io);

        const scan = try scan_vault(arena, io, dir);
        const ideas = scan.ideas;
        std.sort.block(IdeaMeta, ideas, {}, IdeaMeta.lessThan);
        const invalid_count = scan.files_scanned - ideas.len;

        var total: usize = 0;
        var active_count: usize = 0;
        var archived_count: usize = 0;
        var oldest: i64 = std.math.maxInt(i64);
        var newest: i64 = 0;
        var kind_counts: [5]usize = .{0} ** 5;

        // Count unique projects, tags, and domains
        var projects: std.ArrayList([]const u8) = .empty;
        var tag_list: std.ArrayList([]const u8) = .empty;

        for (ideas) |meta| {
            total += 1;
            if (meta.archived_at > 0) archived_count += 1 else active_count += 1;
            kind_counts[kind_index(meta.kind)] += 1;
            if (meta.timestamp < oldest) oldest = meta.timestamp;
            if (meta.timestamp > newest) newest = meta.timestamp;

            // Deduplicate projects
            var found = false;
            for (projects.items) |p| {
                if (std.mem.eql(u8, p, meta.project)) {
                    found = true;
                    break;
                }
            }
            if (!found and meta.project.len > 0) {
                try projects.append(arena, meta.project);
            }

            // Deduplicate tags
            if (meta.tags.len > 0) {
                var pos: usize = 0;
                while (pos < meta.tags.len) {
                    const end = std.mem.indexOfScalarPos(u8, meta.tags, pos, ',') orelse meta.tags.len;
                    const tag = std.mem.trim(u8, meta.tags[pos..end], " ");
                    if (tag.len > 0) {
                        var tag_found = false;
                        for (tag_list.items) |t| {
                            if (std.ascii.eqlIgnoreCase(t, tag)) {
                                tag_found = true;
                                break;
                            }
                        }
                        if (!tag_found) try tag_list.append(arena, tag);
                    }
                    if (end >= meta.tags.len) break;
                    pos = end + 1;
                }
            }
        }

        var out_buf: [4096]u8 = undefined;
        var w = std.Io.File.stdout().writer(io, &out_buf);

        if (output_format == .json) {
            try w.interface.writeAll("{\"vault\":\"");
            try print_escaped_json(&w.interface, vault_path);
            try w.interface.print("\",\"ideas\":{d},\"active\":{d},\"archived\":{d},\"invalid\":{d},\"projects\":[", .{ total, active_count, archived_count, invalid_count });
            for (projects.items, 0..) |project, i| {
                if (i > 0) try w.interface.writeAll(",");
                try w.interface.writeAll("\"");
                try print_escaped_json(&w.interface, project);
                try w.interface.writeAll("\"");
            }
            try w.interface.writeAll("],\"tags\":[");
            for (tag_list.items, 0..) |tag, i| {
                if (i > 0) try w.interface.writeAll(",");
                try w.interface.writeAll("\"");
                try print_escaped_json(&w.interface, tag);
                try w.interface.writeAll("\"");
            }
            try w.interface.writeAll("],\"kinds\":{");
            for (kind_names, 0..) |kind, i| {
                if (i > 0) try w.interface.writeAll(",");
                try w.interface.print("\"{s}\":{d}", .{ kind, kind_counts[i] });
            }
            try w.interface.writeAll("}");
            if (total > 0) try w.interface.print(",\"oldest\":{d},\"newest\":{d}", .{ oldest, newest });
            try w.interface.writeAll("}\n");
        } else {
            try w.interface.print("Vault:    {s}\n", .{vault_path});
            try w.interface.print("Ideas:    {d}\n", .{total});
            try w.interface.print("Active:   {d}\nArchived: {d}\nInvalid:  {d}\n", .{ active_count, archived_count, invalid_count });
            try w.interface.print("Projects: {d}\n", .{projects.items.len});
            if (projects.items.len > 0) {
                try w.interface.writeAll("          ");
                for (projects.items, 0..) |project, i| {
                    if (i > 0) try w.interface.writeAll(", ");
                    try w.interface.print("{s}", .{project});
                }
                try w.interface.writeAll("\n");
            }
            try w.interface.writeAll("Kinds:    ");
            for (kind_names, 0..) |kind, i| {
                if (i > 0) try w.interface.writeAll(", ");
                try w.interface.print("{s}={d}", .{ kind, kind_counts[i] });
            }
            try w.interface.writeAll("\n");
            if (tag_list.items.len > 0) {
                try w.interface.print("Tags:     {d}\n          ", .{tag_list.items.len});
                for (tag_list.items, 0..) |tag, i| {
                    if (i > 0) try w.interface.writeAll(", ");
                    try w.interface.print("{s}", .{tag});
                }
                try w.interface.writeAll("\n");
            }
            if (total > 0) {
                try w.interface.writeAll("Oldest:   ");
                try format_date(&w.interface, oldest);
                try w.interface.writeAll("\nNewest:   ");
                try format_date(&w.interface, newest);
                try w.interface.writeAll("\n");
            }
        }

        try w.end();
    } else if (std.mem.eql(u8, cmd, "view") or std.mem.eql(u8, cmd, "view-project")) {
        const project_scoped = std.mem.eql(u8, cmd, "view-project");
        const view_args = parse_view_args(args, cmd, project_scoped);
        try run_view_command(arena, io, init.environ_map, vault_path, view_args);
    } else {
        std.debug.print("Error: Unknown subcommand '{s}'\n\n", .{cmd});
        try print_usage(io);
        std.process.exit(1);
    }
}

test "front matter diagnostics reject malformed values" {
    var arena_state = std.heap.ArenaAllocator.init(std.testing.allocator);
    defer arena_state.deinit();
    const arena = arena_state.allocator();
    const content =
        "---\n" ++
        "schema: 1\n" ++
        "id: \"abcdef123456\"\n" ++
        "project: \"example\"\n" ++
        "kind: \"unknown\"\n" ++
        "timestamp: nope\n" ++
        "title: \"Broken\"\n" ++
        "---\n" ++
        "# Broken\n";
    var issues: std.ArrayList(Issue) = .empty;
    _ = try parse_front_matter_detailed(arena, content, "broken.md", &issues);
    var saw_kind = false;
    var saw_integer = false;
    for (issues.items) |issue| {
        if (std.mem.eql(u8, issue.code, "invalid_kind")) saw_kind = true;
        if (std.mem.eql(u8, issue.code, "invalid_integer")) saw_integer = true;
    }
    try std.testing.expect(saw_kind);
    try std.testing.expect(saw_integer);
    try std.testing.expect((try parse_front_matter_from_buf(arena, content)) == null);
}

test "front matter rewrite preserves body and unknown fields" {
    var arena_state = std.heap.ArenaAllocator.init(std.testing.allocator);
    defer arena_state.deinit();
    const arena = arena_state.allocator();
    const content =
        "---\n" ++
        "project: \"example\"\n" ++
        "custom: \"keep me\"\n" ++
        "title: \"Legacy\"\n" ++
        "---\n" ++
        "# Legacy\n\nBody.\n";
    const rewritten = try rewrite_front_matter(arena, content, &.{
        .{ .key = "schema", .value = "1" },
        .{ .key = "id", .value = "\"abcdef123456\"" },
    });
    try std.testing.expect(std.mem.indexOf(u8, rewritten, "custom: \"keep me\"") != null);
    try std.testing.expect(std.mem.endsWith(u8, rewritten, "# Legacy\n\nBody.\n"));
}

test "search ranks title matches above body matches" {
    var arena_state = std.heap.ArenaAllocator.init(std.testing.allocator);
    defer arena_state.deinit();
    const arena = arena_state.allocator();
    const title_content =
        "---\nproject: \"p\"\nkind: \"product\"\ntimestamp: 1\ntitle: \"Ranked results\"\n---\nBody.\n";
    const body_content =
        "---\nproject: \"p\"\nkind: \"technical\"\ntimestamp: 2\ntitle: \"Incidental\"\n---\nRanked results appear here.\n";
    const title_meta = (try parse_front_matter_from_buf(arena, title_content)).?;
    const body_meta = (try parse_front_matter_from_buf(arena, body_content)).?;
    try std.testing.expect(search_score(title_meta, "ranked results").? > search_score(body_meta, "ranked results").?);
}
